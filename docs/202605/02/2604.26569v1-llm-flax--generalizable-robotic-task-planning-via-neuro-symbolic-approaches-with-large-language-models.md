---
title: "LLM-Flax : Generalizable Robotic Task Planning via Neuro-Symbolic Approaches with Large Language Models"
title_zh: LLM-Flax：通过大语言模型神经符号方法实现可泛化的机器人任务规划
authors: "Seongmin Kim, Daegyu Lee"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26569v1"
tags: ["query:vla"]
score: 8.0
evidence: 通过大语言模型的神经符号方法实现通用的机器人任务规划
tldr: 本研究提出LLM-Flax，一个旨在消除神经符号任务规划中人工干预的三阶段框架。传统方法依赖专家编写规则和大量训练数据，而LLM-Flax仅需PDDL文件，通过LLM自动生成松弛规则、引导失败恢复并进行零样本物体重要性评分。在MazeNamo基准测试中，该框架在多个难度下均优于人工基准，显著提升了机器人任务规划的泛化能力和自动化水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26569v1/fig-001.webp\", \"caption\": \"Fig. 1: Overview of LLM-Flax. Given only a PDDL domain file, Stage 1 automatically generates relaxation and complementary rules; Stage 2 provides budget-aware LLM failure recovery; and Stage 3 replaces the domain-trained GNN with zero-shot LLM object scoring. Together, the full Full LLM-Flax system deploys on a new domain with no manual engineering.\", \"page\": 2, \"index\": 1, \"width\": 914, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2604.26569v1/fig-002.webp\", \"caption\": \"TABLE VIII: Full four-way comparison across all eight MazeNamo benchmarks. SR = success rate (↑). Pure FD and PLOI results from Appendix experiments; Manual and LLMFlax from Table I.\", \"page\": 10, \"index\": 2, \"width\": 529, \"height\": 1231}, {\"url\": \"assets/figures/arxiv/2604.26569v1/fig-003.webp\", \"caption\": \"Fig. 2: Detailed architecture of LLM-Flax. Left: Stage 1 generates relaxation and complementary rules offline from the PDDL domain file via structured LLM prompting with validation and self-correction. Center: At test time, the three-step Flax planning loop (Step 1: threshold-decay pruning; Step 2: relaxation; Step 3: complementary expansion) is shared by all stages. Right: Stage 2 inserts a feasibility-gated LLM recovery call between a Step 1 timeout and the Step 2 fallback; Stage 3 replaces the GNN scorer with a zero-shot LLM object scorer. Dashed components require no manual authoring or training data.\", \"page\": 3, \"index\": 3, \"width\": 914, \"height\": 546}, {\"url\": \"assets/figures/arxiv/2604.26569v1/fig-004.webp\", \"caption\": \"TABLE I: Flax (manual rules) vs. LLM-Flax (LLM-generated rules) within the Flax pipeline. SR = success rate (↑); Time = avg. planning time over successful problems in seconds (↓); Len = avg. plan length. Bold = better per row pair.\", \"page\": 4, \"index\": 4, \"width\": 518, \"height\": 672}, {\"url\": \"assets/figures/arxiv/2604.26569v1/fig-005.webp\", \"caption\": \"Fig. 3: Success rate vs. difficulty (Flax vs. LLM-Flax) across all grid sizes. Shaded columns mark benchmarks where LLM-Flax strictly outperforms Flax. LLM-Flax matches or exceeds Flax on every benchmark: it ties on stacking-heavy tasks (10×10, 15×15 Medium) and improves on navigation-heavy tasks (12×12 Expert: SR 0.733 vs. ALL FAIL; 15×15 Hard: 1.000 vs. 0.900).\", \"page\": 5, \"index\": 5, \"width\": 894, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.26569v1/fig-006.webp\", \"caption\": \"Fig. 4: LLM-Flax pipeline on a 10×10 hard MazeNamo problem (problem 16, 163 objects). (a) Full problem with all objects. (b) Zero-shot LLM relevance scores assigned to each object (Stage 3): goal scores 1.00, nearby obstacles score 0.5–0.9, distant objects score low. (c) After Step 1 threshold-based pruning: 21 objects excluded (grey), 142 retained at threshold 0.478. (d) Step 2 relaxed sub-problem: light boxes removed via relaxation rules, yielding a rough plan. (e) Step 3 complementary expansion: object pairs with structural constraints restored. (f) Final plan of length 39 found in 0.8 s.\", \"page\": 5, \"index\": 6, \"width\": 841, \"height\": 327}]"
motivation: 针对神经符号任务规划中需要大量专家手动编写规则和训练数据以支持GNN评分器的问题，寻求一种自动化的替代方案。
method: 提出包含自动规则生成、带预算策略的LLM引导失败恢复以及零样本LLM物体评分的三阶段框架。
result: 在MazeNamo基准测试中，LLM-Flax的平均成功率达到0.945，显著超过了人工基准的0.828，且在复杂场景下表现尤为突出。
conclusion: LLM-Flax成功消除了任务规划中的人工负担，证明了LLM在神经符号系统中的潜力，但也指出了上下文窗口限制是未来的主要挑战。
---

## 摘要
如今在新的领域部署神经符号任务规划器需要大量的人工努力：领域专家必须编写松弛规则和补充规则，并且必须解决数百个训练问题以监督图神经网络（GNN）物体评分器。我们提出了 LLM-Flax，这是一个三阶段框架，仅需提供 PDDL 领域文件，即可利用本地托管的 LLM 消除所有这三种人工努力来源。第一阶段通过带有格式验证和自我修正的结构化提示，自动生成松弛规则和补充规则。第二阶段引入了由 LLM 引导的故障恢复，采用可行性门控预算策略，在每次 LLM 调用前明确预留 API 延迟成本，防止下游松弛回退机制因资源匮乏而失效。第三阶段完全用零样本 LLM 物体重要性评分取代了领域训练的 GNN，无需任何训练数据。我们在 10x10、12x12 和 15x15 网格（共 8 个基准测试）的 MazeNamo 基准上评估了所有三个阶段。LLM-Flax 实现了 0.945 的平均成功率（SR），而人工基准为 0.828（+0.117），在所有 8 个基准测试中均达到或超过了人工规则的表现。在 12x12 Expert 任务中，LLM-Flax 的 SR 达到 0.733，而人工规划器完全失败（SR 0.000）；在 15x15 Hard 任务中，其 SR 达到 1.000，而人工规划器为 0.900。第三阶段证明了可行性（在无训练数据的 12x12 Hard 任务中 SR 为 0.720），但在大规模场景下由于上下文窗口限制面临瓶颈，这指出了未来工作的主要挑战。

## Abstract
Deploying a neuro-symbolic task planner on a new domain today requires significant manual effort: a domain expert must author relaxation and complementary rules, and hundreds of training problems must be solved to supervise a Graph Neural Network (GNN) object scorer. We propose LLM-Flax, a three-stage framework that eliminates all three sources of manual effort using a locally hosted LLM given only a PDDL domain file. Stage 1 automatically generates relaxation and complementary rules via structured prompting with format validation and self-correction. Stage 2 introduces LLM-guided failure recovery with a feasibility-gated budget policy that explicitly reserves API latency cost before each LLM call, preventing the downstream relaxation fallback from being starved. Stage 3 replaces the domain-trained GNN entirely with zero-shot LLM object importance scoring, requiring no training data. We evaluate all three stages on the MazeNamo benchmark across 10x10, 12x12, and 15x15 grids (8 benchmarks total). LLM-Flax achieves average SR 0.945 versus the manual baseline's 0.828 (+0.117), matching or outperforming manual rules on every one of the eight benchmarks. On 12x12 Expert, LLM-Flax attains SR 0.733 where the manual planner fails entirely (SR 0.000); on 15x15 Hard, it achieves SR 1.000 versus Manual's 0.900. Stage 3 demonstrates feasibility (SR 0.720 on 12x12 Hard with no training data) but faces a context-window bottleneck at scale, pointing to the primary open challenge for future work.

---

## 论文详细总结（自动生成）

这是一份关于论文《LLM-Flax: Generalizable Robotic Task Planning via Neuro-Symbolic Approaches with Large Language Models》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在复杂的机器人任务规划中，传统的符号规划器（如 Fast Downward）在处理大量物体时会面临搜索空间爆炸的问题。**神经符号方法（如 Flax）**通过引入图神经网络（GNN）预测物体重要性和手动编写“松弛规则”来简化问题，从而提升效率。
然而，Flax 的部署存在**三大人工瓶颈**：
1.  需要专家手动编写**松弛规则**（定义哪些物体可移除）。
2.  需要专家手动编写**补充规则**（定义哪些物体必须成对出现）。
3.  需要解决数百个规划问题来训练 **GNN 评分器**。
**研究动机**：利用大语言模型（LLM）的推理和代码生成能力，仅凭 PDDL 领域文件实现全自动化的规则生成和物体评分，消除人工干预，实现“即插即用”的机器人任务规划。

### 2. 论文提出的方法论：LLM-Flax 三阶段框架
LLM-Flax 旨在通过三个阶段逐步取代人工：
*   **Stage 1：LLM 规则生成（离线）**
    *   **核心思想**：通过结构化提示词（Prompting），让 LLM 理解 PDDL 谓词语义，生成 JSON 格式的松弛和补充规则。
    *   **关键技术**：引入了**验证与自我修正机制**。系统会检查 JSON 格式、索引是否为整数、谓词是否存在于 PDDL 中。若失败，则将错误反馈给 LLM 进行最多 3 次修正。
*   **Stage 2：LLM 引导的失败恢复（在线）**
    *   **核心思想**：当初始规划超时时，调用 LLM 分析当前状态和目标，识别被错误排除的关键物体。
    *   **预算策略**：为了应对 LLM API 的高延迟，提出了**可行性门控预算策略**。在调用 LLM 前预留至少 11 秒（API 延迟 + 最小重规划时间），若剩余时间不足则跳过 LLM 调用，直接进入传统的松弛回退流程，防止“预算饥饿”。
*   **Stage 3：LLM 零样本物体评分（在线）**
    *   **核心思想**：完全弃用需要训练的 GNN，直接让 LLM 根据物体类型和状态事实对物体重要性进行 0-1 打分。

### 3. 实验设计
*   **基准测试（Benchmark）**：使用 **MazeNamo** 任务，这是一个复杂的网格迷宫环境，机器人需要移动轻型箱子（可拾取）和重型箱子（仅能推）来达到目标。
*   **场景设置**：涵盖 10×10、12×12、15×15 三种网格尺寸，难度分为 Easy、Medium、Hard 和 Expert，共 8 个子基准。
*   **对比方法**：
    *   **Manual (Flax)**：使用专家手动编写规则和训练好的 GNN。
    *   **Pure FD**：纯符号规划器 Fast Downward（无神经引导）。
    *   **PLOI**：仅使用 GNN 引导但无规则简化的方法。
    *   **LLM-Flax (不同模型)**：对比了 Gemma3-12B、Qwen2.5-14B、Llama3.1-8B 和 Mistral-7B 的表现。

### 4. 资源与算力
*   **模型部署**：使用了本地托管的开源 LLM，通过 **Ollama** 框架提供 REST API 服务。
*   **具体算力**：文中**未明确说明**具体的 GPU 型号（如 A100 或 RTX 4090）及数量。
*   **训练时长**：由于 Stage 1 是离线生成（约 10 秒），Stage 3 是零样本（无需训练），因此该方法显著节省了传统方法中训练 GNN 所需的时间和算力。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个基准测试上进行了评估，每个测试包含 30 到 50 个问题实例。
*   **消融实验**：针对 Stage 2 的预算策略进行了三代迭代测试，针对 Stage 3 的提示工程（如思维链 CoT、Top-K 评分）进行了详细的对比分析。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅展示了成功案例，还详细记录了不同 LLM 模型的失败模式（如 Mistral 生成过度激进的规则），并对比了纯符号规划器的性能，证明了规则在复杂任务中的核心作用。

### 6. 论文的主要结论与发现
*   **性能超越人工**：LLM-Flax 的平均成功率（SR）为 **0.945**，优于人工基准的 0.828。在 12×12 Expert 任务中，人工规划器完全失败（SR 0.000），而 LLM-Flax 达到了 0.733。
*   **规则质量**：Gemma3-12B 生成的松弛规则与专家编写的完全一致，且生成的补充规则比人工编写的更丰富（涵盖了空间方位关系）。
*   **零样本潜力**：Stage 3 证明了在无需任何训练数据的情况下，LLM 评分器在 12×12 规模下仍能保持 0.720 的成功率。
*   **预算管理的重要性**：如果没有可行性门控策略，LLM 的 API 延迟会严重挤占规划时间，导致性能大幅下降。

### 7. 优点
*   **高度自动化**：真正实现了从 PDDL 到部署的端到端自动化，极大降低了神经符号系统的工程门槛。
*   **鲁棒的验证机制**：通过自我修正和后处理（去重、谓词过滤）确保了生成规则的语法正确性。
*   **泛化能力强**：不依赖特定领域的训练数据，理论上可直接应用于任何符合 STRIPS 标准的 PDDL 领域。

### 8. 不足与局限
*   **上下文窗口瓶颈**：在 15×15 等大规模场景下，物体数量超过 240 个，受限于 LLM 的上下文窗口和状态事实采样限制，零样本评分（Stage 3）的成功率显著下降（从 0.720 降至 0.200）。
*   **推理延迟**：LLM 的 API 调用增加了单次规划的时间开销（约 5 秒），在对实时性要求极高的场景下可能受限。
*   **模型依赖性**：性能高度依赖于底层 LLM 的推理能力，较小的模型（如 7B/8B）容易产生幻觉或生成无效规则。

（完）
