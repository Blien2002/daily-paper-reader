---
title: "CorridorVLA: Explicit Spatial Constraints for Generative Action Heads via Sparse Anchors"
title_zh: CorridorVLA：通过稀疏锚点为生成式动作头提供显式空间约束
authors: "Dachong Li, ZhuangZhuang Chen, Jin Zhang, Jianqiang Li"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21241v1"
tags: ["query:vla"]
score: 10.0
evidence: VLA模型生成式动作头的显式空间约束
tldr: 本文提出CorridorVLA，旨在解决视觉-语言-动作（VLA）模型中空间引导通常仅通过隐式特征注入的问题。该方法通过预测稀疏空间锚点作为增量物理变化，并以此构建“走廊”区域来显式约束生成式动作头。在LIBERO-Plus基准测试中，CorridorVLA显著提升了SmolVLA和GR00T的成功率，证明了显式空间约束在提升生成式动作策略性能和可解释性方面的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21241v1/fig-001.webp\", \"caption\": \"Fig. 2. Framework. (A) The backbone predicts a small set of future key spatial increments, while the action output is augmented with the corresponding end-effector displacement fields. These key increments are then used to constrain action generation, requiring only a few additional prediction slots with minimal changes to the original VLA pipeline. (B) Spatial-change guidance provides a simple prior: manipulation trajectories tend to evolve smoothly, so sparse key increments can offer a safe, structured signal that reduces unstructured exploration under stochastic generation.\", \"page\": 3, \"index\": 1, \"width\": 512, \"height\": 410}]"
motivation: 现有的VLA模型主要依赖隐式潜在特征进行空间引导，缺乏对动作生成的显式物理约束和空间指导。
method: 通过预测稀疏空间锚点定义一个物理演化“走廊”，并利用流匹配动作头对超出该区域的轨迹施加纠正梯度。
result: "在LIBERO-Plus基准测试中，该方法使基线模型的成功率提升了3.4%至12.4%，其中GR00T-Corr变体达到了83.21%的成功率。"
conclusion: 与动作对齐的物理线索能为生成式动作策略提供直接且可解释的约束，是视觉或潜在空间引导的有效补充。
---

## 摘要
视觉-语言-动作（VLA）模型通常使用中间表示来连接多模态输入与连续控制，然而空间引导往往通过潜特征隐式注入。我们提出了 CorridorVLA，它将稀疏空间锚点预测为增量物理变化（例如 Δ-位置），并利用它们在动作生成的训练目标中施加显式的容差区域。这些锚点定义了一个“走廊”，用于引导流匹配（flow-matching）动作头：如果轨迹所隐含的空间演化超出了该走廊，则会接收到修正梯度，同时允许由接触和执行噪声引起的轻微偏差。在更具挑战性的 LIBERO-Plus 基准测试中，CorridorVLA 在 SmolVLA 和 GR00T 上均取得了持续的提升，相比相应的基线模型，成功率提高了 3.4% 至 12.4%；值得注意的是，我们的 GR00T-Corr 变体达到了 83.21% 的成功率。这些结果表明，与动作对齐的物理线索可以为生成式动作策略提供直接且可解释的约束，从而补充了以视觉或潜形式编码的空间引导。代码可在 https://github.com/corridorVLA 获取。

## Abstract
Vision--Language--Action (VLA) models often use intermediate representations to connect multimodal inputs with continuous control, yet spatial guidance is often injected implicitly through latent features. We propose $CorridorVLA$, which predicts sparse spatial anchors as incremental physical changes (e.g., $Δ$-positions) and uses them to impose an explicit tolerance region in the training objective for action generation. The anchors define a corridor that guides a flow-matching action head: trajectories whose implied spatial evolution falls outside it receive corrective gradients, while minor deviations from contacts and execution noise are permitted. On the more challenging LIBERO-Plus benchmark, CorridorVLA yields consistent gains across both SmolVLA and GR00T, improving success rate by $3.4\%$--$12.4\%$ over the corresponding baselines; notably, our GR00T-Corr variant reaches a success rate of $83.21\%$. These results indicate that action-aligned physical cues can provide direct and interpretable constraints for generative action policies, complementing spatial guidance encoded in visual or latent forms. Code is available at https://github.com/corridorVLA.

---

## 论文详细总结（自动生成）

这是一份关于论文《CorridorVLA: Explicit Spatial Constraints for Generative Action Heads via Sparse Anchors》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）在处理多模态输入与连续控制时，通常将空间引导信息（如物体位置、运动趋势）隐式地编码在潜特征（Latent Features）中。这种方式存在两个问题：一是空间约束不够直接，动作生成头（Action Head）只能通过特征交互间接获取指导；二是缺乏可解释性。
**CorridorVLA 的核心动机**是探索一种轻量级的替代方案：将空间引导表达为显式的、与动作空间对齐的物理量（如末端执行器的增量位移），并以此在训练目标层面建立一个“走廊（Corridor）”约束，直接规范生成式动作轨迹的演化。

### 2. 方法论：核心思想与技术细节
CorridorVLA 的核心思想是在 VLA 的主干网络（Backbone）和动作头（Action Head）之间引入稀疏锚点作为显式约束。

*   **稀疏空间锚点预测**：
    *   模型在 Backbone 中通过少量的可学习槽位（Slots），预测未来动作块（Action Chunk）中 $K$ 个关键时间步的末端执行器（EE）的 3D 增量位置（$\Delta$-positions）。
    *   实验证明，预测“增量位置”比预测“绝对位置”更具鲁棒性，因为它减少了对相机校准和初始偏移的依赖。
*   **走廊约束目标（Corridor Regularizer）**：
    *   **走廊缓冲区（Corridor Buffer）**：定义一个容差区域 $\delta$。当生成的动作轨迹隐含的空间演化超出此区域时，施加修正梯度；在区域内则允许轻微的噪声和接触偏差。
    *   **走廊内一致性（In-corridor Consistency）**：在轨迹进入走廊后，通过阶段性累积进度损失进一步精细化轨迹。
*   **流匹配（Flow Matching）耦合**：
    *   该约束被集成到流匹配动作头中。损失函数由三部分组成：标准流匹配损失（$L_{FM}$）、锚点预测损失（$L_{\Delta p}$）和走廊正则化项（$L_{corr}$）。
    *   **噪声感知加权**：随着流匹配过程中噪声水平的变化，动态调整约束权重，在接近真实数据分布时强化空间约束。

### 3. 实验设计
*   **数据集与场景**：主要在 **LIBERO**（包含 Long, Goal, Object, Spatial 四个子集）和更具挑战性的 **LIBERO-Plus** 基准测试上进行。
*   **基线模型（Backbones）**：
    *   **SmolVLA (0.45B)**：轻量级模型，用于快速迭代和消融实验。
    *   **GR00T (N1)**：高性能 VLA 模型，用于验证方法的跨架构迁移能力。
*   **对比方法**：包括 $\pi_0$ (3.3B), GraspVLA, NORA, UniVLA, OpenVLA-OFT 等当前主流的 VLA 模型。

### 4. 资源与算力
*   **算力说明**：论文中**未明确给出**具体的 GPU 型号、数量及训练总时长。
*   **实现细节**：提到基于 LeRobot 框架（v0.32）实现，SmolVLA 的视觉编码器分辨率为 512，而 LIBERO-Plus 实验使用的是 256 分辨率。由于该方法仅增加了极少量的预测 Token（$K=3$），作者强调其推理开销几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型基准测试（LIBERO 和 LIBERO-Plus）上进行了完整评估。
    *   针对 SmolVLA 和 GR00T 两种不同规模、不同设计理念的 Backbone 进行了验证。
*   **消融实验**：非常充分。作者对比了：
    1. 走廊损失的不同组件（Buffer vs. Consistency）。
    2. 锚点选择策略（均匀采样 vs. RDP 几何感知采样）。
    3. 预测目标（绝对位置 vs. 增量位置）。
    4. Backbone 的交互方式（因果掩码 vs. 双向注意力）。
*   **客观性**：实验设置遵循了基线模型的官方默认参数，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：CorridorVLA 在所有测试中均表现出一致的增益。在 LIBERO-Plus 上，SmolVLA-Corr 比基线提升了 **12.4%**；GR00T-Corr 达到了 **83.21%** 的成功率。
*   **空间约束的有效性**：显式的物理锚点比隐式的潜特征引导更能有效减少随机生成过程中的无序探索，尤其在长程任务（Long-horizon tasks）中表现突出。
*   **增量表示的优越性**：使用 $\Delta$-positions 作为锚点比绝对坐标更稳定，更符合控制任务的本质。

### 7. 优点（亮点）
*   **轻量且高效**：仅需在 Backbone 中增加极少量的 Token（如 3 个），对模型架构改动极小，不增加推理负担。
*   **可解释性强**：通过显式的物理量约束动作生成，使得模型预测的“意图”可以通过空间锚点直观观察。
*   **通用性**：成功在两种不同的 VLA 架构上实现提升，证明了该方法作为一种通用训练目标的潜力。

### 8. 不足与局限
*   **缺乏真机验证**：所有实验均在仿真环境（LIBERO）中完成，尚未在真实机器人平台上验证其对复杂物理接触和传感器噪声的适应性。
*   **对比范围限制**：未与基于图像生成或视频预测的空间引导方法（如 ReconVLA）进行直接的“头对头”对比，尽管作者认为两者是互补关系。
*   **超参数敏感性**：走廊宽度 $\delta$ 和噪声加权函数的选择可能需要针对不同机器人形态进行微调，论文未深入讨论这些超参数的泛化性。

（完）
