---
title: "LiteVLA-H: Dual-Rate Vision-Language-Action Inference for Onboard Aerial Guidance and Semantic Perception"
title_zh: LiteVLA-H：用于机载空中引导与语义感知的双速率视觉-语言-动作推理
authors: "Justn williams, Kishor Datta Gupta, Roy George, Mrinmoy Sarkar"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2605.00884v1"
tags: ["query:vla"]
score: 10.0
evidence: 用于机载空中引导和语义感知的紧凑型VLA系统
tldr: LiteVLA-H是一个针对无人机机载部署设计的轻量级视觉-语言-动作（VLA）模型，拥有2.56亿参数。针对无人机对低延迟引导和语义感知的双重需求，该模型在NVIDIA Jetson AGX Orin平台上实现了双速率运行模式：快速动作分支提供近20Hz的实时引导，慢速语义分支提供场景理解。通过知识保留微调，模型在保持描述能力的同时实现了高效的边缘推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00884v1/fig-001.webp\", \"caption\": \"Figure 1. LiteVLA-H system diagram. One shared multimodal backbone is queried at two timescales: a fast action mode for outer-loop guidance and a slower semantic mode for scene awareness, logging, and operator support. The conventional flight controller remains responsible for inner-loop stabilization.\", \"page\": 3, \"index\": 1, \"width\": 954, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2605.00884v1/fig-002.webp\", \"caption\": \"Table 7. Positioning ofLiteVLA-H relative to representative prior work.\", \"page\": 7, \"index\": 2, \"width\": 1017, \"height\": 202}]"
motivation: 无人机在严格的计算限制下需要极低延迟的闭环引导和语义感知能力，而现有VLA模型难以满足其实时性要求。
method: 设计了双速率调度机制和256M参数的轻量化架构，并采用混合飞行、语义及通用视觉问答数据的知识保留微调策略。
result: 在Jetson AGX Orin上，动作分支推理频率达到19.74Hz，语义分支频率约6Hz，推理速率超越了多个现有SOTA模型。
conclusion: 该研究证明了通过合理的任务调度与轻量化设计，可以在边缘设备上兼顾高效的实时飞行控制与复杂的语义环境感知。
---

## 摘要
视觉-语言-动作（VLA）模型在操作任务中展现了强大的语义落地和任务泛化能力，但由于无人机在严格的机载计算和通信限制下需要低延迟的闭环引导，其在空中部署仍然具有挑战性。我们提出了 LiteVLA-H，这是一个参数量为 256M 的紧凑型 VLA 系统，专为 NVIDIA Jetson AGX Orin 上的双速率运行而设计：一种用于短动作标记输出的快速外环引导模式，以及一种用于场景理解、危险描述和面向操作员叙述的较慢语义模式。核心经验观察是，在这种紧凑的边缘计算环境下，端到端延迟主要由多模态预填充（pre-fill）决定，而非解码少量额外标记的边际成本。这促使我们设计了一个调度器，在同一嵌入式平台上以 50.65 毫秒（19.74 Hz）的速度发布反应式动作标记，同时仍支持 149.90-164.57 毫秒（6.08-6.67 Hz）的句子级语义输出。为了在不破坏模型描述能力的情况下使其专业化，我们采用了一种知识保留微调方案，混合了反应式飞行数据、空中语义数据以及通用的描述/视觉问答（VQA）监督。除了报告当前的延迟测量结果外，我们还将该系统与最新的先进架构（包括 AnywhereVLA、FutureVLA 和 ReMem-VLA）进行了对比，结果表明，在我们的部署条件下，所测量的动作分支在保持周期性语义感知的同时，达到了更高的边缘推理速率。

## Abstract
Vision-language-action (VLA) models have shown strong semantic grounding and task generalization in manipulation, but aerial deployment remains difficult because drones require low-latency closed-loop guidance under strict onboard compute and communication constraints. We present LiteVLA-H, a compact 256M-parameter VLA system designed for dual-rate operation on an NVIDIA Jetson AGX Orin: a fast outer-loop guidance mode for short action-token outputs and a slower semantic mode for scene understanding, hazard description, and operator-facing narration. The central empirical observation is that, in this compact edge regime, end-to-end latency is dominated by multimodal pre-fill rather than by the marginal cost of decoding a few extra tokens. This motivates a scheduler that issues reactive action tokens at 50.65,ms (19.74,Hz) while still supporting sentence-level semantic outputs at 149.90--164.57\ms (6.08--6.67,Hz) on the same embedded platform. To specialize the model without collapsing its descriptive competence, we use a knowledge-preserving fine-tuning recipe that mixes reactive flight data, aerial semantic data, and generic caption/VQA supervision. Beyond reporting current latency measurements, we position the system against recent state-of-the-art architectures, including AnywhereVLA, FutureVLA, and ReMem-VLA, showing that the measured action branch reaches a higher edge inference rate under our deployment conditions while retaining periodic semantic awareness.

---

## 论文详细总结（自动生成）

这是一份关于论文《LiteVLA-H: Dual-Rate Vision-Language-Action Inference for Onboard Aerial Guidance and Semantic Perception》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在资源受限的无人机（UAV）边缘计算平台上，同时实现**高频实时飞行引导**（Reactive Guidance）和**低频深度语义感知**（Semantic Perception）。
*   **背景**：传统的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）虽然泛化能力强，但参数量巨大且推理延迟高，难以满足无人机对外环控制（通常需 >15Hz）的实时性要求。
*   **研究动机**：作者观察到在边缘设备上，VLA 的延迟主要由“多模态预填充（Pre-fill）”阶段决定，而非解码阶段。基于此，论文旨在通过双速率调度机制，在单一模型上平衡控制频率与理解深度。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用一个紧凑的 256M 参数多模态骨干网络，通过**双速率调度器（Dual-Rate Scheduler）**将推理任务分为“快速动作分支”和“慢速语义分支”。
*   **关键技术细节**：
    *   **双速率调度**：动作分支仅输出 1-2 个 Token（约 20Hz），用于速度、航向等外环引导；语义分支输出完整句子（约 6Hz），用于障碍物描述和任务汇报。
    *   **延迟分解模型**：总延迟 $L(n) = P + \sum D_i$，其中 $P$ 是预填充成本，$D_i$ 是逐 Token 解码成本。实验证明 $P \gg D_i$（预填充占总延迟约 94.4%），这为短输出的快速动作分支提供了理论依据。
    *   **知识保留微调（KPFT）**：使用混合损失函数 $\mathcal{L} = \lambda_a \mathcal{L}_{act} + \lambda_s \mathcal{L}_{sem} + \lambda_g \mathcal{L}_{gen} + \lambda_{kp} \mathcal{L}_{kp}$。其中 $\mathcal{L}_{kp}$ 是针对原始模型的蒸馏项，防止模型在学习飞行控制时丧失通用的语言描述能力。
    *   **系统架构**：VLA 负责外环引导，传统的飞行控制器（Autopilot）负责高频的内环姿态稳定，确保物理安全。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：包含 12 万个反应式飞行样本、4.5 万个空中语义样本以及 8.5 万个通用描述/VQA 样本。
    *   **测试场景**：模拟跑道导航（Simulated Runway Navigation）和硬件障碍物测试（Hardware Obstacle Course）。
*   **Benchmark 与对比方法**：
    *   **对比模型**：包括大型模型 OpenVLA-OFT (7B)、边缘模型 AnywhereVLA (450M)、以及具备记忆或预测能力的 FutureVLA 和 ReMem-VLA。
    *   **基准线**：LiteVLA-Edge（作者的前期工作）。

### 4. 资源与算力
*   **推理平台**：**NVIDIA Jetson AGX Orin**（边缘计算模块）。
*   **配置**：FP16 半精度推理，上下文窗口 $n_{ctx} = 2048$。
*   **训练细节**：总训练步数 50,000 步（约 10 个 Epoch），Batch Size 为 256，学习率 3e-4。
*   **功耗**：双速率模式下功耗约为 22.1W。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **消融实验**：针对训练数据混合比例（表 4）和运行精度/调度策略（表 5）进行了详细对比。
    *   **性能测试**：对比了 5 种以上主流 VLA 架构在相同或相似硬件下的延迟、频率和成功率。
*   **充分性评价**：实验设计较为客观，特别是在延迟分解（Profiling）方面做得非常深入。然而，闭环飞行实验（表 6）的部分数据来源于引用或复现，由于不同机体和环境的差异，硬件对比的绝对公平性略受限制。

### 6. 主要结论与发现
*   **预填充主导性**：在边缘 VLA 部署中，优化输出长度对提升频率的作用有限，真正的瓶颈在于图像和 Prompt 的初始处理。
*   **双速率可行性**：LiteVLA-H 在 Orin 上实现了 **19.74 Hz** 的动作频率，足以支持无人机的外环引导，同时维持了 **6.67 Hz** 的语义感知。
*   **性能优势**：在保持相似成功率（81.3%）的情况下，其动作频率远高于 OpenVLA（2.2Hz）和 FutureVLA（4.0Hz）。

### 7. 优点
*   **系统级创新**：不只是改进模型架构，而是从系统调度和延迟分解的角度解决了边缘部署的痛点。
*   **实用性强**：明确了 VLA 在无人机控制链中的位置（外环引导而非直接电机控制），降低了安全风险。
*   **平衡性好**：通过知识保留微调，成功解决了“灾难性遗忘”问题，使模型既能飞又能说。

### 8. 不足与局限
*   **环境覆盖有限**：实验主要在跑道和障碍物场景进行，尚未充分验证在极端天气、光照剧烈变化或高动态环境下的鲁棒性。
*   **语义评估主观性**：语义保留主要通过 CIDEr 和 F1 等代理指标衡量，缺乏更多人类评估或复杂逻辑推理的验证。
*   **控制深度**：目前仅限于外环引导，对于需要极高动态响应的特技飞行或复杂避障，20Hz 的频率可能仍显不足。

（完）
