---
title: "FrameSkip: Learning from Fewer but More Informative Frames in VLA Training"
title_zh: FrameSkip：在 VLA 训练中从更少但更具信息量的帧中学习
authors: "Bin Yu, Shijie Lian, Xiaopeng Lin, Zhaolong Shen, Yuliang Wei, Changti Wu, Hang Yuan, Haishan Liu, Bailing Wang, Cong Huang, Kai Chen"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13757v1"
tags: ["query:vla"]
score: 9.0
evidence: 视觉-语言-动作策略训练中的信息帧选择
tldr: "针对视觉-语言-动作（VLA）模型训练中密集轨迹导致的监督不平衡问题，本文提出FrameSkip框架。该框架通过动作变化、视觉一致性、任务进度及夹具状态等指标对视频帧进行评分，筛选出关键信息帧进行训练。FrameSkip在不改变模型架构的前提下，仅保留20%的帧便在多个基准测试中显著提升了任务成功率，为高效且高性能的机器人策略训练提供了新方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13757v1/fig-001.webp\", \"caption\": \"Figure 3: FRAMESKIP pipeline. FRAMESKIP scores frames in each demonstration trajectory, retains highimportance frames under a target ratio, and injects the compressed trajectory view into VLA training through dataloader index remapping while leaving the model and inference procedure unchanged.\", \"page\": 4, \"index\": 1, \"width\": 761, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2605.13757v1/fig-002.webp\", \"caption\": \"Table 7: Results of evaluating the VLA models with the GR1 robot in the RoboCasa-GR1 Tabletop simulation environment. The results for Isaac-GR00T N1.5 and Isaac-GR00T N1.6 are sourced from the official Isaac-GR00T GitHub repository (NVIDIA et al., 2025b). We highlight the best results in bold and the second-best results with underline.\", \"page\": 12, \"index\": 2, \"width\": 950, \"height\": 1067}]"
motivation: 传统的VLA训练将所有轨迹帧等同对待，导致冗余的低变化片段掩盖了关键的操纵转换动作，造成监督信号的时间不平衡。
method: 开发了一个数据层框架，根据动作变异性、视觉-动作相干性、任务进度先验和夹具转换保留情况对帧进行评分并重采样。
result: "在三大基准测试中，FrameSkip仅使用20%的唯一帧就将宏平均成功率从66.50%提升至76.15%，显著优于全量帧训练。"
conclusion: 通过筛选更具信息量的帧进行训练，可以在大幅减少计算负担的同时，有效解决VLA模型训练中的数据冗余与监督失衡问题。
---

## 摘要
视觉-语言-动作（VLA）策略通常从密集的机器人演示轨迹中训练，这些轨迹通常通过远程操作收集，并采样每个记录的帧，仿佛它们提供了同等有用的监督。我们认为这种惯例造成了时间监督的不平衡：长时间的低变化片段主导了训练流，而诸如对齐、接触、抓取和释放等关键操作转换则出现得非常稀疏。我们引入了 FrameSkip，这是一个数据层帧选择框架，它利用动作变化、视觉-动作一致性、任务进度先验和夹具转换保留来对轨迹帧进行评分，然后在目标保留率下将训练样本重新映射到高重要性帧。由于 FrameSkip 仅在数据加载器（dataloader）中运行，它保持了 VLA 架构、动作头、训练目标和推理过程不变。在 RoboCasa-GR1、SimplerEnv 和 LIBERO 上的实验表明，FrameSkip 在成功率与保留率的权衡上优于全帧训练和更简单的帧选择变体，在三个基准测试中实现了 76.15% 的宏平均成功率，而全帧训练为 66.50%，同时在主要设置中仅保留了 20% 的唯一帧。

## Abstract
Vision-Language-Action (VLA) policies are commonly trained from dense robot demonstration trajectories, often collected through teleoperation, by sampling every recorded frame as if it provided equally useful supervision. We argue that this convention creates a temporal supervision imbalance: long low-change segments dominate the training stream, while manipulation-critical transitions such as alignment, contact, grasping, and release appear only sparsely. We introduce FrameSkip, a data-layer frame selection framework that scores trajectory frames using action variation, visual-action coherence, task-progress priors, and gripper-transition preservation, then remaps training samples toward high-importance frames under a target retention ratio. Because FrameSkip operates only in the dataloader, it leaves the VLA architecture, action head, training objective, and inference procedure unchanged. Across RoboCasa-GR1, SimplerEnv, and LIBERO, FrameSkip improves the success-retention trade-off over full-frame training and simpler frame selection variants, achieving a macro-average success rate of 76.15% across the three benchmarks compared with 66.50% for full-frame training while using a compressed trajectory view that retains 20% of unique frames in the main setting.

---

## 论文详细总结（自动生成）

这是一份关于论文《FrameSkip: Learning from Fewer but More Informative Frames in VLA Training》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的视觉-语言-动作（VLA）模型训练通常对机器人演示轨迹进行均匀采样。然而，机器人轨迹中存在严重的**时间监督不平衡**：大量时间处于低变化的冗余状态（如接近物体、平稳运输），而关键的操纵转换（如对齐、接触、抓取、释放）仅占极小比例。
*   **研究动机**：均匀采样会导致稀疏的关键决策帧被海量的平庸观察所稀释，使得模型在处理复杂交互时表现脆弱。论文旨在探索是否可以通过重新分配监督权重，让模型从更少但更具信息量的帧中学习，从而提升性能。

### 2. 方法论：FrameSkip 框架
*   **核心思想**：在数据加载层（Dataloader）引入一个重要性评分机制，筛选出关键帧并构建压缩的轨迹视图，同时保持模型架构和推理过程完全不变。
*   **关键技术细节**：
    *   **重要性评分（Importance Scoring）**：结合了四个维度的信号：
        1.  **动作变化（AVI）**：计算当前动作与前一帧及未来几帧的差异，捕捉行为突变。
        2.  **视觉-动作一致性（VAC）**：利用 DINOv2 提取特征，衡量视觉变化相对于动作幅度的比例，识别物体接触或运动阶段。
        3.  **任务进度先验（TPI）**：利用高斯混合模型（GMM）拟合关键阶段（如抓取点）在轨迹中的位置分布。
        4.  **夹具转换保留**：强制保留夹具状态改变（开/合）的帧。
    *   **比例感知剪枝（Ratio-Aware Pruning）**：根据设定的保留率 $r$（如 20%），选取评分最高的帧，并确保轨迹的首尾帧及关键动作帧不被丢失。
    *   **训练集成策略**：采用“预热+混合采样”模式。先进行全帧训练（Warmup），随后以 5:1 的比例混合“压缩帧小批量”和“全帧锚点小批量”，以兼顾局部关键细节和全局上下文。

### 3. 实验设计
*   **数据集与场景**：
    *   **RoboCasa-GR1**：双臂人形机器人（GR1）在厨房环境中的 24 项桌面操纵任务。
    *   **SimplerEnv**：WidowX 机械臂在模拟环境中的泛化性测试（基于 BridgeV2 真实数据训练）。
    *   **LIBERO**：Franka 机械臂的标准化语言条件操纵基准。
*   **对比方法**：
    *   **全帧训练（Full-Frame Training）**：基准对照组。
    *   **SOTA VLA 模型**：包括 OpenVLA、$\pi_0$、$\pi_{0.5}$、GR00T 系列、TwinBrainVLA 等。
    *   **消融变体**：随机剪枝（Random）、仅基于动作变化剪枝（AVI）等。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练时长/规模**：
    *   RoboCasa-GR1：10 万步（100K steps）。
    *   SimplerEnv：6 万步（60K steps）。
    *   LIBERO：3 万步（30K steps）。
    *   全局 Batch Size 统一为 128。
*   **软件框架**：基于 StarVLA 框架，使用 DeepSpeed ZeRO-2 进行分布式训练。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个截然不同的机器人平台和基准上进行了验证，涵盖了单臂、双臂、真实转模拟等多种场景。
*   **消融实验**：针对保留率（10% 到 100%）、重要性指标组合（AVI/VAC/TPI）、预热步数等进行了详尽的消融研究。
*   **客观性**：实验采用了相同的模型架构（Qwen3-4B-VL + Diffusion Transformer）和优化设置，确保了对比的公平性。结果显示 FrameSkip 在不同保留率下均表现稳健。

### 6. 主要结论与发现
*   **性能提升**：在仅保留 20% 唯一帧的情况下，FrameSkip 将三大基准的宏平均成功率从 **66.50% 提升至 76.15%**。
*   **冗余有害论**：实验证明，简单地增加训练帧数并不总是有效，过多的冗余帧反而会稀释关键监督信号。
*   **指标协同**：动作、视觉和任务进度先验的结合比单一指标更有效，其中夹具状态的保留对操纵任务至关重要。

### 7. 优点（亮点）
*   **架构无关性**：作为数据层插件，它不需要修改 VLA 模型结构或损失函数，具有极强的通用性。
*   **训练效率与效果双赢**：在减少数据处理负担的同时，显著提升了模型在关键交互阶段的鲁棒性。
*   **即插即用**：通过索引重映射实现，易于集成到现有的训练流水线中。

### 8. 不足与局限
*   **预处理开销**：虽然训练时不需要，但离线计算视觉特征（VAC）和拟合 GMM 先验需要额外的预处理步骤。
*   **先验依赖**：TPI 指标依赖于对任务进度的弱标注或假设，对于完全未知的任务类型，其增益可能受限。
*   **长程依赖风险**：虽然实验中表现良好，但极端的帧剪枝（如低于 10%）可能会破坏模型对长程因果关系的理解。
*   **真实环境验证**：论文主要在仿真环境中评估，尚未展示在复杂多变的真实物理世界中的长效表现。

（完）
