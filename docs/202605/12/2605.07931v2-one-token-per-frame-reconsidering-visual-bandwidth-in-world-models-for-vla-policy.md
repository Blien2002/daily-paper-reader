---
title: "One Token Per Frame: Reconsidering Visual Bandwidth in World Models for VLA Policy"
title_zh: 每帧一个 Token：重新审视 VLA 策略世界模型中的视觉带宽
authors: "Zuojin Tang, Shengchao Yuan, Xiaoxin Bai, Zhiyuan Jing, De Ma, Gang Pan, Bin Liu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07931v2"
tags: ["query:vla"]
score: 10.0
evidence: 重新考虑VLA策略世界模型中的视觉带宽
tldr: 本研究针对视觉-语言-动作（VLA）模型在长程规划中视觉带宽过高的问题，提出了OneWM-VLA。该模型通过自适应注意力池化将每帧图像压缩为单个语义标记，并采用统一的流匹配目标同时预测潜在状态流和动作轨迹。实验表明，在极低视觉带宽下，该方法在MetaWorld、LIBERO及真实机器人任务中均显著提升了成功率，证明了高效视觉表征在世界模型增强型VLA中的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.07931v2/fig-001.webp\", \"caption\": \"Figure 4: Evaluation suites used in this work: the LIBERO and MetaWorld MT50 simulation benchmarks, and a real Piper robot arm.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2605.07931v2/fig-002.webp\", \"caption\": \"Table 1: Main results on MetaWorld MT50. Success rates (%) across difficulty tiers and planning horizons H . ∆π0 and ∆π0.5 denote the absolute gain over π0 and π0.5.\", \"page\": 6, \"index\": 2, \"width\": 660, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2605.07931v2/fig-003.webp\", \"caption\": \"Table 13: Inference-time horizon sweep on LIBERO-Long, using the same model weights (trained at AH=20). Tuning only the inference horizon and the replan step moves the success rate from 93.4% to 95.6%.\", \"page\": 15, \"index\": 3, \"width\": 760, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2605.07931v2/fig-004.webp\", \"caption\": \"Table 9: Task difficulty partition for MetaWorld MT50, following [36].\", \"page\": 14, \"index\": 4, \"width\": 802, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2605.07931v2/fig-005.webp\", \"caption\": \"Figure 2: The OneWM-VLA Framework. Through Adaptive Attention Pooling (Adaptive Fusion), we distill multi-view visual features into a dense latent world token for each perspective. These tokens are aligned with action trajectories via Joint Flow Matching to ensure alignment between environment dynamics transitions and robot control.\", \"page\": 3, \"index\": 5, \"width\": 820, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2605.07931v2/fig-006.webp\", \"caption\": \"Figure 5: PCA visualization of visual features on LIBERO-Long. Top: before pooling (256 tokens, mean-aggregated to a single vector per sample). Bottom: after Adaptive Attention Pooling (1 token per view per frame). The class-level structure remains visually separable after pooling.\", \"page\": 16, \"index\": 6, \"width\": 829, \"height\": 356}]"
motivation: 现有的VLA世界模型通常采用高带宽视觉流且动作预测与状态预测耦合不足，导致在有限计算预算下效率低下。
method: 提出OneWM-VLA，利用自适应注意力池化将每帧压缩为单标记，并通过统一的流匹配目标联合生成潜在流与动作轨迹。
result: "在MetaWorld MT50和LIBERO-Long任务中成功率分别提升至61.3%和95.6%，并在真实机械臂折衣服任务中实现三倍性能提升。"
conclusion: 视觉带宽可大幅压缩至每帧单标记而不损害长程任务性能，且统一的生成目标能更有效地整合世界模型与策略。
---

## 摘要
视觉-语言-动作（VLA）模型日益依赖辅助世界模块进行长时程规划，然而如何在预训练 VLA 之上对这些模块进行参数化仍是一个开放的设计问题。现有的增强了世界模型的 VLA 通常以高视觉带宽将逐帧视觉流输入世界模块，并将其展开（rollout）视为动作预测的副产品；在冻结骨干网络且适应预算受限的情况下，这使得逐帧表示和潜在动作耦合都未得到充分研究。我们推出了 OneWM-VLA，它通过自适应注意力池化（Adaptive Attention Pooling）将每个视角压缩为每帧单个语义 Token，并在单一流匹配（flow-matching）目标下生成结果潜在流和动作轨迹，而不是通过单独的解码器将它们连接起来。实验表明，在我们的设置下，逐帧视觉带宽可以减少到单个 Token，而不会损害长时程性能。OneWM-VLA 在 π₀ (2B) 骨干网络上使用 14.71M LoRA 参数进行训练，在 MetaWorld MT50 上的平均成功率从 47.9% 提高到 61.3%，在 LIBERO-Long 上达到 95.6%（π₀ 为 85.2%），并在真实 Piper 机械臂的长时程可变形任务“折叠布料”上达到 60.0%（π₀ 为 20.0%）。

## Abstract
Vision-language-action (VLA) models increasingly rely on auxiliary world modules to plan over long horizons, yet how such modules should be parameterized on top of a pretrained VLA remains an open design question. Existing world-model-augmented VLAs typically pass the per-frame visual stream into the world module at high visual bandwidth and treat its rollout as a side product of action prediction; under a constrained adaptation budget on a frozen backbone, this leaves both the per-frame representation and the latent action coupling under-examined. We introduce OneWM-VLA, which compresses each view into a single semantic token per frame through an Adaptive Attention Pooling, and produces the resulting latent stream and the action trajectory under a single flow-matching objective rather than connecting them through a separate decoder. Empirically, we find that per-frame visual bandwidth can be reduced to a single token without compromising long-horizon performance under our setup. Trained with 14.71M LoRA parameters on a $π_0$ (2B) backbone, OneWM-VLA improves the average success rate from 47.9% to 61.3% on MetaWorld~MT50, reaches 95.6% on LIBERO-Long (vs.85.2% for $π_0$), and reaches 60.0% on the long-horizon deformable task Fold Cloth on a real Piper arm (vs.20.0% for $π_0$).

---

## 论文详细总结（自动生成）

这是一份关于论文《One Token Per Frame: Reconsidering Visual Bandwidth in World Models for VLA Policy》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了在视觉-语言-动作（VLA）模型中集成“世界模型”（World Model）时的**视觉带宽效率**问题。
*   **研究动机**：现有的世界模型增强型 VLA 通常需要高带宽的视觉输入（每帧多个 Token），且往往在像素空间进行预测。这导致在长时程（Long-horizon）规划时计算开销巨大，且模型容易被像素级的细节（如光影变化）干扰，而非关注控制相关的动力学。
*   **核心问题**：在预训练 VLA 的微调阶段，世界模型究竟需要多少视觉带宽才能支持长时程控制？动作预测与环境动力学预测应如何耦合？

### 2. 方法论：OneWM-VLA
论文提出了 **OneWM-VLA** 框架，其核心思想是“极度压缩”与“联合生成”：
*   **自适应注意力池化（Adaptive Attention Pooling）**：
    *   **多策略池化**：对每个视角的视觉特征（通常为 256 个 Token），分别使用最大值（MAX）、求和（SUM）和可学习（LEARN）三种策略进行评分，通过 Softmax 归一化后聚合为 3 个候选 Token。
    *   **自适应视图融合**：通过可学习的权重将上述 3 个 Token 融合为**每帧仅 1 个语义 Token**。这使得世界模块的计算预算与规划时程解耦，不再随视觉分辨率增加。
*   **联合流匹配（Joint Flow Matching）**：
    *   模型不再将世界模型作为辅助头，而是将**未来潜在状态流（Latent Stream）**与**未来动作轨迹（Action Trajectory）**放在同一个 Transformer 序列中。
    *   使用统一的流匹配目标函数进行训练，使潜在状态和动作在推理时的去噪过程中通过自注意力机制相互耦合，互为结构化先验。

### 3. 实验设计
*   **数据集与场景**：
    *   **MetaWorld MT50**：包含 50 个操纵任务，分为 Easy 到 Very Hard 四个难度等级，用于测试长时程规划（H=5 到 30）。
    *   **LIBERO**：包括 Spatial、Object、Goal 和 Long 四个子集，重点关注 LIBERO-Long。
    *   **真实世界实验**：使用 6 自由度 Piper 机械臂，执行“捡香蕉”、“折叠布料”（长时程可变形物体）和“拉抽屉”任务。
*   **Benchmark 与对比方法**：
    *   **基准模型**：以 $\pi_0$ (2B) 和 $\pi_{0.5}$ (3B) 为主要对比对象。
    *   **其他方法**：对比了 Diffusion Policy, ACT, OpenVLA, CoT-VLA, WorldVLA, DreamVLA 等多种 SOTA 模型。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A800 GPU**。
*   **训练配置**：在 $\pi_0$ 骨干网络上使用 **LoRA** 进行微调，新增参数量仅为 **14.71M**。
*   **时长**：训练迭代次数为 **30K steps**。
*   **推理效率**：在 H=30 的长时程任务下，OneWM-VLA 达到 4.81 FPS，而全 Token 模式会因显存溢出（OOM）无法运行。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的仿真测试（50+ 任务）和实机验证，实验设计较为全面。
*   **消融实验**：针对“Token 数量（1 到 12 个）”、“池化策略（静态 vs 自适应）”、“预测空间（语义 vs 像素）”以及“联合损失函数”进行了详尽的对比。
*   **客观性**：通过在不同规划时程（H）下观察性能衰减情况，客观展示了该方法在长时程任务上的稳定性优势。

### 6. 主要结论与发现
*   **带宽压缩可行性**：在特定设置下，每帧视觉带宽可以压缩至**单个 Token** 而不损害性能，甚至比多 Token 表现更好（可能起到了正则化作用）。
*   **长时程优势**：随着规划时程 H 的增加，基准模型性能显著下降，而 OneWM-VLA 保持了极高的成功率。
*   **语义空间优于像素空间**：在语义潜在空间进行预测比在像素空间压缩更有效，因为前者过滤了与控制无关的噪声。
*   **联合生成至关重要**：将潜在状态预测作为动作生成的结构化先验（而非辅助任务），是提升性能的关键。

### 7. 优点与亮点
*   **极高的计算效率**：通过将视觉带宽降至最低，解决了长时程规划中的显存瓶颈问题。
*   **即插即用**：作为 LoRA 模块附加在预训练 VLA 上，参数量极小（14M），易于部署。
*   **实机表现强劲**：在极具挑战性的可变形物体操纵（折衣服）任务中，成功率从 20% 提升至 60%。

### 8. 不足与局限
*   **骨干网络单一**：实验主要基于 $\pi_0$ 框架，尚未验证该结论在其他架构（如基于 Transformer 解码器的纯自回归模型）上的普适性。
*   **感知复杂度限制**：目前的任务场景感知复杂度尚属中等，在极度复杂的视觉环境（如充满杂物的动态场景）中，单 Token 是否足以捕捉所有关键特征仍存疑。
*   **训练时长依赖**：论文提到更大 Token 数量可能需要更长的训练时间才能发挥优势，目前的对比基于固定的 30K 步训练，可能对高带宽方案不够公平。

（完）
