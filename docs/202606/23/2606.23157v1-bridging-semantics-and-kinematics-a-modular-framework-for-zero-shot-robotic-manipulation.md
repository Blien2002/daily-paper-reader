---
title: "Bridging Semantics and Kinematics: A Modular Framework for Zero-Shot Robotic Manipulation"
title_zh: 弥合语义与运动学之间的鸿沟：用于零样本机器人操作的模块化框架
authors: "Ali Alabbas, Dipshikha Das, Camillo Murgia, Sainul Ansary, Alaa Elkamash, Philip Long"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23157v1"
tags: ["query:vla"]
score: 9.0
evidence: 使用VLM和LLM实现零样本语言引导操作
tldr: "针对零样本语言引导的机器人操作中高层语义与底层运动之间的鸿沟，提出一种模块化免训练框架。视觉感知模块采用FastSAM和Set-of-Mark提示动态生成接地视觉锚点，语义路由模块将开放指令转化为可验证配置，任务编排模块利用MoveIt Task Constructor生成无碰撞轨迹。在两个零样本场景（开放世界顺序操作和密集关系空间推理）中，端到端任务成功率达62%。该方法无需领域特定训练或手动坐标编程，有效连接语义理解与运动控制。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23157v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 881, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23157v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 891, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23157v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 864, \"height\": 506, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23157v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 907, \"height\": 495, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23157v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 903, \"height\": 131, \"label\": \"Table\"}]"
motivation: 现有视觉语言模型存在空间模糊性和语义幻觉，且缺乏与底层运动规划的桥接，导致零样本机器人操作困难。
method: 提出三阶段框架：视觉感知（FastSAM+SoM生成锚点）、语义路由（LLM解析指令为配置）、任务编排（MTC生成无碰撞轨迹）。
result: "两个零样本实验（开放世界操作和关系推理）中，端到端任务成功率达62%，无需领域训练或手动编程。"
conclusion: 模块化免训练框架有效融合高层语义与底层运动，为零样本语言引导操作提供了可扩展、可泛化的解决方案。
---

## 摘要
本文提出了一种模块化免训练框架，用于在半结构化环境中进行零样本、语言引导的机器人操作。该架构通过将视觉-动作流水线分解为三个阶段：视觉感知、语义解释和任务执行，弥合了高层推理与低层运动学之间的鸿沟。为克服标准视觉-语言模型（VLM）固有的空间模糊性和语义幻觉，感知模块采用FastSAM和Set-of-Mark（SoM）提示来动态生成基于语义的字母数字视觉锚点。随后，相同的基模型纯粹作为大型语言模型（LLM）充当语义路由器，将无约束的人类指令转换为可验证、可重配置的配置。最后，这些配置由任务编排器动态解析为MoveIt Task Constructor（MTC），以生成无碰撞轨迹。该框架在两个零样本实验设置中进行了评估：无约束的开放世界顺序操作和密集关系空间推理，在两个场景中实现了62%的端到端任务成功率，展示了其无需领域特定训练或手动坐标编程即可可靠执行复杂物理动作的能力。

## Abstract
This paper presents a modular training-free framework for zero-shot, language-guided robotic manipulation in semi-structured environments. The architecture bridges the gap between high-level reasoning and low-level kinematics by decomposing the vision-action pipeline into three stages: visual perception, semantic interpretation, and task execution. To overcome the spatial ambiguity and semantic hallucinations inherent in standard Vision-Language Models (VLMs), the perception module employs FastSAM and Set-of-Mark (SoM) prompting to dynamically generate grounded, alphanumeric visual anchors. The same foundation model then operates purely as a Large Language Model (LLM) to act as a semantic router, translating unconstrained human directives into verifiable, reconfigurable configurations. Finally, these configurations are dynamically parsed by a Task Orchestrator into MoveIt Task Constructor (MTC) to generate collision-free trajectories. The framework is evaluated across two zero-shot experimental setups: unconstrained open-world sequential manipulation and dense relational spatial reasoning, achieving a 62% end-to-end task success rate across both scenarios, demonstrating its capacity to reliably execute complex physical actions without domain-specific training or manual coordinate programming.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：现有视觉-语言模型（VLM）存在空间模糊性和语义幻觉，且难以将高层语义理解直接转化为精确的机器人动作指令。同时，端到端的视觉-语言-动作（VLA）模型虽然强大，但参数巨大、需领域微调、计算成本高且缺乏可解释性，给实际部署带来瓶颈。
- **整体含义**：本文旨在**弥合语义理解与底层运动学之间的鸿沟**，通过模块化、免训练的设计，使机器人能够基于自然语言指令在零样本条件下自主完成复杂操作任务，无需人工坐标编程或领域特定训练。

## 2. 方法论：核心思想、关键技术细节与流程
- **核心思想**：将视觉-动作流水线分解为三个模块，各自负责特定任务，通过结构化中间表示（YAML配置）连接语义与运动。
- **关键技术细节**：
  - **感知模块**：使用**FastSAM**进行实例分割，并通过**Set-of-Mark（SoM）提示**为每个分割区域动态生成独一无二的字母数字视觉锚点（如“screw_driver_1”）。这些锚点映射到ROS2的变换树（tf），建立共享空间词汇。
  - **语义解释模块**：利用本地部署的**Molmo2（8B参数）** 作为LLM/VLM，输入用户自然语言指令 + 带注释的视觉图像，输出符合预定义YAML模式的**结构化任务序列**（例如“pick screw_driver and place to dustpan”）。约束其只填充模板中的参数，避免直接生成代码。
  - **任务规划与执行模块**：**任务编排器**解析YAML配置，将抽象动作绑定到MoveIt Task Constructor（MTC）的**可组合阶段库**（如approach_pick、descend_place等），自动生成无碰撞轨迹并执行。
- **流程**：用户口头命令 → 感知模块分割并标注 → 语义模块生成YAML任务描述 → 任务编排器编译为MTC流水线 → UR5e机械臂执行。

## 3. 实验设计：场景、基准与对比方法
- **实验场景**：
  1. **开放世界顺序操作**（30次试验）：桌面杂乱放置多种日常物品（盒子、螺丝刀、任务板等），用户通过显示在手机屏幕上的多步命令（如“首先按红色按钮，然后拿起触控笔放到白板上……”）引导操作。
  2. **密集关系空间推理**（30次试验）：包含3×3、5×5和圆形钉板，视觉上相同的孔洞，用户提供抽象空间关系指令（如“填充最上面一排孔”“填充已填孔相邻的孔”）。
- **基准（Benchmark）**：无标准外部数据集，采用自建场景。对比方法为**消融实验中的基线**：直接向Molmo2输入未标注的原始图像和相同指令，不使用FastSAM+SoM标注。
- **对比方法**：论文在Table I中定性比较了与RT-2、OpenVLA、Gemini Robotics等VLA模型的特性（泛化性、可解释性、零样本、模块化、参数绑定），但**没有进行定量对比实验**。消融实验仅在10+10 trials上对比基线。

## 4. 资源与算力
- **硬件**：
  - UR5e机械臂、Robotiq Hand-E夹爪、英特尔RealSense D435深度相机（腕装）。
  - 本地工作站：**单张NVIDIA RTX A4500 GPU（20GB VRAM）**、64 GB RAM。
- **软件**：ROS2框架、MoveIt Task Constructor（MTC）、Molmo2（8B open-weight VLM/LLM）。
- **未明确**：训练时长（因免训练，仅推理）。无分布式训练或多GPU信息。

## 5. 实验数量与充分性
- **实验数量**：
  - 开放世界：30次试验（end-to-end）。
  - 密集空间：30次试验（end-to-end）。
  - 消融实验：基线10次 + 本文方案10次（各5次开放世界+5次密集空间）。
- **充分性评价**：
  - **优点**：覆盖了两类典型零样本任务（顺序操作、空间推理），消融实验直接证明了SoM标注的关键作用（密集空间从0%提升至100%）。
  - **不足**：
    - 消融样本量仅20次，统计意义有限。
    - 未与其他VLA模型（如RT-2、OpenVLA）在同一硬件/场景下定量对比，公平性存疑。
    - 仅使用一种VLM（Molmo2），未验证对其他VLM（如GPT-4V）的通用性。
    - 实验场景均为半结构化桌面，缺乏移动操作或动态障碍物场景。

## 6. 主要结论与发现
- **整体成功率**：端到端任务成功率为 **62%**（37/60），其中开放世界70%，密集空间53.3%。
- **各阶段成功率**：
  - **感知**：开放世界76.7%，密集空间53.3%。失败主因：FastSAM部分-整体分割错误（如将锤柄识别为铅笔）、VLM对大小比较失效、未推理功能状态（如检测到尘铲已满）。
  - **语义翻译**：在感知通过后，翻译成功率为91.3%（开放世界）和100%（密集空间）。失败源于语言先验导致幻觉（如将“按压”错误关联到刷子）。
  - **执行**：一旦生成有效YAML，执行成功率为**100%**，证明MTC流水线高度可靠。
- **消融结论**：无SoM标注时，开放世界成功率降至60%，密集空间降至0%；而SoM方案使密集空间达100%执行成功，证明视觉锚点对密集相同物体的空间推理至关重要。

## 7. 优点
- **模块化与免训练**：各模块可独立替换，无需任何领域微调，计算开销低（本地单GPU运行8B模型）。
- **可解释性与安全性**：中间YAML配置人类可读，便于调试；执行失败时自动终止（无法找到运动学解），避免危险动作。
- **零样本泛化**：对未见物体、新命令、多种空间关系指令（“相邻”“对角线”“填满”）均能直接应对，无需预设坐标。
- **强鲁棒性**：执行层100%成功率，表明一旦语义正确，运动规划可靠性极高。

## 8. 不足与局限
- **感知局限性**：FastSAM在几何相似物体上易出错；2D分割无法处理密集遮挡或6-DoF操作（如插入、旋转）。
- **语义局限**：VLM存在**强语言先验**，可能覆写视觉信息导致幻觉帧；无法推理物理状态（如物体是否已占用）。
- **缺乏闭环反馈**：系统为开环控制，无法监测执行过程，一旦动作失败无法自动重规划。
- **实验覆盖有限**：仅评估两个场景（共60 trials），缺少与其他VLA的定量对比、缺少动态环境、缺少长时程任务。
- **部署限制**：依赖ROS2和MTC生态，迁移至其他机器人平台需适配；VLM推理仍需要较高GPU显存（20GB），限制了更轻量级硬件。

（完）
