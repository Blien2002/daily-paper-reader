---
title: "TacStyle: Personalizing Tactile Robot Policies using Structured Behavior Representations"
title_zh: TacStyle：使用结构化行为表示个性化触觉机器人策略
authors: "Kevin Robledo, Matías I. Torres Galaz, Kumar Dixhant Rai, Shelly Sara Ulman, Tasmia Tasrin, Heramb Nemlekar"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14862v1"
tags: ["query:tf"]
score: 9.0
evidence: 使用结构化行为表示个性化触觉机器人策略，整合力与触觉模态
tldr: 现有语言条件策略难以精确控制机器人行为力度等连续偏好。TacStyle先学习结构化潜在表示组织用户偏好，再使用基础模型根据偏好提示在潜在空间中选取对应值，生成期望行为。实验表明该方法相比直接语言条件策略能更精确适应偏好，且所需偏好标签显著减少。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14862v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1427, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2606.14862v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1427, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2606.14862v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1423, \"height\": 900}, {\"url\": \"assets/figures/arxiv/2606.14862v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 998, \"height\": 687}, {\"url\": \"assets/figures/arxiv/2606.14862v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1446, \"height\": 421}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14862v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1510, \"height\": 743}, {\"url\": \"assets/tables/arxiv/2606.14862v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1505, \"height\": 271}]"
motivation: 语言指令难以精确表达机器人应施加的力度等连续行为偏好，需要大量标注且控制粗糙。
method: 学习结构化潜在表示组织偏好差异，用基础模型解析该空间并根据提示选取值生成行为。
result: 模拟和实机实验显示，相比语言条件策略，更精确适应偏好，所需标签更少。
conclusion: 从结构化潜在空间选择行为能实现精细偏好适应，降低标注成本。
---

## 摘要
辅助人类的机器人系统应能够根据个体用户偏好调整其行为。例如，用户可能希望机器人手臂在折叠衣物或清洁家具时调整施加的力。自然语言为人类传达此类偏好提供了一种直观方式。最近在语言条件机器人策略方面的进展表明，机器人能够成功使用语言提示来确定要执行的任务。然而，将相同方法扩展到实现任务应如何执行，需要描述任务数据中轨迹偏好或风格的详细标签。收集此类注释不仅具有挑战性，而且直接基于这些标签进行条件控制可能无法提供对连续行为范围的精细控制。例如，通过“比之前稍微用力一点”这样的抽象指令来传达机器人必须施加的确切力度可能很困难。因此，在这项工作中，我们提出使用语言来推理偏好行为，而不是直接生成它们。我们首先学习一个结构化的潜在表示，根据相应轨迹的差异来组织用户偏好。然后，给定一个偏好提示，我们使用基础模型来解释这个潜在空间，并选择一个能产生所需行为的值。通过仿真和真实世界实验，我们表明，从直观结构化的潜在空间中选择机器人行为，能够更精确地适应用户偏好，同时所需的偏好标签比语言条件策略少得多。

## Abstract
Robotic systems that assist humans should be capable of adapting their behaviors to individual user preferences. For instance, users may want a robot arm to adjust the amount of force it applies while folding their laundry or cleaning furniture. Natural language provides an intuitive way for humans to communicate such preferences. Recent progress in language-conditioned robot policies has shown that robots can successfully use language prompts to determine what task to perform. However, extending the same approach to realize how the task should be performed requires detailed labels describing the preferences or styles of trajectories in the task data. Not only is collecting such annotations challenging, but conditioning directly on these labels may also fail to provide fine-grained control over a continuous range of behaviors. For example, it can be difficult to convey the exact force that a robot must apply through abstract instructions like "apply a bit more pressure than before". Therefore, in this work, we propose using language to reason over preferred behaviors instead of directly generating them. We first learn a structured latent representation that organizes user preferences according to differences in the corresponding trajectories. Then, given a preference prompt, we use a foundation model to interpret this latent space and choose a value that produces the desired behavior. Through both simulation and real-world experiments, we show that selecting robot behaviors from an intuitively structured latent space enables more precise adaptation to user preferences while requiring significantly fewer preference labels than language-conditioned policies.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：机器人辅助人类时需根据个体偏好调整行为（如擦拭力度、折叠松紧），但通过自然语言直接控制机器人策略存在两个瓶颈：(1) 需要大量偏好标签来覆盖连续的行为风格；(2) 语言条件策略难以精细控制连续偏好（如“比之前稍微用力一点”），因为语言标签通常只提供粗粒度的描述。
- **整体含义**：作者提出一种替代思路——不直接让语言生成行为，而是让语言**推理**行为。先学习一个结构化的低维潜在空间，使不同行为风格（如轻柔/强力）在空间中的距离正比于实际行为差异；然后利用大语言模型（LLM）解析该空间，根据用户语言提示选择合适的潜在值，再由机器人策略据此生成行为。该方法旨在实现更精确、更少标签依赖的个性化适应。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：构建一个**线性比例化**的潜在风格空间，使LLM能自然地在空间内插值与推理，无需大量语言标注。
- **关键技术细节**：
  - **两阶段框架**：
    - **离线阶段**：同时训练一个**风格编码器** \(f_\phi\) 和一个**策略网络** \(\pi_\theta\)。编码器将完整轨迹 \(\xi\) 映射为低维潜在风格 \(z \in \mathbb{R}^n\)（实验中 \(n=1\)）；策略网络以当前状态 \(s_t\) 和潜在风格 \(z\) 为输入，预测动作 \(a_t\)。训练损失为行为克隆损失 \(\mathcal{L}_{bc}\) 和比例性正则化损失 \(\mathcal{L}_{prop}\)。
    - **比例性正则化损失**：\(\mathcal{L}_{prop}(\phi, \beta) = \mathbb{E}_{(\xi_i, \xi_j)} \left[ (|z_i - z_j| - \beta d(\xi_i, \xi_j))^2 \right]\)，其中 \(d(\xi_i, \xi_j)\) 是轨迹间的行为差异（如时间对齐状态的平均欧氏距离）。该损失强制潜在空间距离与行为差异成正比。
    - **在线阶段**：用户输入自然语言提示 \(L\)（如“用力擦”），LLM（GPT-4o mini）根据已知的极端风格标签（\(P_{\min}, P_{\max}\)）和对应潜在值（\(z_{\min}, z_{\max}\)），结合当前潜在值 \(z_{\text{pre}}\)（若有相对指令），输出新的潜在值 \(z_{\text{new}}\)。策略网络使用该值生成动作。
- **公式**（文字说明）：
  - 联合优化：\(\mathcal{L}(\theta, \phi, \beta) = \mathcal{L}_{bc} + \lambda \mathcal{L}_{prop}\)。\(\mathcal{L}_{bc}\) 引导策略模仿动作；\(\mathcal{L}_{prop}\) 使潜在空间结构化。
  - LLM映射：\((z_{\min}, z_{\max}, P_{\min}, P_{\max}, z_{\text{pre}}) \mapsto z_{\text{new}}\)，通过精心设计的系统提示（如图4）实现线性插值推理。

### 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：
  - **模拟擦拭任务**（PyBullet）：6-DOF机械臂抓取可变形布沿直线擦拭桌面，偏好变量为**擦拭高度**（影响压力），共5种高度，采集30条演示。状态包括6-D关节角、RGB图像、1-D夹爪状态、3-D触觉读数。
  - **真实世界折叠任务**（Fairino FR5机械臂）：与人类协作折叠T恤，偏好为**折叠松紧度**（由抓取位置决定），共3种紧度，采集15条演示。状态包括6-D关节角、2-D夹爪状态、15-D触觉读数（eFlesh传感器）。
- **基准方法**：
  - **Vision-Tactile-Language-Action (VTLA)** 基线（模拟）和 **Tactile-Language-Action (TLA)** 基线（真实）：使用冻结的Qwen2语言编码器将偏好标签映射为语义嵌入，与视觉/触觉/状态融合后输入变压器策略。
  - **Baseline (5)**、**Baseline (3)**、**Baseline (2)**：分别用全部5种、3种、2种风格的偏好标签训练。
  - **Ours-P**：本方法的消融变体，不使用比例性正则化损失（\(\mathcal{L}_{prop}\)）。
  - **Ours**：完整方法（1维潜在空间，GPT-4o mini推理）。
- **评估指标**：**Style Error**——目标风格与执行轨迹实际风格的绝对差异（末端执行器位置距离）。

### 4. 资源与算力

论文**未明确说明**所使用的GPU型号、数量、训练时长等具体算力信息。仅提及所有网络使用Adam优化器，学习率1e-4，训练至类似损失。实验中使用了GPT-4o mini作为LLM推理模型，但未报告API调用成本或响应时间。因此无法提供量化的算力总结。

### 5. 实验数量与充分性

- **实验数量与设置**：
  - **模拟擦拭任务**：10次独立训练与测试运行（不同随机种子）。
  - **真实折叠任务**：5次独立训练与测试运行。
  - **相对偏好实验**（附录A.3）：在折叠任务上进行了迭代调整实验（5次“更松”循环、3次“更紧”循环），重复5次。
  - **消融实验**：对比Ours与Ours-P，验证比例性正则化的效果。
  - **基线标签数量消融**：Baseline (2)/ (3)/ (5) 评估标签依赖性。
- **充分性分析**：
  - **优点**：任务覆盖了模拟和真实场景、不同接触类型（擦拭与折叠）；对比了多种标签减少程度和消融；相对偏好实验证明了渐进适应能力。
  - **不足**：真实实验仅有一种任务类型，且用户提示由LLM（或acle）生成，而非真实人类；缺乏用户研究评估主观满意度。实验次数有限（模拟10次、真实5次），未报告置信区间或统计显著性检验。总体而言，实验设计合理，但覆盖面和统计稳健性仍有提升空间。

### 6. 论文的主要结论与发现

- **主要结论**：从结构化潜在空间中选择行为（Ours）比直接语言条件策略（VTLA/TLA）能更精确地实现用户偏好的连续调整，且**仅需两端的极端风格标签**，大幅减少标注成本。
- **具体发现**：
  - 在模拟擦拭任务中，Ours与使用全部5种标签的Baseline (5) 性能相当，但标签量少近60%；在真实折叠任务中，Ours显著优于所有基线。
  - 比例性正则化（Ours vs. Ours-P）在复杂任务（折叠）中带来实质性提升，在简单任务（擦拭）中效果相近。
  - 基线在减少标签时性能急剧下降，而Ours对标签数量不敏感。
  - 在相对偏好（“更紧/更松”）场景下，Ours能持续渐进调整，而基线因语言嵌入缺乏方向性而快速收敛到固定行为。

### 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - **结构化潜在空间**：通过比例性正则化使潜在距离与行为差异线性相关，使LLM能自然进行插值和推理，无需复杂解释。
  - **标签高效**：仅需两个极端风格标签，大幅降低人工标注成本，实用性增强。
  - **结合LLM推理**：利用基础模型的语言理解能力，同时规避了直接语言条件策略的粗粒度问题。
  - **支持相对与绝对指令**：可处理“更用力”等渐进式调整，且能记忆之前风格（\(z_{\text{pre}}\)）。
- **实验设计亮点**：
  - 同时设置模拟和真实场景，涵盖不同传感器模态（触觉、视觉）。
  - 设计了多种标签数量的基线，清晰展示标签减少带来的性能退化。
  - 消融实验（Ours vs. Ours-P）证实了核心组件的有效性。
  - 附加实验验证了相对指令下的渐进适应能力，并深入分析了语言嵌入的PCA分布。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **维度局限性**：当前仅探索一维潜在空间（单轴偏好），无法处理多维度偏好权衡（如“用力但缓慢”）。扩展至高维需要解耦表示，且LLM的推理可靠性需进一步验证。
- **用户评估缺失**：语言提示完全由LLM（oracle）生成，缺乏真实人类用户的交互研究。真实用户可能表达模糊或带有歧义，且主观感知可能与客观指标不符。
- **规模限制**：实验仅针对两个特定接触操作任务，未在大型多任务数据集（如Open X-Embodiment）上验证，可扩展性未知。
- **标签来源假设**：假设训练数据中已有极端风格标签（\(P_{\min}, P_{\max}\)），但在实际部署中仍需初始标注或演示来建立这些端点。
- **LLM依赖**：推理阶段依赖外部LLM（GPT-4o mini），存在API成本、延迟和模型更新带来的不确定性。
- **未报告统计显著性**：实验结果未提供t检验或置信区间，难以判断差异的统计可靠性。

（完）
