---
title: "Decoupling Semantics and Geometric Grounding: Spatial Visual Prompts for Language-Conditioned Imitation Learning"
title_zh: 解耦语义与几何基础：语言条件模仿学习的空间视觉提示
authors: "Yanzhe Tang, Xinyu Shao, Yuxuan Hu, Siyu Chen, Bowen Yang, Yajun Gao, Tongtong Cao, Xiu Li, Long Zeng"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25360v1"
tags: ["query:vla"]
score: 8.0
evidence: 解耦的VLA架构与空间视觉提示
tldr: "端到端视觉-语言-动作模型将语义推理与空间控制耦合，导致数据受限时目标区分困难。本文提出解耦架构SVP-IL，利用视觉语言基础模型将指令转化为零样本几何掩膜作为空间视觉提示，通过轻量特征融合注入动作生成器。在仅50-100个演示训练下，高歧义任务成功率从24.0%提升至39.5%，标准基准达67.8%。该方法显著提升了数据效率与鲁棒性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25360v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 513, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 813, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25360v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 899, \"height\": 872, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25360v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 908, \"height\": 822, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 808, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 907, \"height\": 868, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25360v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 907, \"height\": 294, \"label\": \"Table\"}]"
motivation: 解决端到端VLA模型中语义与空间控制的耦合瓶颈，提升数据受限下的精确目标区分能力。
method: 提出SVP-IL，利用视觉语言模型生成零样本几何掩膜作为空间视觉提示，通过轻量特征融合注入连续动作生成器。
result: "在50-100个演示下，高歧义任务成功率从24.0%升至39.5%，标准基准达67.8%。"
conclusion: SVP-IL通过解耦语义与空间控制，实现了数据高效且鲁棒的模仿学习，优于现有方法。
---

## 摘要
尽管端到端的视觉-语言-动作（VLA）模型在机器人操作中展现出前景，但其单一体结构内在地将语义推理与空间控制耦合在一起。这造成了严重的对齐瓶颈，限制了数据受限的模仿学习中的精确目标分辨能力。为了克服这一问题，我们提出了SVP-IL，一种解耦架构，从动作生成循环中显式提取空间视觉基础。通过利用视觉语言基础模型，我们将指令解析为零样本几何掩码，将语言转化为显式的空间视觉提示（SVP）。这些先验通过轻量级的直接特征级融合机制注入到连续动作生成器中。这种集成提供了显式且无污染的空间梯度引导，同时确保了在低数据量下的高度稳定优化。大量实验表明，SVP-IL显著优于最先进的VLA模型和纯视觉运动基线。在仅使用50到100次演示进行训练的情况下，SVP-IL将高度模糊的语言条件任务的平均成功率从24.0%提升到39.5%，在标准基准上达到67.8%。真实世界的机器人实验进一步验证了其在非结构化物理环境中的鲁棒性和数据效率。

## Abstract
While end-to-end Vision-Language-Action (VLA) models show promise in robotic manipulation, their monolithic paradigm inherently couples semantic reasoning and spatial control. This creates a severe alignment bottleneck, limiting precise target disambiguation in data-constrained imitation learning. To overcome this, we propose SVP-IL, a decoupled architecture that explicitly extracts spatial visual grounding from the action generation loop. By leveraging vision-language foundation models, we parse instructions into zero-shot geometric masks, translating language into explicit Spatial Visual Prompts (SVP). These priors are injected into a continuous action generator via a lightweight direct feature-level fusion mechanism. This integration provides explicit and uncorrupted spatial gradient guidance while ensuring highly stable optimization under low-data regimes. Extensive experiments demonstrate that SVP-IL significantly outperforms state-of-the-art VLAs and pure visuomotor baselines. Trained on as few as 50 to 100 demonstrations, SVP-IL improves average success rates on highly ambiguous language-conditioned tasks from 24.0% to 39.5%, achieving 67.8% on standard benchmarks. Real-world robotic experiments further validate its robustness and data efficiency in unstructured physical environments.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文信息生成的结构化、深入、客观的中文总结。

# 论文总结：解耦语义与几何基础：语言条件模仿学习的空间视觉提示 (SVP-IL)

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：端到端的 Vision-Language-Action (VLA) 模型在机器人操作中表现出色，但其“大一统”的架构将语义推理与空间控制高度耦合。这导致了严重的 **语义-空间对齐瓶颈**：模型难以从有限数据中隐式学习如何将抽象的语义指令（如“红色苹果”）精准地映射到连续的物理动作上，特别是在存在多个视觉上相似目标的复杂场景中，极易出现目标分辨错误和操作失误。
- **研究动机**：为克服这一耦合瓶颈，作者提出范式转变，将感知（语义/空间理解）与控制（动作生成）进行解耦。通过利用现成的视觉-语言基础模型来处理复杂的语义理解，从而减轻下游控制策略的学习负担。
- **整体含义**：SVP-IL框架旨在实现一种**数据高效、鲁棒性强**的语言条件化模仿学习。它将繁琐且易出错的隐式目标解析过程，转变为更简单、更明确的显式定位执行任务，显著提升了在低数据量下的性能。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：**解耦感知-控制架构**。将“理解指令指向哪个物体”这一语义推理任务从“如何运动以抓取该物体”的连续控制循环中剥离出来。通过一个外部的、冻结的视觉-语言模型将语言指令转化为显式的**空间视觉提示**，再将其作为强几何先验注入到可训练的动作生成器中。

- **关键技术细节**：
    1.  **两步式空间视觉提示生成**：
        - **指令解析**：使用大语言模型 (LLM) 从原始自然语言指令 `l` 中提取出目标物体的核心名称 `c`。
        - **零样本掩膜生成**：使用 SAM 3 模型，根据当前图像 `I_t` 和物体名称 `c`，生成一个零样本的、精确的二进制几何掩膜 `M_t`。这个掩膜就是所谓的 **空间视觉提示**，它剥离了纹理、光照等干扰信息，只留下目标的空间位置和形状。
    2.  **轻量级特征级融合机制**：
        - **问题**：传统的将掩膜叠加到RGB图像上的方法（图像级融合）会严重破坏预训练视觉编码器（如ResNet）的底层权重，导致纹理识别能力下降，在小样本数据下极易过拟合。
        - **解决方案**：提出一个并行的**提示流**（Prompt Stream），即一个轻量级的侧边卷积神经网络。该网络将稀疏的掩膜 `M_t` 编码成与基础编码器某一中间层 `i` 维度相同的特征图。
        - **融合方式**：通过直接的**逐元素加法**，将提示流特征 `F_prompt(i)` 与主视觉编码器在该层的特征 `F_rgb(i)` 融合。这种直接加法被证明比可学习的门控机制更稳定，因为它作为一种非参数化的结构偏置，能立即、无污染地为网络提供空间梯度引导，避免了低数据量下的过拟合风险。

- **整体流程**：给定图像和语言指令 -> LLM解析目标物体名 -> SAM 3生成空间掩膜 -> 主视觉编码器处理RGB图像 -> 侧边CNN处理掩膜 -> 特征级加法融合 -> 融合特征与机械臂本体感知、文本嵌入拼接 -> 输入扩散策略 (Diffusion Policy) 预测动作序列。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集与场景**：
    - **模拟环境**：使用 **RoboTwin 2.0** 基准，包含9个典型任务（如调整瓶子、挂杯子、摇瓶子等）。同时设计了**6个自定义的高歧义语言条件任务**（如“将特定物体放入篮子”），目标物体根据指令变化。评估在两种环境下进行：**Clean** (简洁背景) 和 **Randomized** (杂乱背景、可变光照)。
    - **实物环境**：使用 **Aloha-AgileX** 双臂机器人，设计了三个密集杂乱的任务：Table Setting、Desktop Cleaning、Bimanual Sorting，以测试在真实物理世界中的鲁棒性和数据效率。

- **基准 (Benchmark)**：RoboTwin 2.0 和一个自定义的高歧义语言条件任务基准。

- **对比方法**：
    - **ACT** (Action Chunking with Transformers)
    - **DP** (Diffusion Policy) - 纯视觉运动基线
    - **π0** - 一个具有代表性的、强大的端到端VLA基座模型

## 4. 资源与算力

- 论文中**未明确提及**训练所使用的GPU型号、数量或训练时长。这是一个常见的信息缺失，增加这些细节可以使工作更易于复现和比较。

## 5. 实验数量与充分性

- **实验数量**：相当充分。主要包括：
    - **主体实验**：在9个RoboTwin 2.0任务和6个自定义任务上比较成功率（共15个任务）。
    - **分布偏移鲁棒性测试**：对比在不同环境（Clean vs. Random）下训练和测试的表现。
    - **消融研究**：系统地对比了不同的掩膜融合策略（图像级叠加、早级通道拼接 vs. 本文的特征级融合）。
    - **实物实验**：在3个真实世界任务上进行验证，每个任务20次试验。

- **充分性与公平性**：
    - **充分**：实验覆盖了标准任务和高歧义任务，模拟和实物环境，从多个角度验证了方法的有效性。
    - **公平**：所有方法在相同的数据集上训练（50或100次演示），并采用相同的训练配置。用于比较的基线方法（DP, ACT, π0）都使用了其官方的默认配置，进行公平对比。
    - **客观**：实验结果以具体的成功率数值呈现，并进行了详细的对比分析，客观地指出了SVP-IL的优势和DP、π0的不足。

## 6. 主要结论与发现

- **有效性显著**：在模拟环境的标准基准上，SVP-IL的平均成功率达到 **67.8%**，显著优于π0 (53.7%)、ACT (45.2%) 和 DP (42.3%)。在高度依赖语义分辨的自定义任务上，优势更为明显，SVP-IL (39.5%) 几乎是 π0 (24.0%) 的两倍。
- **数据效率极高**：仅用50到100次演示就获得了上述性能，证明了其强大的数据利用效率。
- **特征级融合优于图像级融合**：消融实验表明，直接将空间掩膜进行特征级融合（SVP-IL）远优于在图像级进行叠加或通道拼接。后者会破坏预训练权重，性能提升有限。
- **强鲁棒性**：在Randomized环境中，SVP-IL的表现虽然也受到影响，但依然优于所有对比方法。即使DP使用了领域随机化（DR）进行训练，在低数据场景下，其性能因过拟合而崩溃，而SVP-IL提供的显式几何先验是更稳定、更有效的归纳偏置。
- **真实世界验证成功**：在真实物理世界的 Aloha-AgileX 机器人上，SVP-IL 达到了 **60.0%** 的平均成功率，远高于 DP (28.3%) 和 π0 (31.7%)，证明了其从模拟到真实 (sim-to-real) 的迁移能力和在实际杂乱场景下的鲁棒性。

## 7. 优点

- **架构创新**：新颖地将语义推理与空间执行解耦，直接、有效地解决了端到端VLA模型的核心对齐瓶颈。
- **数据高效**：在小样本（50-100条演示）情况下表现卓越，极大地降低了在实际机器人应用中采集数据的人力成本。
- **即插即用**：该方法不依赖于特定的下游控制策略，可以作为一个通用模块集成到任何视觉运动策略（如DP）中。
- **强健的融合机制**：提出的直接特征级加性融合简单、有效且稳定，避免了复杂门控机制在低数据量下的过拟合风险，提供了即时的空间梯度指导。
- **强可解释性**：通过显式的掩膜来指导控制，使得模型的决策过程更加透明和易于理解。

## 8. 不足与局限

- **上游模型瓶颈**：SVP-IL的性能高度依赖于上游视觉语言模型（本例中为SAM 3）。对于透明、高反射或被严重遮挡的物体，SAM 3可能会生成质量较差的掩膜，从而直接导致下游策略的失败。
- **计算开销**：在控制循环中集成了SAM 3等重型模型，引入了显著的计算延迟。这使得系统难以应对高速动态环境，更适合于静态或准静态的任务场景。
- **指令类型限制**：当前系统依赖**显式的、物体中心**的指令（如“拿起黄色香蕉”）。对于更抽象的、需要推断指令（如“清理桌子”）的任务，当前的方案尚无法直接应用。
- **对动态场景响应慢**：由于引入了上游模型推理的延迟，整个系统在需要快速反应和调整的动态环境中表现受限。

（完）
