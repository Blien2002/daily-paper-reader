---
title: "$ω$-EVA: Envision, Verify, and Act with Latent Interactive World Models"
title_zh: $\omega$-EVA：利用潜在交互式世界模型进行设想、验证与行动
authors: "Zhenguo Sun, Yu Sun, Hande Huang, Alois Knoll"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09457v1"
tags: ["query:vla"]
score: 9.0
evidence: 基于潜在世界模型的设想-验证-执行循环用于动作生成
tldr: 现有具身策略直接映射观察到动作，缺乏对候选动作后果的显式推理。本文提出ω-EVA，一个潜在交互世界模型，通过三阶段框架实现“设想-验证-行动”循环：学习动作条件潜在动力学，训练语言条件流策略，并利用世界模型反馈策略提议。三分支精炼器联合推理当前状态、动作条件未来和提议动作，避免生成未来视频。在多种模拟任务中，该交互流程显著提升策略性能，仅1.2B参数且无需额外预训练数据，使世界模型成为主动动作反馈模块。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09457v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1430, \"height\": 530, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09457v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1297, \"height\": 679, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09457v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1439, \"height\": 394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09457v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1434, \"height\": 997, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09457v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1458, \"height\": 471, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09457v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1455, \"height\": 344, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09457v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1450, \"height\": 476, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09457v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1225, \"height\": 226, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09457v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1453, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09457v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1455, \"height\": 279, \"label\": \"Table\"}]"
motivation: 现有世界模型多为被动预测器，策略难以在行动前检查自身提议的后果，导致动作质量受限。
method: ω-EVA包含三个阶段：学习动作条件潜在动力学、训练语言条件流策略、通过世界模型反馈优化提议；采用三分支精炼器联合推理。
result: 在单臂、双臂、长时域及扰动模拟中，ω-EVA的完整交互流程持续提升策略性能，以1.2B参数实现高效数据-性能权衡。
conclusion: ω-EVA将世界模型从被动预测器转变为主动动作反馈模块，展示了紧凑模型在具身任务中的竞争力。
---

## 摘要
具身策略通常直接将当前观测映射到动作，隐含了候选动作的后果。世界模型提供了预测性监督、表示或外部模拟，但很少让策略在行动前检查其自身提议的想象后果。我们引入了$\omega$-EVA，一种潜在交互式世界模型，实现了用于具身动作生成的“设想-验证-行动”循环。其三阶段框架学习动作条件下的潜在动力学，在动力学感知视觉表示上训练语言条件下的流策略，并通过世界模型反馈策略的提议。一个三分支精化器联合推理当前状态、提议条件下的未来以及提议动作，以生成最终的动作片段。由于后果推理保持在潜在特征空间中，$\omega$-EVA避免了在推理时生成未来视频。在多种单臂、双臂、长时域和扰动仿真设置中的评估表明，完整的交互管道持续改进了提议策略，而潜在诊断指示了有意义的动作条件未来结构。凭借约12亿参数且无额外机器人数据预训练，$\omega$-EVA展示了紧凑而有竞争力的性能-规模-数据权衡，使世界模型成为主动的动作反馈模块而非被动预测器。

## Abstract
Embodied policies typically map current observations directly to actions, leaving candidate-action consequences implicit. World models provide predictive supervision, representations, or external simulation, but rarely let a policy inspect the imagined consequence of its own proposal before acting. We introduce $ω$-EVA, a latent interactive world model that realizes an Envision--Verify--Act loop for embodied action generation. Its three-stage framework learns action-conditioned latent dynamics, trains a language-conditioned flow policy on dynamics-aware visual representations, and feeds the policy's proposal back through the world model. A tri-branch refiner jointly reasons over the current state, proposal-conditioned future, and proposed action to produce the final action chunk. Because consequence reasoning remains in latent feature space, $ω$-EVA avoids generating future videos at inference. Evaluations across diverse single-arm, bimanual, long-horizon, and perturbed simulation settings show that the complete interaction pipeline consistently improves the proposal policy, while latent diagnostics indicate meaningful action-conditioned future structure. With approximately 1.2B parameters and no additional robot-data pretraining, $ω$-EVA demonstrates a compact and competitive performance--scale--data trade-off, making the world model an active action-feedback module rather than a passive predictor.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究背景**：现有具身策略大多直接将当前观测映射为动作，候选动作的未来后果隐式编码在策略参数中，无法被策略在行动前显式检查。世界模型虽能提供预测性监督或表示，但通常仅作为训练辅助目标、表示学习信号或外部模拟器，很少让策略在行动前审视自身提议的后果并据此修正。
- **核心问题**：如何将世界模型从被动预测器转变为主动动作反馈模块，使策略在执行前通过“设想-验证-行动”循环检查其候选动作的想象后果，从而提升动作的鲁棒性和适应性。
- **整体含义**：本文提出的 ω-EVA 是一种潜在交互式世界模型，通过三阶段框架实现上述循环。其核心创新在于将未来预测转化为提案条件化反馈：策略首先提出动作块，世界模型设想该提案导致的潜在未来，精炼器联合推理当前状态、想象未来和提案动作，输出最终动作。整个交互在潜在特征空间进行，无需像素级视频生成，兼顾了后果推理和闭环控制效率。

### 2. 论文提出的方法论

- **核心思想**：将世界模型作为主动动作反馈模块嵌入动作生成流程中，实现“Envision–Verify–Act”循环。
- **关键技术与细节**：
  - **三阶段训练框架**：
    - **Stage 1：动作条件潜在世界模型**  
      - 输入：当前观测的特征 `Ic`（来自冻结的 DINOv3）、可变步长 `n` 的动作前缀 `a^{(n)}_{t:t+H}` 和一组可学习的未来查询 `Q`。  
      - 输出：预测的未来视觉特征 `Îf` 和动力学感知的当前表示 `ct`（通过因果多模态注意力掩码保证 `ct` 独立于动作）。  
      - 损失：`L_wm = ∥Îf - I^{(n)}_f∥_1`，目标是对应 `n` 后的未来观测特征。
    - **Stage 2：世界感知动作生成器**  
      - 使用 Stage 1 得到的 `ct` 和语言编码 `Te`（来自冻结的 T5），采用条件流匹配生成动作提案。  
      - 输入：带噪动作 `x_τ = (1-τ)a + τϵ`，流时间 `τ` 编码为自适应层归一化条件。  
      - 架构：时间条件查询变换器，包含自注意力、对 `ct` 的交叉注意力、对 `Te` 的交叉注意力。  
      - 训练损失：`L_{f_m} = ∥v_ϕ(x_τ, ct, Te, τ) - (ϵ-a)∥_2^2`，联合优化世界模型（`λ_wm=0.1`）或单独优化策略。  
      - 推理：从 `τ=1` 的噪声开始，通过欧拉积分沿递减序列采样至 `τ=0`，得到初始提案 `̂a^0_{t:t+H}`。
    - **Stage 3：想象交互动作精炼器**  
      - 冻结 Stage 1 和 Stage 2，仅训练精炼器 `R_ψ`。  
      - 流程：  
        1. 用哑动作从世界模型提取 `ct`，用 Stage 2 生成提案 `̂a^0_{t:t+H}`。  
        2. 将提案反馈给世界模型，得到想象未来 `Îf`。  
        3. 三分支精炼器接收当前表示 `ct`、想象未来 `Îf` 和提案 `̂a^0_{t:t+H}`，无注意力掩码联合交互，直接输出精炼动作 `̂a_{t:t+H}`。  
      - 损失：`L_{refine} = MAE(̂a, a_true)` 加权掩码。
  - **推理时的 Envision–Verify–Act 循环**：  
    - 编码当前观测和语言，提取 `ct`。  
    - 流策略生成初始提案。  
    - 世界模型设想提案的未来潜在后果。  
    - 精炼器输出最终动作，仅执行前 K 步，然后重规划。
- **公式与算法**：Algorithm 1（三阶段训练）、Algorithm 2（推理循环）详细描述了流程。

### 3. 实验设计

- **数据集/场景**：
  - **LIBERO**：包含 Spatial、Object、Goal、Long 四个子套件，测试空间推理、对象交互、目标理解和长时域执行。使用 agent 视图和手腕视图，256×256，动作维度 7，动作 horizon H=16。
  - **LIBERO-PLUS**：LIBERO 扩展，增加七类扰动（相机、机器人外观、语言、光照、背景、传感器噪声、场景布局）。评估两种设置：零样本（仅训练 LIBERO）和专训（在 LIBERO-PLUS 训练）。
  - **RoboTwin 2.0**：双臂操控，提供左右手腕和头部相机，三视图拼接为单图，动作维度 14，H=16。
- **Benchmark 与对比方法**：
  - 对比方法包括 OpenVLA、π0、π0.5、LingBot-VA、Motus、VLA-JEPA、Fast-WAM 等，区分是否使用机器人预训练。
  - ω-EVA 不使用任何额外机器人预训练（DINOv3 和 T5 仅作为通用编码器）。
- **评价指标**：任务成功率（%）。

### 4. 资源与算力

- **硬件**：16 块 NVIDIA H100 GPU（80GB 显存），使用 Distributed Data Parallel。
- **训练配置**：  
  - 优化器：AdamW，初始学习率 1e-4，余弦退火至 1e-6，权重衰减 1e-6，β=(0.9,0.95)。  
  - 预热 500 步，bfloat16 混合精度，总 batch size 1024。  
  - Stage 1/2/3 分别训练 50/30/20 个 epoch。  
- 算力信息明确，但未给出具体训练耗时（小时数）。

### 5. 实验数量与充分性

- **主要实验**：  
  - LIBERO 四个子套件的成功率对比（表 1）。  
  - LIBERO-PLUS 零样本和专训成功率（表 2）。  
  - RoboTwin 2.0 干净和随机化设置成功率（表 3）。  
- **消融实验**（均基于 LIBERO）：  
  - 当前表示的可视化分析（图 3）和动作不变性验证（表 4）。  
  - 潜在未来保真度分析（表 5，SSIM / FID）。  
  - Stage 3 分支消融：移除想象未来或移除动作提案（表 6）。  
- **充分性评价**：  
  - 覆盖了单臂、双臂、长时域、扰动等多种场景，对比方法包含多个强基线且区分预训练状态，实验设计较为全面。  
  - 消融实验验证了各组件的必要性，特别是 Stage 3 的未来反馈和提案锚定作用。  
  - 可能不足：LIBERO-PLUS 零样本实验仅训练在 LIBERO，而其他方法可能使用了更多数据；未进行真实机器人验证；未探索迭代精炼或多步规划。

### 6. 论文的主要结论与发现

- **核心结果**：完整的 Envision–Verify–Act 管道在三个 benchmark 上持续优于仅用 Stage 2 的提案策略：  
  - LIBERO 平均成功率从 97.9% 升至 98.6%（最大提升在 Long 套件，+1.6pts）。  
  - LIBERO-PLUS 零样本从 71.3%→72.2%，专训从 81.2%→83.4%。  
  - RoboTwin 2.0 从 88.9%→90.3%。  
- **紧凑性**：ω-EVA 仅约 1.2B 参数（不含 T5），无机器人预训练，优于或接近更大规模的、有预训练的方法，展示良好的性能-规模-数据权衡。  
- **诊断分析**：  
  - 当前表示 `ct` 在空间激活上聚焦于末端执行器和交互区域，且严格动作无关（表 4：不同动作下 `ct` 完全一致）。  
  - 潜在未来保真度：Stage 3 精炼后动作编码的未来比 Stage 2 提案更接近真实未来（SSIM 提升 0.0042，FID 略微改善）。  
  - Stage 3 分支消融证实想象未来和提案锚定共同作用，缺失其一会导致性能下降甚至低于 Stage 2。

### 7. 优点

- **方法创新性**：将世界模型从被动预测器提升为主动动作反馈模块，通过“设想-验证-行动”循环实现提案条件化后果推理，无需像素级视频生成，兼顾效率与可解释性。
- **设计优雅**：三阶段逐步解耦：学习动力学 → 训练策略 → 加入交互精炼，易于训练和调优；因果注意力掩码保证 `ct` 不受动作影响，使策略获得动力学感知但动作无关的表示。
- **实验证明充分**：在多个模拟 benchmark 上验证了有效性，消融实验深入剖析了每个组件的贡献，动作不变性检验和潜在未来可视化等诊断合理。
- **资源高效**：1.2B 参数、无机器人预训练即达到或超越更大模型的结果，显示紧凑架构的竞争力。

### 8. 不足与局限

- **实验覆盖有限**：仅在模拟环境中评估，未进行真实机器人实验，泛化到真实场景的可靠性未知。
- **部分扰动改进不均衡**：在 LIBERO-PLUS 中，语言和光照扰动下性能略有下降（Stage 3 vs Stage 2），说明当前精炼机制对这些扰动不鲁棒。
- **未探索迭代精炼**：仅进行一阶“设想-验证-行动”，未研究多次迭代的收敛行为、延迟与性能权衡。
- **潜在表示的可解释性有限**：虽然诊断解码器展示了预测保真度，但潜在 `Îf` 的具体语义内涵未充分分析，可能限制了精细调试。
- **数据与规模限制**：仅使用 benchmark 训练数据，未利用大规模机器人视频或轨迹进行预训练；尽管紧凑，但在某些任务（如 RoboTwin 2.0）上仍低于部分更大模型（如 Fast-WAM 6B）。  
- **假设条件**：依赖于冻结的 DINOv3 和 T5 编码器，当观测与预训练分布偏移较大时可能失效。

（完）
