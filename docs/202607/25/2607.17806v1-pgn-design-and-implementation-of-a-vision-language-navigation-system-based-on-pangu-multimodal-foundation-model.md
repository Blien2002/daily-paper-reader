---
title: "PGN: Design and Implementation of a Vision-Language Navigation System Based on Pangu Multimodal Foundation Model"
title_zh: PGN：基于盘古多模态基础模型的视觉语言导航系统的设计与实现
authors: "Li Xian, Mingxi Li, Yizheng Wang, Yiming Shen, Qi Chen, Zhuoling Xiao"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17806v1"
tags: ["query:vla"]
score: 9.0
evidence: 基于多模态基础模型的视觉语言导航系统
tldr: "视觉-语言导航要求具身代理理解自然语言指令并依据时序视觉观察预测动作。本文提出PGN系统，基于OpenPangu-7B多模态大模型，分两阶段训练：阶段一冻结视觉与语言骨干，训练Q-Former和MLP进行对齐；阶段二采用五观察窗口、epoch依赖采样和推理后动作格式，微调少量参数适配专家轨迹。在500条留出轨迹的教师强制评估下，V9模型达到62.29%归一化动作匹配和100%非空率，表明离线动作预测与专家行为高度一致。该工作为多模态大模型在视觉-语言导航任务中的应用提供了有效框架。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17806v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1169, \"height\": 845, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 804, \"height\": 567, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1812, \"height\": 751, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 819, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 854, \"height\": 679, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 796, \"height\": 667, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17806v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 280, \"label\": \"Table\"}]"
motivation: 将多模态大模型适配到VLN任务面临视觉-语言对齐、紧凑时序输入、动作空间基础及稳定训练挑战，PGN旨在解决这些问题。
method: 两阶段训练：阶段一训练Q-Former和MLP对齐视觉与语言；阶段二冻结视觉通路，更新结构令牌嵌入和LoRA，用五观察窗口和推理后动作格式适配专家轨迹。
result: "在500条留出轨迹的教师强制评估下，V9达62.29%归一化动作匹配和100%非空率，离线动作预测与专家高度一致。"
conclusion: PGN证明了基于盘古多模态模型的离线VLN动作预测可行，为闭环评估与优化奠定基础。
---

## 摘要
视觉语言导航（VLN）要求具身智能体根据自然语言指令，从时序视觉观测中预测动作。将多模态大语言模型适配到VLN需要视觉-语言对齐、紧凑的时序输入、动作空间基础化以及在目标硬件上的稳定训练。本技术报告介绍了PGN（盘古导航器），一个基于OpenPangu-7B构建的离线VLN动作预测系统。训练分两个阶段进行。首先，PGMM通过训练Q-Former和两层MLP投影器，将冻结的EVA-ViT-G/14视觉编码器与冻结的语言主干对齐。其次，PGN使用五观测窗口、依赖于周期的时序采样以及推理后动作的输出格式，将对齐后的模型适配到专家导航轨迹；该阶段冻结对齐的视觉通路，更新三个结构词元嵌入和LoRA适配器。实现结合了混合精度计算、选择性FP32计算以及在八个Ascend 910B NPU上的DeepSpeed ZeRO-2。在500条保留的专家轨迹上，在教师强制、开环评估下，V9报告了62.29%的归一化动作匹配（NAM）和100.00%的非空率（NER）。这些指标量化了离线的专家动作对齐，而非闭环导航成功；评估误差累积、路径效率和目标完成度仍是未来工作。

## Abstract
Vision-Language Navigation (VLN) requires an embodied agent to interpret a natural-language instruction and predict actions from temporally ordered visual observations. Adapting a multimodal large language model to VLN requires visual-language alignment, compact temporal inputs, action-space grounding, and stable training on the target hardware. This technical report presents PGN (Pangu Navigator), an offline VLN action-prediction system built on OpenPangu-7B. Training proceeds in two stages. First, PGMM aligns a frozen EVA-ViT-G/14 vision encoder with the frozen language backbone by training a Q-Former and a two-layer MLP projector. Second, PGN adapts the aligned model to expert navigation trajectories using five-observation windows, epoch-dependent temporal sampling, and a reasoning-then-action output format; this stage freezes the aligned visual pathway and updates three structural-token embeddings and LoRA adapters. The implementation combines mixed-precision computation, selective FP32 computation, and DeepSpeed ZeRO-2 on eight Ascend 910B NPUs. Under teacher-forced, open-loop evaluation on 500 held-out expert trajectories, V9 reports a 62.29% Normalized Action Match (NAM) and a 100.00% Non-empty Rate (NER). These metrics quantify offline expert-action alignment rather than closed-loop navigation success; evaluating error accumulation, path efficiency, and goal completion remains future work.

---

## 论文详细总结（自动生成）

# 论文详细中文总结：PGN：基于盘古多模态基础模型的视觉语言导航系统设计与实现

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：将多模态大语言模型（MLLM）适配到连续环境下的视觉语言导航（VLN）任务面临四个核心挑战：视觉与语言模态的对齐、时序观测的紧凑表示、模型输出与有限动作空间的接地，以及在特定硬件（如 Ascend NPU）上的稳定训练。
- **动机**：现有 VLN 方法多依赖历史感知 Transformer 或域内预训练，但尚未充分挖掘 MLLM 的巨大潜力。本文旨在通过两阶段训练策略，将 7B 规模的 MLLM（OpenPangu-7B）适配到离线 VLN 动作预测任务，验证可行性并为后续闭环导航奠定基础。

## 2. 提出的方法论
- **核心思想**：采用分阶段的参数冻结训练策略，实现视觉-语言对齐与导航任务适应的解耦，并利用 LoRA 等参数高效微调技术降低训练开销。
- **关键技术细节**：
  - **视觉-语言对齐（阶段一）**：
    - 冻结视觉编码器（EVA-ViT-G/14，输出 patch 级特征）和语言骨干（OpenPangu-7B）。
    - 训练 Q-Former（32 个可学习查询）和两层 MLP 投影器，将 768 维视觉表示映射到 4096 维语言嵌入空间。
    - 在图像-文本对数据集（COCO、Visual Genome）上进行图像条件文本生成训练。
  - **导航适应（阶段二）**：
    - 冻结阶段一训练好的视觉通路及语言骨干，仅更新三个结构词元嵌入（分别标记图像开始、结束和帧边界）和 LoRA 适配器。
    - 输入格式：固定长度为 5 的观测窗口 `Ot = (ot-4s, ot-3s, ot-2s, ot-s, ot)`，其中 `s` 是可变的时序步长；观测按时间顺序拼接，并用结构词元分隔。
    - 动作空间：`{forward, left, right, stop}`（前进 0.25m，转弯 15°）。
    - 输出格式：推理文本（`rt`）后跟规范动作（`at`），条件概率分解为 `P(rt, at | I, Ot) = P(rt | I, Ot) P(at | rt, I, Ot)`。
  - **时序与动作感知采样**：
    - 根据周期 `e` 改变起始索引 `istart = e mod s`，实现多样的时序偏移。
    - 三阶段课程（s, p）：(4, 0.1)、(6, 0.6)、(8, 0.2)，其中 `p` 是动作感知采样中替换前向类样本为转向/停止样本的概率。
  - **优化与硬件适配**：
    - 使用 8 张 Ascend 910B NPU、混合精度计算（FP16）、DeepSpeed ZeRO-2。
    - 对出现数值溢出的 FP16 注意力掩码操作，选择性使用 FP32 计算，而非全局 FP32。
- **公式/算法流程**（文字说明）：
  - 训练分为两阶段：阶段一优化 Q-Former 和投影器 → 阶段二冻结视觉通路，优化结构词元和 LoRA → 推理时输入指令和 5 帧观测，生成推理文本+动作。

## 3. 实验设计
- **数据集/场景**：
  - **视觉-语言对齐训练**：Microsoft COCO 和 Visual Genome 的图像-文本对。
  - **导航轨迹收集**：通过 DAgger 兼容管道（β=1）仅使用专家策略收集 19,699 条轨迹，共 1,567,487 帧；每帧含自然语言指令、观测序列和专家动作标签。
  - **离线评估**：500 条留出的专家轨迹（验证集），步长 s=1，进行教师强制（teacher-forced）、开环（open-loop）评估——提供固定五帧观测窗口，模型预测动作但不执行。
- **Benchmark 与对比方法**：
  - 未与现有的 VLN 基准方法（如 VLN-CE 上的主流模型）直接对比。
  - 仅比较自身不同版本（V3、V5、V6、V8、V9）的离线指标。
- **评估指标**：
  - Exact-string Match（EM）：生成响应精确等于规范动作字符串（V8/V9 因输出推理文本导致 EM=0）。
  - Normalized Action Match（NAM）：从响应中提取归一化动作并与专家动作比较。
  - Non-empty Rate（NER）：产生非空响应的比例。

## 4. 资源与算力
- **硬件**：8 张华为 Ascend 910B NPU。
- **软件栈**：混合精度（FP16），DeepSpeed ZeRO-2，对不稳定操作选择性 FP32。
- **训练时长**：文中未明确说明阶段一和阶段二的训练周期数或总时长，也未报告具体 Wall-clock 时间。

## 5. 实验数量与充分性
- **实验数量**：
  - 主要报告了 V3、V5、V6、V8、V9 五个版本在 500 条轨迹上的动作级平均指标和每个轨迹区间的分布。
  - V6 保留逐动作日志（40,000 条预测），V8/V9 仅保留汇总统计。
- **充分性与公平性**：
  - **不足**：
    - 仅有离线教师强制评估，无闭环 Habitat 模拟器评估，无法衡量成功率、SPL、导航误差等标准 VLN 指标。
    - 未与现有 VLN 方法（如 NavGPT、History-Aware Transformer 等）做对比。
    - 缺乏消融实验：推理后动作格式、时序采样、动作感知采样等组件是同时引入的，无法分离各组件贡献。
    - V3 的数据区间计数之和仅 56 而非 500，数据一致性存疑。
    - 未报告 V8/V9 的逐动作错误分布。
  - **客观性**：论文明确承认这些限制，并声明报告指标仅为开发记录，不构成与已有方法的比较。

## 6. 主要结论与发现
- V9 达到 **62.29% NAM** 和 **100.00% NER**，表明在教师强制、开环设置下，模型能高比例匹配专家动作且几乎从不产生空响应。
- 从 V5/V6 到 V8/V9，NAM 显著提升（约 23% → 62%），且每个轨迹的动作匹配分布从 10-30% 区域移向 50-80% 区域，说明改进有效。
- 论文强调这些结果 **不** 代表闭环导航成功，未来需在 Habitat 模拟器上执行预测动作并评估误差累积、路径效率和目标完成度。

## 7. 优点
- **分阶段训练解耦**：将视觉-语言对齐与导航任务适应明确分开，降低了训练复杂度，便于模块化改进。
- **参数高效微调**：阶段二仅更新少量结构词元嵌入和 LoRA 适配器，在 7B 模型上实现经济高效的适配。
- **稳定性处理**：针对 Ascend NPU 上 FP16 的不稳定问题，采用选择性 FP32 计算而非全局降精度，兼顾效率与数值稳定性。
- **结构化输入输出设计**：引入帧边界、推理文本等结构元素，促进模型理解时序和多模态输入，符合当前 MLLM 指令跟随范式。
- **提供详细开发历程**：公开不同迭代版本的指标分布，便于社区了解模型演进轨迹，具有工程指导意义。

## 8. 不足与局限
- **实验覆盖不足**：仅离线、教师强制评估，未进行闭环 Habitat 测试，无法直接对比主流 VLN-CE 方法（如成功率、SPL）。
- **缺乏对比实验**：未与已知 VLN 方法（如 NavGPT、HAMT、VLN-CE 基线）在同一条件下比较，削弱了方法有效性的说服力。
- **无消融分析**：未能分离推理后动作格式、时序采样策略、动作感知采样等组件的独立贡献，无法进行因果归因。
- **数据与复现问题**：V3 数据异常（区间计数和不为 500），且 V8/V9 未保留逐动作日志，限制了精细错误分析和复现验证。
- **应用限制**：仅在固定观测窗口（5 帧、步长 s=1 评估）下工作，未验证较长序列或变长输入下的泛化能力；动作空间简化（仅四种离散动作），与真实连续导航仍有差距。
- **硬件说明：训练时长和能耗未记载**，不利于评估实际部署成本。

（完）
