---
title: "Anticipate Before Acting: Future-State-Conditioned Vision-Language Navigation"
title_zh: 先预测再行动：基于未来状态的视觉语言导航
authors: "Lingfeng Zhang, Zhanguang Zhang, Liheng Ma, Tongtong Cao, Yingxue Zhang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18042v2"
tags: ["query:vla"]
score: 9.0
evidence: 端到端视觉语言动作模型用于导航
tldr: 端到端视觉语言导航中，标准行为克隆仅监督下一步动作，导致长程决策受限。本文提出未来状态条件化导航（FSC-VLN），通过训练时将未来查询表示与未来视觉嵌入对齐，使策略状态隐式蕴含未来信息，推理时仅需历史观测。在R2R数据集上，FSC-VLN显著提升导航成功率、目标导向成功率及SPL，尤其长程任务提升更明显。该方法仅增加两个前缀token，计算开销极小，为VLN提供了高效的长程规划新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18042v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1429, \"height\": 685, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18042v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1361, \"height\": 410, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1261, \"height\": 361, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1291, \"height\": 248, \"label\": \"Table\"}]"
motivation: 标准行为克隆缺乏对未来视觉结果的显式预测，限制长程决策；特权输入实验表明未来观测包含丰富信息。
method: 提出FSC-VLN，训练时用未来视觉嵌入监督未来查询token，使策略状态编码未来信息，推理不变。
result: 在R2R val-unseen上，SR/OSR/SPL均有提升，长程任务增益更大，且仅增加两个前缀token。
conclusion: FSC-VLN通过隐式未来状态预测有效提升VLN长程能力，轻量设计易于集成。
---

## 摘要
端到端的视觉语言导航（VLN）使用因果视觉语言模型将指令和第一人称观察直接映射到动作，但标准的行为克隆仅监督下一个动作，并不明确鼓励策略状态预测未来的视觉结果，从而限制了长程决策。一项特权输入诊断表明，访问专家轨迹中的未来图像可以显著改善导航，这表明未来观察包含丰富的可操作线索，尽管这类输入在部署时不可用。受此信号启发，我们提出了基于未来状态的VLN（FSC-VLN），这是一种可部署的模型，它通过未来查询令牌增强因果策略，并使用仅训练时使用的未来状态监督，将未来观察中的信息提炼到策略状态中。具体来说，在训练期间，我们将未来查询表示与提前$Δ$步的冻结视觉嵌入对齐，而推理时仅需要过去和当前的观察。这种设计保留了基线推理模式，仅增加了两个学习的前缀令牌，意味着最小的开销。在R2R val-unseen上，FSC-VLN在两种训练数据设置下，相较于StreamVLN风格的基线，在SR/OSR/SPL指标上均有提升，且在长程场景中增益更大；消融实验进一步支持了将未来查询与动作查询分离的双查询设计。

## Abstract
End-to-end vision-language navigation (VLN) with causal vision-language models maps instructions and egocentric observations directly to actions, but standard behavior cloning supervises only the next action and does not explicitly encourage the policy state to be predictive of future visual outcomes, limiting long-horizon decision making. A privileged-input diagnostic shows that access to an expert-trajectory future image can substantially improve navigation, indicating that future observations contain rich, actionable cues, though such inputs are unavailable at deployment. Motivated by this signal, we propose Future-State-Conditioned VLN (FSC-VLN), a deployable model that augments a causal policy with a future-query token and uses training-only future-state supervision to distill information from future observations into the policy state. Concretely, during training we align the future-query representation to a frozen visual embedding $Δ$ steps ahead, while inference requires only past and current observations. This design preserves the baseline inference pattern and adds only two learned prefix tokens, implying minimal overhead. On R2R val-unseen, FSC-VLN improves SR/OSR/SPL over a StreamVLN-style baseline under two training-data regimes, with larger gains on long-horizon episodes; ablations further support the dual-query design that separates future and action queries.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在端到端的视觉语言导航（VLN）中，标准行为克隆（behavior cloning）仅监督下一步动作的预测，缺乏对策略状态预测未来视觉结果的显式约束。这导致长程导航任务中局部动作误差逐渐累积，限制了智能体在长程规划中的决策能力。
- **研究动机**：
    - 特权输入诊断实验表明，若在测试时直接提供专家轨迹中未来 Δ 步的图像，导航性能显著提升（SR 从 29.96% 提升至 71.77%），证明未来观测包含丰富的可操作线索。
    - 如何在不访问未来观测的条件下，仅通过训练时的未来状态监督来提升在线策略？论文提出将未来预测作为辅助训练目标，蒸馏未来信息到策略状态中，且推理时不增加额外开销。
- **整体含义**：本文提出了一种轻量级的未来状态条件化导航方法（FSC-VLN），通过训练时对齐未来查询表示与未来视觉嵌入，使策略隐式编码未来信息，从而改善长程决策，而推理时仅需过去和当前观测。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

**核心思想**：在因果VLM策略中插入一个可学习的未来查询 token（qF）和一个动作查询 token（qA），训练时仅将 qF 的隐藏状态与冻结视觉编码器提取的 Δ 步后观测的池化嵌入进行对齐，而动作生成时通过因果关系利用该未来感知表示；推理时丢弃目标分支，仅保留未来的隐式表示。

**关键技术细节**：
- **模型架构**（图1）：
    - 固定视觉编码器（SigLIP + MLP）处理当前和历史观测，生成视觉 tokens。
    - 在线上下文 Ct 后依次插入未来查询 qF 和动作查询 qA，然后生成动作 token 序列。
    - 训练时，额外使用相同的冻结视觉编码器处理未来观测 ot+Δ，通过 stop-gradient 得到目标嵌入 z*_{t+Δ}（平均池化后的 visual token）。
    - 未来对齐损失 L_future = || g(hF_t) - g(z*_{t+Δ}) ||_1，其中 g 为低维投影，使用 ℓ1 损失。
- **因果序列**：Xt = [Ct, qF, qA, at]。qF 可关注上下文，qA 可关注 qF 及上下文，动作 token 可关注所有前缀。这种排序使动作生成能利用未来预测信息。
- **训练目标**：总损失 L_total = LLM + λF L_future，其中 LLM 为动作的交叉熵损失，λF=0.1。
- **推理**：仅保留在线分支和两个查询 token，没有目标分支，无未来图像输入，无额外前向传播。所有 token 在一次因果预填充中处理，然后自回归解码动作。

**公式/算法流程文字说明**：
1. 输入：指令 I，当前观测 ot，历史观测 Ht。
2. 冻结 SigLIP 编码所有观测得到视觉 tokens，构建上下文 Ct。
3. 拼接 qF, qA，序列输入因果Transformer。
4. 从 Transformer 输出层取出 qF 位置隐藏状态 hF_t，与未来目标嵌入 z*_{t+Δ} 计算 ℓ1 对齐损失。
5. 从 qA 位置隐藏状态开始自回归预测动作，计算交叉熵损失。
6. 总损失优化 Transformer 参数及两个查询嵌入（视觉编码器冻结）。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
    - 训练：R2R（Room-to-Room）和 RxR（Room-across-Room）的室内导航指令-轨迹对。
    - 额外扩充：EnvDrop（环境丢弃数据增强）和 ScaleVLN（大规模VLN训练集）。
    - 评估：R2R val-unseen 分割（1839个episode），按起始-目标测地距离分为短程（≤10 m，1293个）和长程（>10 m，546个）。
- **基准方法**：StreamVLN风格基线（StreamVLN），共享相同的骨干、优化器、学习率等配置。
- **对比方法**：
    - 直接与基线对比（两种训练数据设置：R2R+RxR 和 R2R+RxR+EnvDrop+ScaleVLN）。
    - 特权输入诊断实验：基线 + 专家未来图像（仅诊断，不可部署）。
    - 消融实验：移除动作查询 token（仅保留未来查询）的变体。

## 4. 资源与算力

- **文中明确说明**：
    - 使用 4 块 GPU，每设备 batch size 1，梯度累积 16 步（全局 batch size 64）。
    - 训练 1 个 epoch（遍历所有构建的轨迹片段）。
    - 使用 bfloat16 精度、梯度检查点、DeepSpeed ZeRO-2。
    - 未说明具体 GPU 型号（通常可推断为 NVIDIA A100 或类似），未说明训练时长。

## 5. 实验数量与充分性

- **实验组数**：
    1. 特权输入诊断实验（1组）。
    2. 主要结果：两种训练数据设置下 FSC-VLN vs 基线（2组）。
    3. 按轨迹长度分层评估（短程/长程，共4组结果）。
    4. 消融实验：完整模型 vs 移除动作查询（1组，含分层结果）。
- **充分性与客观性**：
    - 实验设计较为完整，评估了总体指标及长程/短程差异，验证了核心假设。
    - 采用分区评估，避免平均效应掩盖关键增益。
    - 消融实验直接检验了双查询设计的必要性。
    - 但缺乏与其他最新VLN方法（如NaVid、Uni-NaVid、NaVILA等）的直接对比（论文仅在基线上改进）。
    - 超参数（如 λF=0.1, Δ=32）未进行系统敏感性分析。
    - 仅使用了 R2R 一个测试集（val-unseen），未报告 RxR 或测试服务器结果，泛化性证据有限。

## 6. 论文的主要结论与发现

- **未来观测信息有用**：特权输入实验证明未来图像能极大提升导航性能（SR+41.81 pp）。
- **FSC-VLN有效**：在两种训练数据设置下，FSC-VLN 均优于基线，总体指标提升（R2R+RxR: SR +1.47, OSR +3.27, SPL +1.11 pp；加更大数据：SR +0.98, OSR +1.04, SPL +0.85 pp）。
- **长程收益更显著**：短程任务增益很小（<1 pp），长程任务 SR 提升 2.93–3.85 pp，SPL 提升 2.52–3.70 pp，OSR 提升 3.67–7.15 pp。
- **双查询必要**：移除动作查询后，长程性能下降（SR -1.28 pp, SPL -1.09 pp），表明未来监督与动作生成应解耦。

## 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
    - 轻量级设计：仅增加两个可学习前缀 token，推理成本几乎不变。
    - 训练-推理解耦：未来监督仅在训练时使用，推理无额外开销，无需未来图像或生成模型。
    - 因果顺序设计（qF→qA→动作）使未来信息自然流入动作解码。
- **实验亮点**：
    - 特权输入诊断直接验证了未来观测的可用性，为方法提供强动机。
    - 分层分析揭示长程提升，避免被短程结果平均掩盖。
    - 消融实验设计简洁，直接对比是否分离未来查询与动作查询。

## 8. 不足与局限

- **实验覆盖不足**：
    - 仅测试 R2R val-unseen 一个分割，未报告 RxR 验证集或 R2R 测试服务器结果，泛化性存疑。
    - 未与近期端到端工作（如 NaVid、Uni-NaVid、NaVILA、JanusVLN）进行直接对比，仅以 StreamVLN 为基线。
    - 超参数（Δ=32, λF=0.1）无敏感性分析，可能依赖经验设定。
- **局限**：
    - 未来目标来自专家轨迹，与智能体实际 on-policy 的未来可能不一致，可能导致训练-测试不匹配（compounding errors）。
    - 固定 Δ 可能不适应不同场景的长度或速度。
    - 仅使用 7B 模型（Qwen2-7B），未探索其他骨干（如更小/更大模型）的适用性。
    - 1 epoch 训练可能不足以充分利用未来监督信号。
- **应用限制**：方法假设存在专家轨迹（预收集数据），无法用于在线探索场景。

（完）
