---
title: "Embodied.cpp: A Portable Inference Runtime of Embodied AI Models on Heterogeneous Robots"
title_zh: "Embodied.cpp: 面向异构机器人上具身AI模型的可移植推理运行时"
authors: "Ling Xu, Chuyu Han, Borui Li, Hao Wu, Shiqi Jiang, Ting Cao, Chuanyou Li, Sheng Zhong, Shuai Wang"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02501v2"
tags: ["query:vla"]
score: 9.0
evidence: 面向异构机器人的VLA与WAM模型便携推理运行时
tldr: "具身AI模型（如VLA和WAM）的部署面临碎片化问题，现有推理运行时仅适用于请求-响应服务，无法满足多速率闭环控制、异构硬件和可扩展接口的需求。本文提出Embodied.cpp，一种便携C++推理运行时，通过五层架构（输入适配器、序列构建器、骨干执行、头插件、部署适配器）实现模块化多速率执行和延迟优先融合推理。在HY-VLA和pi0.5上任务成功率达100%和91%，WAM块内存从312.2MiB降至88.1MiB。该工作提升了部署效率，保持了高精度，为异构机器人上的具身模型部署提供了统一方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02501v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1784, \"height\": 892}, {\"url\": \"assets/figures/arxiv/2607.02501v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1779, \"height\": 965}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02501v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1786, \"height\": 1048}, {\"url\": \"assets/tables/arxiv/2607.02501v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1782, \"height\": 384}, {\"url\": \"assets/tables/arxiv/2607.02501v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1790, \"height\": 290}, {\"url\": \"assets/tables/arxiv/2607.02501v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1789, \"height\": 215}]"
motivation: 现有推理运行时仅适合请求-响应式服务，不满足具身部署的闭环控制、异构硬件和可扩展接口需求。
method: 提出五层架构（输入适配器、序列构建器、骨干执行、头插件、部署适配器），实现模块化多速率执行和延迟优先融合推理。
result: "VLA模型任务成功率100%和91%，WAM块内存从312.2MiB降至88.1MiB。"
conclusion: Embodied.cpp提升了部署效率，同时保持高精度，适用于多种具身模型架构。
---

## 摘要
具身AI模型现在涵盖了视觉-语言-动作（VLA）模型和世界-动作模型（WAM），但实际部署仍然分散在不同的模型特定Python栈、后端假设和机器人侧胶水代码中，尤其是在异构边缘设备上。现有的推理运行时主要设计用于请求-响应服务，因此不满足具身部署的运行时要求：闭环控制内的多速率执行、异构硬件上的延迟优先批量1推理，以及超越固定令牌I/O的可扩展具身接口。我们提出了Embodied$.$cpp，一个用于具身模型的可移植C++推理运行时。基于对代表性VLA模型和WAM的架构分析，Embodied$.$cpp捕获了一个共享执行路径，并将其组织为五个层次：输入适配器、序列构建器、骨干执行、头插件和部署适配器。该运行时提供了模块化的多速率执行、延迟优先的融合推理以及可扩展的操作符和I/O支持，通过一个后端抽象实现了在异构设备、机器人和模拟器上的部署。我们在两个VLA模型HY-VLA和pi0.5，以及一个使用LingBot-VA Transformer块的初步WAM基准上评估了Embodied$.$cpp。VLA部署实现了成功的闭环执行，任务成功率分别为100.0%和91.0%。WAM基准将块内存从312.2 MiB减少到88.1 MiB。这些结果表明，Embodied$.$cpp在保持跨多种具身模型架构的高准确性的同时，提高了部署效率。

## Abstract
Embodied AI models now span vision-language-action (VLA) models and world-action models (WAMs), but practical deployment remains fragmented across model-specific Python stacks, backend assumptions, and robot-side glue code, especially on heterogeneous edge devices. Existing inference runtimes are designed mainly for request-response serving and therefore do not satisfy the runtime contract of embodied deployment: multi-rate execution inside closed-loop control, latency-first batch-1 inference on heterogeneous hardware, and extensible embodied interfaces beyond fixed token I/O. We present Embodied$.$cpp, a portable C++ inference runtime for embodied models. Based on an architectural analysis of representative VLA models and WAMs, Embodied$.$cpp captures a shared execution path and organizes it into five layers: input adapters, sequence builders, backbone execution, head plugins, and deployment adapters. The runtime provides modular multi-rate execution, latency-first fused inference, and extensible operator and I/O support, enabling deployment across heterogeneous devices, robots, and simulators through one backend abstraction. We evaluate Embodied$.$cpp on two VLA models, HY-VLA and pi0.5, and on a preliminary WAM benchmark using a LingBot-VA Transformer block. The VLA deployments achieve successful closed-loop execution with 100.0% and 91.0% task success rates, respectively. The WAM benchmark reduces block memory from 312.2 MiB to 88.1 MiB. These results show that Embodied$.$cpp improves deployment efficiency while preserving high accuracy across diverse embodied model architectures.

---

## 论文详细总结（自动生成）

# Embodied.cpp: 面向异构机器人上具身AI模型的可移植推理运行时 —— 详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：具身AI模型（包括视觉-语言-动作VLA模型和世界-动作模型WAM）在实际部署中仍然高度碎片化。每个模型家族往往依赖特定的Python栈、专用的后端假设和机器人侧的胶水代码，尤其是在异构边缘设备（如Jetson、RK板、x86边缘盒等）上。现有的推理运行时（如llama.cpp、ONNX Runtime、SGLang、vLLM-Omni）主要针对请求-响应式服务设计，采用统一的令牌I/O和吞吐量优化，无法满足具身部署的特殊运行时合约：**闭环控制内的多速率执行**（感知、预测、动作等模块可能以不同频率运行）、**异构硬件上的延迟优先批量1推理**（而非吞吐量），以及**超越固定令牌I/O的可扩展具身接口**（输入包括图像、语言、本体感受、触觉等，输出包括动作块、世界预测等）。
- **整体含义**：现有系统要么是通用推理运行时缺乏具身模型的一等支持，要么是仅针对VLA模型（如vla.cpp）。缺乏一个**统一、可移植、跨模型家族（VLA+WAM）且直接对接机器人和模拟器的推理运行时**。论文旨在填补这一空白。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- **共享执行路径的提取**：通过对VLA模型和WAM的架构分析（图1、表1），发现尽管模型结构多样，但可以抽象出一个共享执行路径，主要差异集中在少数的可插拔头（head）和预测模块上。
- **五层架构**：将共享路径组织为五个层次：
  1. **输入适配器（Input Adapters）**：吸收在线传感器流（摄像头、力触觉、IMU）和离线数据集样本（如LIBERO、DROID）。
  2. **序列构建器（Sequence Builders）**：将多模态输入投影到统一表示空间。
  3. **骨干执行（Backbone Execution）**：执行Transformer等核心推理。
  4. **头插件（Head Plugins）**：可插拔的模型特定头部（动作预测、世界模型输出等）。
  5. **部署适配器（Deployment Adapters）**：将运行时输出桥接到模拟器（如ManiSkill、Isaac Sim）和真实机器人。
- **三大设计原则**：
  1. **模块化多速率执行**：将不同组件（感知编码器、骨干、预测分支、动作头）解耦，支持独立刷新策略，通过显式执行单元和共享状态池实现多速率调度。
  2. **延迟优先融合执行**：针对批量1场景优化，采用图重放、缓冲区复用、算子融合、后端特定调度、主机-设备数据移动优化，保证低延迟和低抖动，同时支持异构硬件（CPU/GPU/NPU）。
  3. **可扩展的算子与I/O支持**：提供类型化具身接口、可插拔头、一等公民的部署适配器，以及具身AI内核仓库（收集可复用算子和模型特定内核），使新模型家族可无缝集成。

### 关键技术细节（算法/流程，文字说明）
- 将整个推理组织为五个层的流水线，类似于一个插件化的解释器：输入经过适配器变为标准张量，序列构建器组装为模型输入，骨干执行完成核心计算，头插件根据模型类型输出动作或预测，最后部署适配器转换为机器人控制指令或模拟器状态。
- 多速率执行通过配置各个组件的**刷新策略**（如感知每5步更新一次，动作每步执行）实现，无需强制同步。
- 延迟优先的融合推理通过**图级别的算子融合**和**内存复用**，减少小批量推理中的核启动和内存拷贝开销。
- 后端抽象层允许同一套运行时代码无缝切换到不同硬件（CPU、不同GPU厂商、NPU等），通过注册式后端插件实现。

## 3. 实验设计

### 使用的数据集/场景
- **VLA模型评估**：
  - HY-VLA：在**RoboTwin place_empty_cup任务**上测试，使用相应checkpoint。
  - pi0.5：使用其对应的C++部署配置，未指定具体任务名称（可能是标准benchmark场景）。
- **WAM评估**：
  - 使用**LingBot-VA**模型的第一个Transformer块作为微基准。完整模型因边缘设备限制未进行闭环节点评估。

### Benchmark
- VLA：报告任务成功率（Success Rate）、动作块长度（Action Chunk）、服务器侧推理延迟（Inf. ms）、环境步耗时（Step ms）和峰值GPU内存（VRAM MiB）。
- WAM：对比**Python原始实现（BF16权重）**与**Embodied.cpp量化实现（Q4_K）**，测量每块延迟、内存占用、MAE和余弦相似度。

### 对比的方法
- VLA：仅报告Embodied.cpp的C++部署结果，未与其他运行时（如原始Python推理、vla.cpp等）直接对比。但论文声称保持高准确性，并展示了成功执行。
- WAM：对比Python原始PyTorch BF16基线 vs Embodied.cpp GGUF Q4_K量化块。

### 实验开放性
- 论文未进行与其他运行时（如ONNX Runtime、llama.cpp）在同一模型或设备上的公平对比。仅提供了自身运行结果。

## 4. 资源与算力

- **GPU型号和数量**：论文**未明确指定**实验中使用的具体GPU型号或数量。Table 3中列出了VRAM消耗（HY-VLA 6850 MiB，pi0.5 6546 MiB），表明使用了一定算力的GPU，但未说明是单卡还是多卡，具体如NVIDIA Jetson Orin、RTX 30系列等。
- **训练时长**：论文只关注**推理部署**，不涉及训练，无训练时长信息。
- 总体而言，资源信息不透明；作者提及在“constrained local edge setup”上运行，但未详细说明硬件参数。

## 5. 实验数量与充分性

- **实验组数**：
  - VLA：两个模型（HY-VLA、pi0.5）各1个任务，共2组主要实验。
  - WAM：1个微基准（LingBot-VA单块），共1组对比。
- **消融实验**：未进行。例如没有对比不同量化级别、不同多速率策略、不同后端等的影响。
- **充分性评估**：实验覆盖**非常有限**：
  - VLA仅测试了单一任务（HY-VLA）和未指定环境（pi0.5），缺乏在多个benchmark（如LIBERO、ManiSkill）上的系统评估。
  - WAM仅测试了单块Transformer，缺乏完整的闭环任务成功率。
  - 与现有运行时（vla.cpp、llama.cpp等）的横向对比缺失，无法体现Embodied.cpp的优越性。
- **客观性**：VLA成功报告中给出了置信区间（如[83.9, 100.0]），WAM量化误差指标明确，但对比基线仅Python原始，未与同样优化的C++运行时对比。

## 6. 论文的主要结论与发现

1. **VLA模型可成功部署**：HY-VLA在RoboTwin任务上取得100.0%成功率，pi0.5取得91.0%成功率，证明Embodied.cpp能在保持高准确性的同时实现闭环控制。
2. **WAM组件量化效果显著**：对LingBot-VA单块，使用Q4_K量化后，内存从312.2 MiB降至88.1 MiB（减少约72%），同时MAE低于3.3×10⁻²，余弦相似度高于0.9997，验证了运行时对WAM组件的支持能力。
3. **统一架构可行**：通过五层架构，可以在一个C++运行时内支持不同VLA和WAM家族，且保留了可扩展性。
4. **延迟和内存开销**：即使在较大骨干（Hunyuan-VL）上，闭环步延迟仍在一定范围内（HY-VLA 735.9 ms步延迟，pi0.5 56.85 ms），满足实时控制要求。

## 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - 提出统一的五层架构，将具身模型的共享执行路径抽象出来，使不同模型家族可以共用基础设施。
  - 明确区分了“多速率执行”和“延迟优先融合”，直击具身部署的核心痛点。
  - 提供可扩展的算子仓库和部署适配器，降低新模型集成成本。
- **实验设计亮点**：
  - 对WAM组件进行了量化精度与内存的定量分析，展示了量化带来的巨大收益。
  - VLA结果带有置信区间，增加了可信度。
  - 公开了项目仓库（GitHub），便于复现和扩展。

## 8. 不足与局限

- **实验覆盖严重不足**：
  - 仅两个VLA模型（且只各一个任务），缺乏在多种机器人任务（抓取、导航等）和数据集（如LEROBOT、CALVIN）上的评估。
  - 缺乏与其他推理运行时（vla.cpp、ONNX Runtime、llama.cpp）在同一模型和硬件上的直接性能对比，无法证明Embodied.cpp相比现有系统的优势。
  - WAM评估仅局限于单块量化，未实现完整模型的闭环部署，因此声称“支持WAM”尚需更强证据。
- **未提供多速率执行的实际效果数据**：没有实验展示多速率调度如何改善延迟或资源占用。
- **未分析Jitter、稳定性和实时性**：虽然强调延迟优先，但未提供延迟分布、抖动指标或控制频率的保持情况。
- **缺乏消融研究**：例如未测试不同量化级别、不同融合策略、不同后端对性能的影响。
- **硬件资源不透明**：缺少GPU型号和具体平台描述，影响实验复现和公平比较。
- **局限性声明**：论文自身承认“full LingBot-VA closed-loop results are not included because the complete model is not yet stable on the constrained local edge setup”，说明当前结果具有临时性。

（完）
