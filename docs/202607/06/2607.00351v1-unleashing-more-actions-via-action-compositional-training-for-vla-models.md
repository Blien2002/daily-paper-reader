---
title: Unleashing More Actions via Action Compositional Training for VLA Models
title_zh: 通过动作组合训练释放VLA模型更多动作能力
authors: "Kai Peng, Jie Lu, Xiaojiang Peng"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00351v1"
tags: ["query:vla"]
score: 9.0
evidence: 面向VLA模型的动作组合训练
tldr: VLA模型在机器人操作中表现优异，但标准训练易过拟合特定行为，无法泛化到子技能组合的新场景。本文提出ACT-VLA离线数据增强框架，利用模型潜在任务表示从现有任务自动合成新颖且物理可行的演示，无需额外人工收集。该方法有效扩展训练分布并缓解过拟合，在模拟任务中显著提升成功率。这表明利用现有任务自动合成演示是提升VLA泛化性的高效、可扩展途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.00351v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 486, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00351v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1871, \"height\": 1027, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.00351v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 841, \"height\": 528, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.00351v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1512, \"height\": 318, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.00351v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 928, \"height\": 244, \"label\": \"Table\"}]"
motivation: 标准VLA训练过拟合特定行为，难以泛化到子技能组合的新场景，而扩展高质量数据集成本高昂。
method: 提出ACT-VLA，利用模型潜在任务表示从现有任务自动合成新颖、物理可行的演示数据，扩充训练分布。
result: 模拟操作任务中，用合成数据训练的VLA相比基线大幅提高成功率。
conclusion: 证明利用现有任务自动合成演示是提升VLA泛化性的有效、可扩展且数据高效的方法。
---

## 摘要
视觉-语言-动作模型在机器人操作中表现出色，这得益于演示数据的规模和多样性。然而，标准训练范式常常导致VLA模型严重过拟合于特定行为模式，即使场景仅需相同子技能的崭新组合，模型也无法泛化到分布外场景。虽然扩展数据集可以缓解过拟合，但获取高质量机器人数据仍然众所周知地劳动密集且成本高昂。为了在不依赖昂贵人工遥操作的情况下解决这一困境，并真正释放更多动作——即让VLA模型能够将已知子技能组合成远超原始演示的可执行行为集合——我们提出了ACT-VLA（VLA模型的动作组合训练），一种离线数据增强框架，该框架利用模型潜在的任务表示，直接从现有任务中合成新颖且物理上有效的演示用于策略训练。通过消除额外的手动数据收集，我们的方法自动扩展训练分布并缓解过拟合。我们在仿真中具有挑战性的操作任务上评估了我们的方法。实验表明，尽管由于原始分布过拟合，基线VLA模型泛化能力较差，但使用我们合成数据训练的策略实现了显著更高的成功率，验证了利用现有任务进行自动演示合成为拓宽VLA泛化能力提供了一种有效、可扩展且数据高效的途径。

## Abstract
Vision-Language-Action models excel at robotic manipulation, driven by the scale and diversity of demonstration data. However, standard training paradigms often cause VLA models to severely overfit to specific behavioral patterns, rendering them unable to generalize to out-of-distribution scenarios even when those scenarios merely require novel combinations of identical sub-skills. While expanding datasets can mitigate this overfitting, acquiring high-quality robot data remains notoriously labor-intensive and cost-prohibitive. To resolve this impasse without expensive human teleoperation and to truly unleash more actions,i.e., enable VLA models to compose known sub-skills into a much broader set of executable behaviors beyond the original demonstrations-we propose ACT-VLA (Action Compositional Training for VLA Models), an offline data augmentation framework that leverages the model's latent task representations to synthesize novel, physically valid demonstrations directly from existing tasks for policy training. By eliminating additional manual data collection, our method automatically expands the training distribution and mitigates overfitting. We evaluate our approach on challenging manipulation tasks in simulation. Experiments demonstrate that while baseline VLA models generalize poorly due to original distribution overfitting, policies trained with our synthesized data achieve substantially higher success rates, validating that leveraging existing tasks for automated demonstration synthesis provides an effective, scalable, and data-efficient route to broadening VLA generalization.

---

## 论文详细总结（自动生成）

# 论文详细总结：Unleashing More Actions via Action Compositional Training for VLA Models

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：Vision-Language-Action (VLA) 模型在机器人操作中表现出色，但标准训练范式容易让模型严重过拟合于训练数据中固定的行为模式（如固定的物体-目标配对）。当遇到虽由相同子技能组成但组合方式新颖的分布外场景时，模型往往无法泛化。而获取高质量机器人演示数据成本极高（需人类遥操作），无法通过穷举所有组合来覆盖训练分布。
- **核心问题**：如何在不增加额外人工数据收集的前提下，让VLA模型学会将已知子技能组合成更多的可执行行为，即实现组合泛化（compositional generalization）。
- **整体含义**：本文提出了一种离线数据增强框架ACT-VLA，通过复用已有任务数据，自动合成新颖组合任务的演示，从而扩大训练分布、缓解过拟合，使模型在不改变架构、不增加推理开销的条件下具备组合泛化能力。

## 2. 方法论：核心思想、关键技术细节、算法流程

- **核心思想**：利用预训练VLA模型中编码的潜在组合结构，将文本潜在插值（TLI）从推理时的控制机制改造为训练时的自动演示生成引擎，为模型提供原本缺失的组合任务演示数据。
- **关键技术细节**：
  - **文本潜在插值（TLI）**：基于已有工作[12]，Transformer-based VLA 模型的文本token隐藏状态会编码任务语义。对两个基任务分别提取文本潜在向量T1和T2，在推理时通过插值控制行为过渡。公式为：  
    `hT(i) ← hT(i) + (1-α)T1 + αT2 - [(1-α)T2 + αT1]`  
    其中α = i/λ 线性从0到1变化，实现从任务1到任务2的平滑过渡。
  - **从推理干预到训练数据合成**：本文的关键贡献不是TLI本身，而是将其系统化为数据生成引擎，并解决了三个非平凡挑战：
    1. **指令设计**：推理时不需要语言指令，但训练时需要与合成轨迹语义一致的指令。通过拆分并重组基任务指令的文本组件来生成新指令。
    2. **质量控制**：引入双重过滤策略——任务完成检查（模拟器验证）和步数预算约束，仅保留成功完成全部组合任务的轨迹，避免错误状态-动作关联破坏策略。
    3. **训练动态**：保持原始数据与合成数据在套件级别的数据量均衡（每个OOD套件约300-400条轨迹），防止合成分布压过原始分布导致灾难性遗忘。
- **算法流程**（文字描述）：
  - **Stage 1**: 表示引导的轨迹合成。使用TLI机制，给定两个基任务A和B，让预训练VLA模型在模拟器中执行插值后的行为，生成从任务A到任务B的连续轨迹。
  - **Stage 2**: 数据记录与处理。对成功的合成轨迹，记录RGB观测、机器人本体状态、执行的动作，以及根据组合任务生成的文本指令，格式化为与原始演示相同的结构。
  - **Stage 3**: 增强策略训练。将合成数据集与原始演示混合，使用标准行为克隆损失和相同超参数训练VLA模型（如π0.5），无需修改架构。

## 3. 实验设计

- **数据集/场景**：使用 LIBERO 仿真基准测试套件，包含四个标准套件：Spatial、Object、Goal、Long（各10个任务）。还构造了两个分布外(OOD)套件：**LIBERO-Spatial-OOD** 和 **LIBERO-Goal-OOD**，每个由10个新颖的组合任务构成（如将物体A与目标位置B重新配对），这些组合在原训练集中不存在但子技能已覆盖。
- **基准对比方法**：
  - SOTA方法：UniVLA、OpenVLA-OFT、GR00T-N1、π0.5，均使用原始LIBERO演示训练。
  - 推理时组合方法：π0 + 文本潜在插值（TLI，推理时应用）。
  - 消融实验：以π0为骨干，对比仅原始数据、原始+合成数据、以及推理时TLI。
- **评估协议**：每个任务在3个随机种子下各评估50次（共150 episodes），报告各套件平均成功率（%）。所有模型训练30k步，设置相同。

## 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量、训练时长。仅提及所有模型在相同设置下训练30k步。未提及硬件细节。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验（表I）：在6个套件（4个标准 + 2个OOD）上比较了4种现有方法+本文方法（基于π0.5）。每个套件10个任务，任务级成功率取自150 episodes。
  - 消融实验（表II）：在π0骨干上对比了3种配置（仅原始、原始+合成、推理时TLI），覆盖Spatial和Goal两个套件的OOD性能。
  - 共包含至少18个任务×150 episodes = 2700次评估，覆盖多个随机种子。
- **充分性与公平性**：
  - 实验覆盖了**分布内和分布外**场景，OOD套件设计合理（重新配对物体-目标），能有效测试组合泛化。
  - 对比了多种主流VLA模型（UniVLA等）和推理时TLI变体，消融实验控制骨干一致。
  - 但**局限性**：仅验证了顺序两技能组合，且仅在仿真中评估；未在真实机器人上实验；未分析不同合成数据量、不同骨干模型性能边界。总体而言，实验设计针对论文核心主张（通过自动合成演示提升OOD泛化）提供了充分证据，但覆盖范围可进一步扩展。

## 6. 主要结论与发现

- 本文提出的ACT-VLA在OOD套件上带来**巨大提升**：相比训练在原始数据上的π0.5，Spatial-OOD从35.5%提升至88.2%（+52.7pp），Goal-OOD从46.6%提升至95.6%（+49.0pp）。
- 相比推理时TLI（π0+TLI，Spatial-OOD 81.0%，Goal-OOD 85.0%），训练时合成数据方法（85.9%，90.5%）表现更好，且消除了推理时的计算开销。
- 六套件**未加权平均成功率**达到95.6%，超过所有基线（最高为推理时TLI的90.4%），且分布内性能保持竞争力（几乎持平）。
- 消融实验证实：仅合成数据就能将π0的OOD成功率从2.0%提升至85.9%（Spatial-OOD）、从16.0%提升至90.5%（Goal-OOD），说明合成演示提供了有效的训练信号。
- **核心结论**：将组合轨迹合成从推理时干预转移到训练时数据增强，可让VLA模型内化组合转换能力，是提升组合泛化的有效、可扩展且数据高效的途径。

## 7. 优点

- **无需额外人工遥操作**：完全利用已有任务演示自动合成新组合数据，成本低。
- **无需修改模型架构**：兼容现有VLA训练流程（plug-and-play），仅需在训练前增加一步数据合成与过滤。
- **无需推理时干预**：内化组合能力，部署简单且无额外计算开销。
- **有效的质量控制**：双重过滤机制（任务完成+步数预算）确保合成数据质量，避免错误关联。
- **保持分布内性能**：通过数据量平衡，防止灾难性遗忘，在标准套件上性能不降反有竞争力。

## 8. 不足与局限

- **实验覆盖有限**：仅验证了顺序两技能组合（物体-目标重新配对），未涉及更长链组合或更多技能的交错组合。虽然理论上TLI可扩展，但递归组织与过滤会带来指数级挑战。
- **依赖预训练模型质量**：合成效果依赖VLA骨干的语义解耦能力，较弱的基座模型可能瓶颈性能。
- **仅在仿真中验证**：虽然双过滤策略缓解了分布偏移，但模拟到真实的迁移（sim-to-real）未经验证，真实环境的噪声、物理偏差可能影响合成数据有效性。
- **未分析合成数据量影响**：未探索不同数据量对性能的影响曲线，可能导致数据冗余或不足。
- **仅评估了单一骨干（π0/π0.5）**：虽然对比了多种基线，但自身方法仅基于π0系列，未在更强大模型（如Octo、GR00T-N1）上验证是否同样有效。
- **可能存在的偏差风险**：合成数据完全基于预训练模型的潜在表示，若模型本身存在偏差（如对某些物体-目标组合有偏好），合成数据可能放大该偏差。

（完）
