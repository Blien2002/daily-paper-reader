---
title: "τ: Learning Touch-Augmented Vision-Language-Action Models from Future Visual Supervision"
title_zh: τ：从未来视觉监督中学习触觉增强的视觉-语言-动作模型
authors: "Ning Cheng, Jinan Xu, Wanlin Li, Yangzhi Chen, Jing Gao, Yiqun Wang, Kelan Peng, Wenjuan Han"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24485v1"
tags: ["query:tf"]
score: 9.0
evidence: 从未来视觉监督学习触觉增强的视觉-语言-动作模型
tldr: 现有触觉增强的VLA模型面临数据有限和表示高维触觉信号的挑战。本文提出τ框架，利用未来视觉监督（JEPA）学习动作条件时空触觉表示，在低数据下与视觉语言特征融合以生成动作。引入四类接触丰富任务的同步视觉、本体、触觉数据集TacAura。实验证明τ优于现有模型，泛化到未见物体和场景，提升操作性能和鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24485v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1811, \"height\": 1004}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1837, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1820, \"height\": 1218}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 878, \"height\": 580}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 829, \"height\": 478}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24485v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1823, \"height\": 402}, {\"url\": \"assets/tables/arxiv/2607.24485v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1808, \"height\": 293}]"
motivation: 数据层面缺乏任务演示和预训练成本高，建模层面现有方法忽略高维触觉信号或仅关注瞬时状态。
method: 基于JEPA从未来视觉监督学习动作条件时空触觉表示，并与视觉语言特征融合用于动作生成。
result: 在四类接触丰富操作任务中，τ优于现有模型且泛化到未见物体和场景。
conclusion: τ有效提升触觉增强VLA模型的操作性能和鲁棒性，验证未来视觉监督的优越性。
---

## 摘要
学习有信息的触觉表示，同时有效地将其适应到预训练的视觉-语言-动作（VLA）模型，在数据和建模层面仍具有挑战性。在数据层面，有限的任务特定演示限制了表示质量，而大规模预训练则带来高昂成本。在建模层面，现有方法要么关注瞬时接触状态，要么使用6维力/扭矩序列对时间交互动态进行建模，而高维触觉信号尚未得到充分探索。为了解决这些挑战，我们提出了τ，一个触觉增强的VLA框架，它受联合嵌入预测架构（JEPA）启发，从未来视觉监督中学习动作条件的时空触觉表示，并将其与视觉-语言特征融合以在有限数据下进行动作生成。该监督在潜在空间中运行，仅在训练期间使用，不会增加部署开销。我们还引入了TacAura，一个包含四个代表性接触丰富操作任务的同步视觉、本体感受和基于视觉的触觉信号的数据集。实验表明，τ优于现有模型，并能泛化到未见过的物体和场景，提高了操作性能和鲁棒性。

## Abstract
Learning the informative tactile representation while effectively adapting it to pretrained Vision-Language-Action (VLA) models remains challenging at both the data and modeling levels. At the data level, limited task-specific demonstrations constrain representation quality, whereas large-scale pretraining incurs substantial costs. At the modeling level, existing methods either focus on instantaneous contact states or model temporal interaction dynamics using 6D wrench sequences, leaving high-dimensional tactile signals underexplored. To address these challenges, we present τ, a touch-augmented VLA framework that learns an action-conditioned spatiotemporal tactile representation from future visual supervision inspired by the Joint-Embedding Predictive Architecture (JEPA), and fuses it with vision-language features for action generation under limited data. This supervision operates in latent space and is used only during training, adding no deployment overhead. We also introduce TacAura, a dataset of synchronized vision, proprioception, and vision-based tactile signals across four representative contact-rich manipulation tasks. Experiments show that τ outperforms existing models and generalizes to unseen objects and scenes, delivering improved manipulation performance and robustness

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义

### 研究动机与背景
- **核心挑战**：将触觉感知有效集成到预训练的视觉-语言-动作（Vision-Language-Action, VLA）模型中面临双重困难：
  - **数据层面**：接触丰富的操作任务需要大量触觉-视觉-动作配对数据，但大规模多模态预训练成本极高，而小规模专用演示又限制表示质量。
  - **建模层面**：现有触觉增强方法多关注瞬时接触状态（如单帧触觉信号），或仅用6维力/扭矩序列建模时间动态，忽略了高维视觉触觉信号（如GelSight类传感器产生的空间变形模式）中的细粒度时空交互信息。
- **现有方法的不足**：ForceVLA、ForceFlow等使用力或低维触觉表示；T-Rex等依赖大规模预训练；其他方法虽考虑时间信息但未学习预测性的跨模态表示。
- **本文目标**：在有限数据和计算资源下，学习包含时间交互动态的触觉表示，并有效融合到VLA模型中，提升接触丰富任务的性能与泛化能力。

## 2. 方法论

### 核心思想
- **τ框架**：基于预训练VLA模型π0.5，新增**触觉编码与适配模块**将视觉触觉信号映射到VLA潜在空间，并引入**JEPA风格的预测式自监督分支**，通过预测未来视觉特征的变化来学习动作条件时空触觉表示。该分支仅在训练时使用，不增加推理开销。

### 关键技术细节
1. **触觉编码与适配模块**：
   - 触觉传感器（DM-Tac WS）输出左右指端的高分辨率图像（320×240，~40 FPS），由触觉编码器 \( E_{\text{tou}} \) 提取特征 \( z^{\text{touch}}_t \)。
   - 通过可学习的触觉适配器 \( A_{\text{tou}} \) 将特征投影到LLM嵌入空间，得到触觉token \( Z^{\text{touch}}_t \)。
   - 将视觉token、语言token与触觉token拼接为统一表示 \( Z_t = [Z^{\text{vision}}_t; Z^{\text{language}}_t; Z^{\text{touch}}_t] \)，输入VLA的LLM和动作专家预测动作块。

2. **JEPA风格预测式自监督分支**：
   - 输入：当前触觉token \( Z^{\text{touch}}_t \) 和动作token \( Z^{\text{action}}_{t:t+H} \)（通过动作编码器 \( E_{\text{act}} \) 将动作序列向量化得到）。
   - 预测器 \( P \)（轻量MLP，3层全连接+GELU）输出未来视觉潜表示 \( \hat{z}^{\text{vision}}_{t+\Delta_k} \)。
   - 预测目标：未来视觉观察的潜表示 \( z^{\text{vision}}_{t+\Delta_k} \)（由VLA视觉编码器提取，停止梯度）。
   - **损失函数设计**：采用加权余弦对齐损失，预测和目标的**特征变化量**（当前时刻的差值），并由触觉变化幅度加权（clip到 \([w_{\min}, w_{\max}]\)），避免异常值主导。
   - 最终训练目标：\( L_{\text{total}} = L_{\text{IL}} + \lambda L_{\text{SSL}} \)，其中 \( L_{\text{IL}} \) 是条件流匹配的模仿学习损失。

3. **训练策略**：
   - 端到端联合微调：同时优化策略模仿损失和自监督预测损失。预测分支仅在训练时启用，推理时移除。

## 3. 实验设计

### 数据集与场景
- **TacAura数据集**：本文自建，包含四个接触丰富操作任务：
  - 插头插入、USB插入、印章按压、白板擦除。
  - 每个任务100条高质量专家演示（共400条轨迹），由人类通过外骨骼遥操作采集。
  - 硬件：Franka Research 3机械臂 + Franka Hand夹爪 + DM-Tac WS视觉触觉传感器（夹爪两侧） + 三个RGB-D相机（两个固定第三视角，一个腕装）。
  - 多模态同步：时间戳对齐、重采样至10Hz控制频率，自然语言指令标注。

### 基准方法与对比
- **视觉-only VLA基线**：π0、π0.5（通用VLA模型）。
- **触觉增强策略**：ForceVLA†、ForceFlow†、T-Rex†（均改编为本触觉传感器设置，保留原框架）。
- **本文变体**：τ-Wrist Sup.（腕部视图未来视觉监督）、τ-Front Sup.（前视图监督）、τ-DualView Sup.（双视图监督）。

### 评测指标
- 任务分阶段成功率：每个任务分为3个阶段（例如插入：抓取、对齐、插入），报告各阶段成功率及全程成功率。每个模型在随机化条件下进行20次试验。

## 4. 资源与算力
- **训练**：2块NVIDIA A800 80GB GPU，训练30,000 steps，batch size = 32，余弦学习率调度（10,000 warmup steps，峰值5e-5）。
- **推理**：单块NVIDIA RTX 4090 GPU。
- 论文未明确说明训练总时长，但基于上述配置可推断为较经济的微调。

## 5. 实验数量与充分性
- **主要实验**：在4个任务上对比3个基线 + 3个本文变体，每个20次试验，共约20×7×4 = 560次物理试验。报告分阶段指标。
- **消融实验**：分别移除动作序列条件、预测自监督、触觉模块，共3组消融，同样在4任务上各20试次。
- **泛化实验**：在USB插入和白板擦除任务上测试：
  - 物体泛化：2个未见物体实例；
  - 场景泛化：2个干扰物集合（引入额外物体和视觉杂乱）。
  - 每个设置20次试验。
- **充分性评估**：实验设计较全面，覆盖多种任务类型（插入型、按压型、擦拭型），对比了多个有意义基线，消融和泛化实验系统地验证了各组件贡献。但任务数量仅为4，且均为桌面级任务，泛化测试仅限于两个任务。总体客观公平，随机化条件下重复次数合理。

## 6. 主要结论与发现
- τ框架在所有任务上大幅超越基线：最佳变体τ-Wrist Sup.平均全程成功率71.25%，比最强基线ForceVLA†的30.00%高出41.25个百分点。
- 触觉模块是最关键组件（移除后平均降至28.75%），其次是预测自监督（降至51.25%），动作序列条件贡献较小（降至58.75%）。
- 最优监督视图具任务依赖性：插入类任务前视图更优（提供空间线索），接触类任务腕视图更优（提供局部精细信息）。双视图融合并非总是更好。
- 泛化：白板擦除对物体和场景变化都很鲁棒（成功率>90%），而USB插入受物体几何和视觉杂乱影响较大（下降至20-35%），展示了任务依赖性。

## 7. 优点
- **方法创新**：首次将JEPA风格预测自监督引入触觉增强VLA策略学习，通过预测未来视觉变化而非重建像素，在有限数据下学习动力学感知的触觉表示，且不增加推理成本。
- **工程实践**：设计了轻量级数据基础设施，将触觉数据作为“即插即用”模态融入现有VLA数据格式，便于复用和扩展。
- **实验严谨性**：分阶段评估揭示各方法在不同执行阶段的瓶颈；消融实验清晰归因各组件贡献；泛化实验覆盖物体和场景两个维度。
- **开源承诺**：数据集、代码、工具将全部开源，可复现性强。

## 8. 不足与局限
- **任务范围有限**：仅涵盖4个桌面级接触操作任务，未涉及更复杂的操作（如精细手内操控、多物体交互、长时程任务），任务多样性不足。
- **泛化测试不全面**：仅对两个任务进行了泛化测试，且未见物体和场景变化程度有限。未测试跨任务、跨机器人平台的泛化。
- **数据规模小**：每个任务仅100条演示，虽验证了小数据有效性，但可能不足以支持更复杂的策略学习。更大规模数据下的性能提升潜力未被探索。
- **触觉传感器依赖**：仅使用DM-Tac WS一种视觉触觉传感器，所学习的表示对传感器类型可能有特异性。不同触觉传感器的适配性未讨论。
- **计算资源报告不完整**：未给出训练具体耗时，仅说明GPU配置，影响可复现性。
- **消融实验的非加和性**：未深入分析各组件间的交互作用（如是否存在冗余或协同），各移除实验仅反映独立影响。

（完）
