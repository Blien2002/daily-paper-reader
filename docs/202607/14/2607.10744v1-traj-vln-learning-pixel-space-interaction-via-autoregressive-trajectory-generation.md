---
title: "Traj-VLN: Learning Pixel-Space Interaction via Autoregressive Trajectory Generation"
title_zh: "Traj-VLN: 通过自回归轨迹生成学习像素空间交互"
authors: "Changfei Fu, Guangcheng Chen, Wenjun Xu, Hong Zhang"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10744v1"
tags: ["query:vla"]
score: 9.0
evidence: 利用大语言模型和视觉语言模型进行自回归轨迹生成用于视觉语言导航
tldr: 连续环境中视觉语言导航（VLN-CE）面临跨场景泛化挑战，现有方法通过深度信息将视觉嵌入映射到语言空间，但大语言模型主要在RGB图像上预训练，缺乏深度感知能力。本文提出Traj-VLN，将导航任务分解为子任务，通过自回归方式在当前观测图像上生成像素坐标轨迹，直接在2D像素空间学习导航交互。实验证明像素空间轨迹监督优于离散动作学习和像素目标方法，显著提升导航成功率。该工作以有限计算资源实现与最先进方法相当的性能，为VLN提供了无需深度先验的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10744v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 729, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1817, \"height\": 632, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 922, \"height\": 477, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10744v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 920, \"height\": 505, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10744v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 893, \"height\": 633, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10744v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1845, \"height\": 550, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10744v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1845, \"height\": 385, \"label\": \"Table\"}]"
motivation: 现有VLN方法依赖深度信息，但VLMs在RGB图像上训练，难以理解空间交互；需探索无需深度信息的替代方案。
method: 通过自回归生成当前观测图像底部中心开始的一系列像素坐标轨迹，在2D像素空间学习导航交互。
result: 像素空间轨迹监督显著优于离散动作和像素目标方法，在R2R-CE数据集上以有限资源达到SOTA水平。
conclusion: Traj-VLN证明了像素空间轨迹生成是无需深度信息的有效导航范式，拓展了VLMs在具身智能中的应用。
---

## 摘要
受益于大规模预训练数据中嵌入的强大先验知识以及新兴的常识推理能力，大型语言模型（LLMs）在许多研究领域展现出了前所未有的泛化能力。最近，通过视觉-语言模型（VLMs）将视觉嵌入投影到语言空间，以实现从仿真到真实以及跨场景的泛化，已成为连续环境下的视觉与语言导航（VLN-CE）领域的主流范式。VLN要求具身智能体在遵循自然语言指令的情况下，在未知环境中进行导航。我们强调，一个VLN任务可以分解为一系列子任务，每个子任务对应一个与环境的3D空间交互过程，例如指令“走到沙发尽头然后左转”。然而，这种涉及沿着深度感知方向进入图像的空间交互对于VLMs来说是令人困惑的，因为它们主要基于RGB图像的对话进行训练。我们提出了一种替代方法，而不是引入VLMs在预训练中很少遇到的深度或3D几何信息：通过自回归轨迹生成，直接微调VLMs以在2D像素空间中学习导航交互。给定语言指令和历史观测，我们的模型依次预测一系列像素坐标，从当前观测的底部中心绘制一条轨迹。虽然先前的工作已经证明像素目标监督优于离散动作学习，但我们的实验进一步验证了像素空间轨迹的监督显著提升了VLN性能。此外，我们证明了我们的旗舰模型在相对有限的计算资源和训练数据下达到了最先进的性能水平。

## Abstract
Benefiting from the powerful priors embedded in large-scale pre-training data and the emerging commonsense reasoning ability, large language models (LLMs) have shown unprecedented generalization capabilities in many research fields. Recently, projecting visual embeddings into the language space via vision-language models (VLMs) to achieve sim-toreal and cross-scene generalization has become a prevailing paradigm in the field of Vision-and-Language Navigation in Continuous Environments (VLN-CE). VLN requires an embodied agent to navigate through unseen environments following natural linguistic instructions. We emphasize that a VLN task can be decomposed into a sequence of sub-tasks, each corresponding to a process of 3D spatial interaction with the environments described by instructions such as "walk to the end of the sofa and turn left." However, such spatial interactions involving moving into the image along the direction of depth sensing are puzzling for VLMs as they were predominantly trained on conversations with RGB images. Rather than incorporating depth or 3D geometric information-which VLMs rarely encounter during pretrainingwe propose an alternative approach: fine-tuning VLMs to learn navigation interactions directly in 2D pixel space through autoregressive trajectory generation. Given a linguistic instruction and historical observations, our model sequentially predicts a series of pixel coordinates, drawing a trajectory from the bottom center of the current observation. While prior work has proved that pixel-goal supervision outperforms learning of discrete actions, our experiments further verify that the supervision of pixel-space trajectory significantly enhances VLN performance. Moreover, we demonstrate that our flagship model achieves state-of-the-art level performance with relatively limited computational resources and training data.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：连续环境下的视觉与语言导航（VLN-CE）要求智能体遵循自然语言指令在未知环境中导航，现有主流方法通过视觉-语言模型（VLM）将视觉信息投影到语言空间。然而，VLM主要在RGB图像对话数据上预训练，缺乏对深度和3D几何的感知能力，导致难以理解“沿着深度方向移动”的空间交互过程。引入深度或3D信息又面临与VLM预训练分布不匹配的学习困难。
- **整体含义**：论文旨在探索一种无需显式3D信息的替代范式，让VLM通过在熟悉的2D像素空间中学习导航交互，从而避免3D感知瓶颈，提升VLN性能和跨场景泛化能力。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将VLN任务分解为子任务，每个子任务对应一个3D空间交互过程。通过微调VLM，使其在当前观测图像上自回归地生成一条从底部中心开始的像素坐标轨迹，从而在2D像素空间内直接学习导航交互，避免使用深度或3D几何信息。
- **关键技术细节**：
  - **模型框架**：基于Qwen3-VL-8B-Instruct，冻结视觉编码器，微调LLM和投影层。输入包括语言指令、当前观测（640×480）以及8张均匀采样的历史观测（缩放至384×384）。
  - **两阶段对话**：
    - 第一回合：VLM输出“←”、“→”（旋转15度）或“↓”（进入第二阶段）或“STOP”。
    - 第二回合：若输出“↓”，智能体低头30度获取俯瞰图，VLM基于该图自回归生成像素坐标序列 `[u1, v1][u2, v2]...[uk, vk]`，描绘从底部中心开始的轨迹。
  - **轨迹执行**：利用对应深度图将像素坐标转换为3D点，通过相机内参和机器人基座外参变换到机器人坐标系，生成3D轨迹，然后控制机器人依次移动至各点。
  - **数据收集**：从R2R-CE、RxR-CE和ScaleVLN数据集中以4步间隔采样，将未来姿态投影到当前俯瞰图，并筛选过滤被遮挡和重复的点，得到1.75M训练样本。每个样本包含指令、历史观测、当前观测以及监督信号（像素坐标序列、转向动作或“STOP”）。
- **公式与算法流程**（文字说明）：
  - 基座姿态投影：未来位姿相对于当前位姿，通过相机内参矩阵K和当前位姿逆矩阵投影到图像平面，得到像素坐标。
  - 过滤：基于深度图剔除被遮挡或不可见的像素点。
  - 训练损失：使用标准交叉熵损失，让VLM预测文本格式的像素坐标序列（或转向动作）。

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集与Benchmark**：
  - 训练数据：R2R-CE（约10.7K轨迹）、RxR-CE（约19.5K轨迹）、ScaleVLN子集（约77.3K轨迹），共1.75M训练样本。
  - 评估：R2R-CE Val-Unseen（1839条轨迹）和RxR-CE Val-Unseen（3669条轨迹）。
  - 指标：导航误差（NE）、Oracle成功率（OS）、成功率（SR）、加权路径长度（SPL）、归一化动态时间规整（nDTW）。
- **对比方法**：
  - 传统非VLM方法：Sim2Real, AO-Planner, NavMorph。
  - VLM微调方法：StreamVLN, NaVid, NaVid-4D, NaVILA, JanusVLN, InternVLA-N1, Goal2Pixel。
  - 消融对比：在同一设置下（Qwen2.5-VL-7B, LoRA rank=32, 冻结视觉编码器）训练像素目标监督模型（InternVLA-N1-S2）与Traj-VLN。

## 4. 资源与算力

- **旗舰模型（Traj-VLN based Qwen3-VL-8B）**：
  - 硬件：8块 Ascend 910C NPU。
  - 训练时长：约91小时，总计728 NPU小时。
  - 训练步数：16998步，batch size=128，学习率2e-5，1个epoch。
  - 冻结视觉编码器节省约62.5%训练时间。
- **消融实验（Qwen2.5-VL-7B, LoRA）**：
  - 硬件：4块 Nvidia 4090 GPU（各48GB显存）。
  - 训练时长：约171小时，共274266步（1个epoch）。
- **数据规模**：1.75M训练样本（相比InternVLA-N1的4.72M更少）。

## 5. 实验数量与充分性

- **实验组数**：主要包括三大组：
  1. **SOTA对比**（表I）：在R2R-CE和RxR-CE上对比8个VLM方法+3个非VLM方法，共11个基线。
  2. **轨迹监督vs像素目标监督**（表II及图4）：在同一骨干下严格控制变量（仅监督信号不同），对比两种评估方式（最终像素评估、轨迹评估）。
  3. **执行步数消融**（图4）：变化每次轨迹预测后执行像素点数，观察SR与OS变化。
- **充分性与公平性**：
  - 消融实验控制了骨干网络、数据、训练设置，仅改变监督信号，说明轨迹监督优于像素目标监督。
  - SOTA对比中，Traj-VLN使用更少训练样本（1.75M vs 4.72M）达到更高SR，凸显方法高效性。
  - 但评估仅在两个标准数据集上进行，未包括更复杂的真实场景或动态环境，泛化性验证有限。

## 6. 论文的主要结论与发现

- **主要结论**：
  - Traj-VLN在R2R-CE Val-Unseen上SR达58.4%，在RxR-CE上达52.6%，均超越使用更多训练数据的SOTA方法（如InternVLA-N1的55.4%/49.5%）。
  - 像素空间轨迹监督显著优于像素目标监督：在相同设置下，Traj-VLN的SR高约4%（R2R）和3.29%（RxR）。
  - 自回归轨迹生成可作为链式思维（CoT）机制：将最终像素作为目标评估时，Traj-VLN仍优于直接像素目标预测，表明学习过程有助于更准确的像素目标推理。
  - 轨迹生成方法避免了显式3D信息的引入，更契合VLM预训练分布，从而提升学习效果。

## 7. 优点：方法或实验设计上的亮点

- **方法创新性**：首次提出在VLN中用自回归方式生成像素轨迹作为VLM的监督信号，将空间交互过程显式地编码为序列预测问题，既利用了VLM的语言能力，又避开了3D感知短板。
- **高效性**：使用较少训练样本（1.75M）和冻结视觉编码器（节省62.5%训练时间）即可达到SOTA水平，计算资源友好。
- **解释性**：轨迹生成可视为一种CoT机制，提升了预测的推理可解释性；实验通过“最终像素作为目标”的消融验证了这一假说。
- **实验严谨性**：严格控制变量对比轨迹监督与像素目标监督，排除了其他因素干扰。

## 8. 不足与局限

- **实验覆盖有限**：仅在R2R-CE和RxR-CE两个仿真数据集上评估，未在真实环境或更复杂场景（如动态障碍物、多楼层）中验证泛化能力。
- **数据偏差风险**：训练数据全部来自仿真，轨迹投影依赖于精确的深度和位姿，真实环境中噪声可能导致性能下降。
- **依赖俯瞰图像**：方法需要智能体低头获取俯瞰图，这在实际部署中可能受限于机械结构或传感器布局。
- **对比方法选择**：SOTA对比中部分方法使用了不同的骨干网络或训练数据量，虽已尽量对齐，但并非全部在同一基线水平下比较（如NaVILA使用VILA-7B且数据量更大）。
- **消融实验范围**：未与其他潜在的监督信号（如多步动作序列、语言规划）进行对比，仅聚焦于像素目标。

（完）
