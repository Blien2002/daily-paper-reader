---
title: "τ: Learning Touch-Augmented Vision-Language-Action Models from Future Visual Supervision"
title_zh: "τ: 从未来视觉监督中学习触觉增强的视觉-语言-动作模型"
authors: "Ning Cheng, Jinan Xu, Wanlin Li, Yangzhi Chen, Jing Gao, Yiqun Wang, Kelan Peng, Wenjuan Han"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24485v1"
tags: ["query:tf"]
score: 9.0
evidence: 未来视觉监督的触觉增强VLA
tldr: 触觉与Vision-Language-Action模型的融合面临数据稀缺和建模困难。τ框架利用未来视觉监督（JEPA）学习动作条件时空触觉表示，在有限数据下与视觉语言特征融合生成动作。在接触丰富任务上，τ超越现有模型并泛化到未见场景。提供TacAura数据集，显著提升操作性能与鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24485v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1811, \"height\": 1004}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1837, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1820, \"height\": 1218}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 878, \"height\": 580}, {\"url\": \"assets/figures/arxiv/2607.24485v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 829, \"height\": 478}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24485v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1823, \"height\": 402}, {\"url\": \"assets/tables/arxiv/2607.24485v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1808, \"height\": 293}]"
motivation: 现有方法难以在有限演示下有效学习触觉表示并融入预训练VLA模型，且高维触觉信号利用不足。
method: 提出τ框架，基于JEPA从未来视觉监督学习动作条件时空触觉表示，再与视觉语言特征融合进行动作生成，仅训练时使用。
result: 在四个接触丰富操作任务上，τ超越基线，泛化到未见物体和场景，提升操作成功率和鲁棒性。
conclusion: τ有效增强VLA模型的触觉感知，为有限数据下的接触丰富操作提供鲁棒解决方案。
---

## 摘要
学习信息丰富的触觉表示并将其有效适配到预训练的视觉-语言-动作（VLA）模型，在数据和建模层面仍然具有挑战性。在数据层面，有限的任务特定演示限制了表示质量，而大规模预训练则带来高昂成本。在建模层面，现有方法要么关注瞬时接触状态，要么使用6维力/力矩序列建模时间交互动态，忽略了高维触觉信号。为解决这些挑战，我们提出τ，一种触觉增强的VLA框架，它受联合嵌入预测架构（JEPA）启发，从未来视觉监督中学习动作条件化的时空触觉表示，并将其与视觉-语言特征融合以在有限数据下生成动作。这种监督在潜在空间中运行，仅在训练期间使用，不增加部署开销。我们还引入了TacAura，一个包含同步视觉、本体感觉和基于视觉的触觉信号的数据集，涵盖四个代表性的接触丰富操作任务。实验表明，τ优于现有模型，并能泛化到未见过的物体和场景，提供改进的操作性能和鲁棒性。

## Abstract
Learning the informative tactile representation while effectively adapting it to pretrained Vision-Language-Action (VLA) models remains challenging at both the data and modeling levels. At the data level, limited task-specific demonstrations constrain representation quality, whereas large-scale pretraining incurs substantial costs. At the modeling level, existing methods either focus on instantaneous contact states or model temporal interaction dynamics using 6D wrench sequences, leaving high-dimensional tactile signals underexplored. To address these challenges, we present τ, a touch-augmented VLA framework that learns an action-conditioned spatiotemporal tactile representation from future visual supervision inspired by the Joint-Embedding Predictive Architecture (JEPA), and fuses it with vision-language features for action generation under limited data. This supervision operates in latent space and is used only during training, adding no deployment overhead. We also introduce TacAura, a dataset of synchronized vision, proprioception, and vision-based tactile signals across four representative contact-rich manipulation tasks. Experiments show that τ outperforms existing models and generalizes to unseen objects and scenes, delivering improved manipulation performance and robustness

---

## 论文详细总结（自动生成）

当然，以下是对论文《τ: Learning Touch-Augmented Vision-Language-Action Models from Future Visual Supervision》的详细中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：视觉-语言-动作（VLA）模型通过大规模多模态预训练，在通用机器人操作中取得了显著的成功。然而，它们主要依赖视觉作为主要输入模态，在处理接触丰富的操作任务（如插拔、按压）时存在根本性局限。触觉传感可以捕捉到接触状态、力分布和形变等视觉难以推断的物理交互动态信息。
- **核心问题**：尽管触觉传感很有前景，但有效地学习信息丰富的触觉表示，并将其融入预训练的VLA模型以生成动作，面临两个关键挑战：
    - **数据层面**：大规模预训练触觉表示计算成本高昂，而针对特定任务的有限演示数据又不足以学习高质量的表示。
    - **建模层面**：现有方法要么只关注瞬时接触状态，要么使用低维的6D力/力矩序列建模时间交互，忽略了高维、细粒度的视觉触觉信号（如GelSight图像）的时间动态。
- **整体含义**：τ旨在解决在有限数据和计算资源下，有效学习动作条件化的时空触觉表示，并将其与预训练的VLA模型无缝集成，从而提升机器人在接触丰富任务中的性能和泛化能力。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出一种名为 **τ** 的触觉增强VLA框架。它基于**联合嵌入预测架构（JEPA）** 的思想，利用**未来视觉监督**来学习**动作条件化的时空触觉表示**。这种监督只在训练阶段使用，不增加推理时的计算开销。

- **关键技术细节**：
    - **模型架构（τ Framework）**：
        1.  **基础VLA骨干**：基于预训练的VLA模型（如π0.5），负责多模态感知和动作生成。
        2.  **触觉编码与适配模块**：
            -  **触觉编码器（`E_tou`）**：将原始的视觉触觉信号（如来自左右手指的GelSight图像）编码为触觉特征 `z_touch`。
            -  **触觉适配器（`A_tou`）**：通过一个可学习的线性层，将触觉特征 `z_touch` 投影到与VLA骨干相同的语言-视觉嵌入空间，生成 `Z_touch` 令牌。
        3.  **JEPA式预测自监督分支（训练阶段专用）**：
            - **动作条件预测**：当前触觉令牌 `Z_touch` 与编码后的未来动作序列 `Z_action` 被送入一个轻量级的MLP预测器（`P(·)`），预测未来视觉观测的潜在表示变化 `∆ˆz_vision`。
            - **预测目标**：目标值是通过VLA骨干的视觉编码器 `E_vis` 计算真实未来视觉观测的潜在表示变化 `∆z_vision`（当前与未来的差值）。
            - **语义相似性损失**：通过一个**基于触觉变化幅度的加权余弦对齐损失** `L_SSL`，最小化预测变化与真实变化之间的差异。权重 `w_{t+Δk}` 强调了触觉变化显著的未来时刻，使模型关注关键的接触转变。

- **算法流程（文字说明）**：
    1.  在时间步t，输入多视图图像、语言指令和触觉图像。
    2.  **正向传播**：
        -   VLA模型处理视觉和语言信息，生成视觉令牌和语言令牌。
        -   触觉编码和适配模块处理触觉图像，生成适配的触觉令牌。
        -   所有令牌拼接后送入大语言模型和动作专家，生成动作块预测 `ˆa_{t:t+H}`。
        -   【训练时】JEPA分支将触觉令牌和动作令牌送入预测器，输出预测的未来视觉变化。
    3.  **损失函数**：
        -   **模仿学习损失 `L_IL`**：采用条件流匹配，让动作专家学习去噪向量场，以匹配专家演示的动作轨迹。
        -   **自监督学习损失 `L_SSL`**：加权余弦对齐损失，使预测的未来视觉变化与真实未来视觉变化对齐。
    4.  **联合训练**：总损失 `L_total = L_IL + λ * L_SSL`，其中λ平衡两者贡献。
    5.  **推理阶段**：移除JEPA分支，仅使用训练好的触觉增强VLA模型进行动作生成，无额外开销。

### 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：作者自主创建了**TacAura数据集**，这是一个同步的视觉-本体感觉-触觉数据集。它包含**四个接触丰富任务**：插头插入（Plug Insertion）、USB插入（USB Insertion）、印章按压（Stamp Press）和白板擦除（Whiteboard Erasing）。每个任务收集了**100条高质量人类演示**，共400条轨迹。
- **Benchmark**：在真实的机器人平台（Franka Research 3 + 改装Dex-Net手爪 + DM-Tac WS触觉传感器）上进行的真实世界评估。
- **对比方法**：
    - **视觉基础VLA**：π0, π0.5。
    - **触觉感知VLA**：ForceVLA（力表示）、ForceFlow（力-流匹配）、T-Rex（触觉反应灵巧操控）。所有对比模型都适配了相同的触觉编码器，以确保公平比较。

### 4. 资源与算力

- **训练硬件**：2块 **NVIDIA A800 80GB GPU**。
- **训练配置**：每个任务使用100个演示数据，批量大小32，动作视界32，学习率5e-5，训练**30,000步**（其中10,000步为Warmup）。
- **推理硬件**：单个 **NVIDIA RTX 4090 GPU**。

### 5. 实验数量与充分性

- **实验数量**：
    - **主要结果**：在四个任务上，对比了六种模型，每个模型测试20次，总共产生 **4任务 * 6模型 * 20次 = 480次** 实验。
    - **泛化分析**：在USB插入和白板擦除任务上，测试了**2个未见物体**和**2种未见场景（干扰物）** 设置，每个设置20次，产生了额外的80次实验。
    - **消融实验**：对τ模型进行了**三个关键组件**的消融研究（动作序列、自监督学习、触觉模块），每个任务测试20次，产生 **4任务 * 3组件 * 20次 = 240次** 实验。
- **充分性评估**：
    - **客观与公平**：实验在真实机器人上完成，触觉基线模型（ForceVLA, ForceFlow, T-Rex）被适配到相同的触觉输入，确保了比较的公平性。采用阶段式成功率和完整任务成功率为指标，结果清晰。
    - **充分性**：实验覆盖了多种任务、多层基线、详细的消融和泛化测试，设计较为全面。但仅测试了20次，统计显著性可能存在一定局限，但考虑到真实机器人实验的成本，这已是一个比较充分的评估。

### 6. 论文的主要结论与发现

- **核心结论**：τ框架显著优于所有视觉-only和触觉增强的基线模型，在四个接触丰富任务上，平均完整任务成功率从基线的20-30%提升至**71.25%**。
- **关键发现**：
    - **触觉模块至关重要**：消融实验表明，移除触觉模块导致性能下降最严重（-42.5%），证明触觉信息是接触任务性能提升的核心。
    - **JEPA式自监督学习有效**：移除自监督学习损失使性能下降20%，表明学习动作条件化的、能预测未来的触觉表示是有效的。
    - **动作序列条件化贡献**：移除动作条件导致性能下降12.5%，强调了时间依赖性。
    - **泛化能力**：
        - **白板擦除**任务对**未见物体和场景**展现出强大的零样本泛化能力（成功率从95%降至90%）。
        - **USB插入**任务泛化性较弱，对目标几何变化和场景干扰物较为敏感，提示未来需加强视觉定位和几何适应性。
        - **最佳监督视角依赖任务**：插入任务受益于**前视图**监督，而压制和擦拭任务更依赖**腕部视图**监督。双视图融合并未带来提升，反而可能引入噪声。

### 7. 优点：方法或实验设计上的亮点

- **方法创新性**：
    - **JEPA与VLA的巧妙结合**：将JEPA的预测架构引入VLA，通过预测未来视觉变化作为监督信号，学习具有动态感知能力的触觉表示，是一种新颖且高效的多模态融合方法。
    - **零推理开销**：自监督分支仅在训练阶段使用，推理时移除，不增加部署复杂度和计算负担，非常实用。
- **实验设计的亮点**：
    - **全面的消融研究**：系统地揭示了触觉模块、自监督学习、动作条件分别对性能的贡献，验证了各组件的必要性。
    - **有意义的泛化分析**：区分了物体泛化和场景泛化，并揭示了任务特性对泛化能力的影响，提供了更深入的见解。
    - **公开数据集**：TacAura数据集的建立和开源承诺，为触觉VLA领域的后续研究提供了宝贵资源。
    - **公平的基线比较**：对现有触觉基线模型进行了适配，确保了比较的公平性。

### 8. 不足与局限

- **数据规模有限**：每个任务仅100条演示数据，虽然符合“有限数据”的设置，但对于更复杂、变化更多的任务可能不够。未来需要更大规模、更多样化的数据集。
- **任务范围有限**：仅在四个任务上进行了验证，都是短时程、单一阶段的操作。对于**长时程、多阶段**的复杂操作任务（如组装、整理）的适用性未探索。
- **触觉传感器类型**：仅使用了基于视觉的触觉传感器（DM-Tac WS），其输出是图像。对于其他类型（如压阻、电容）的触觉传感器，方法的通用性尚未验证。
- **泛化分析深度**：泛化实验只测试了少量未见物体和干扰场景，对光照变化、视角变化、物体材质变化等更广泛的泛化能力表征不足。
- **未进行大规模多任务训练**：论文探讨的是在有限资源下的微调，而未尝试在多个任务上联合训练的“通用”触觉VLA基础模型，后者可能展现更强的泛化能力。
- **实验统计显著性**：论文报告了20次试验的成功率，但没有提供置信区间或进行统计显著性检验，结论的可靠性可以进一步加强。

（完）
