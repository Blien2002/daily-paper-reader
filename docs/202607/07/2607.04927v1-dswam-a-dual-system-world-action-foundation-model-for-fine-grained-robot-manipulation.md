---
title: "DSWAM: A Dual-System World Action Foundation Model for Fine-Grained Robot Manipulation"
title_zh: DSWAM：面向细粒度机器人操作的双系统世界动作基础模型
authors: "Jian Zhu, Jianjun Zhang, Taiyi Su, Tianbin Liu, Zhangyuan Wang, Kai Xie, Zitai Huang, Chong Ma, Youzhang He, Tianjian Wang, Hanyang Wang, Weihao Ding, Yi Xu"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04927v1"
tags: ["query:vla"]
score: 10.0
evidence: 提出双系统世界动作模型，比较并融合VLA和WAM实现精细操作
tldr: 世界动作模型（WAM）虽擅长物理执行，但缺乏语言级任务分解能力，而VLA策略则相反。为同时具备两者优势，提出DSWAM双系统框架：System 1 WAM执行器默认控制，System 2视觉语言子任务规划器按需激活。执行器通过动作预测与视频联合训练，推理时直接输出动作块，并集成TensorRT加速、异步执行和实时分块技术，确保实机部署。在DeMaVLA变形操作基准上公平对比VLA策略，DSWAM以相同数据与条件实现更优性能，弥合了分解鸿沟并提供了可靠的WAM-VLA比较。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04927v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1712, \"height\": 887, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04927v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1583, \"height\": 2126, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1656, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1209, \"height\": 275, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1215, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1290, \"height\": 280, \"label\": \"Table\"}]"
motivation: 现有WAM缺乏语言级任务分解，而VLA策略缺少物理执行精度，且两者缺乏公平实机比较。
method: DSWAM包含System 1 WAM执行器（默认路径）和System 2视觉语言子任务规划器（按需激活），执行器采用动作预测与视频联合训练，推理时直接输出动作块并引入TensorRT加速。
result: 在DeMaVLA设置下，DSWAM与VLA策略公平对比，验证了其精细操作性能更优，且执行效率满足实机要求。
conclusion: DSWAM作为双系统世界动作基础模型，统一了物理执行与语言分解能力，为WAM-VLA对比提供基准。
---

## 摘要
世界动作模型（WAM）通过将基于视频的世界模型作为机器人动作学习的密集监督信号，为视觉-语言-动作（VLA）策略提供了一种有前景的替代方案。现有的WAM擅长物理基础执行，但通常缺乏基于VLM的VLA中用于分解粗略指令的显式语言级规划接口。当家庭任务涉及复杂的多步骤目标时，这种分解变得至关重要，因为粗略的用户命令需要转换为一系列细粒度的可执行子任务。同时，该领域仍然缺乏VLA与WAM执行能力之间的公平真实机器人对比，因为现有系统在数据、机器人实体和任务协议上往往不同。为了解决分解差距以及对受控WAM-VLA对比的需求，我们引入了DSWAM，一种面向细粒度机器人操作的双系统世界动作基础模型。DSWAM将系统1的WAM执行器作为默认控制路径，并仅在任务分解有用时选择性激活系统2的视觉-语言子任务规划器。规划器从短期视觉历史和全局任务提示中预测可执行的子任务，而WAM执行器对每个指令或子任务执行具有世界感知的动作生成。执行器通过动作预测和视频协同训练进行训练，但推理时直接预测动作片段，无需显式的未来视频生成。为了使该执行路径在真实机器人上实用，我们进一步集成了TensorRT加速、异步执行和实时分块（RTC），从而策略查询不会阻塞机器人控制。为了与VLA策略进行公平的真实机器人对比，我们在DeMaVLA真实世界可变形操作设置下构建和评估DSWAM，匹配机器人平台、预训练数据、后训练数据和评估标准。

## Abstract
World Action Models (WAMs) provide a promising alternative to Vision-Language-Action (VLA) policies by using video-based world modeling as dense supervision for robot action learning. Existing WAMs excel at physically grounded execution, but typically lack the explicit language-level planning interface in VLM-based VLAs for decomposing coarse instructions. Such decomposition becomes important when household tasks involve complex multi-step goals, where coarse user commands need to be converted into sequences of fine-grained executable subtasks. Meanwhile, the field still lacks a fair real-robot comparison between VLA and WAM execution capabilities, since existing systems often differ in data, robot embodiments, and task protocols. To address both the decomposition gap and the need for a controlled WAM-VLA comparison, we introduce DSWAM, a Dual-System World Action Foundation Model for fine-grained robot manipulation. DSWAM keeps a System 1 WAM executor as the default control path and optionally activates a System 2 vision-language subtask planner only when task decomposition is useful. The planner predicts executable subtasks from short-term visual history and a global task prompt, while the WAM executor performs world-aware action generation for each instruction or subtask. The executor is trained with action prediction and video co-training, but inference directly predicts action chunks without explicit future video generation. To make this execution path practical on real robots, we further integrate TensorRT acceleration, asynchronous execution, and real-time chunking (RTC) so that policy queries do not block robot control. To provide a fair real-robot comparison with VLA policies, we build and evaluate DSWAM under the DeMaVLA real-world deformable manipulation setting with matched robot platform, pretraining data, post-training data, and evaluation criteria.

---

## 论文详细总结（自动生成）

# DSWAM 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现有世界动作模型（WAM）擅长基于物理世界的细粒度执行，但缺乏基于视觉语言模型（VLM）的显式语言级规划接口，无法将粗略指令（如“把桌子上的东西整理好”）自动分解为有序的子任务序列。相反，视觉‑语言‑动作（VLA）策略虽能理解自然语言并做高层推理，但其观察‑动作映射缺少对物理动态的密集监督，在接触丰富的操作任务中表现不足。此外，WAM与VLA之间一直缺乏在相同机器人平台、相同数据、相同评估标准下的公平实机对比。
- **整体含义**：论文旨在同时弥补“分解鸿沟”和“公平对比缺失”，提出一个双系统世界动作基础模型DSWAM，统一物理执行与语言规划能力，并在可控条件下验证WAM式执行策略的优越性。

## 2. 论文提出的方法论

### 核心思想
- **双系统设计**：System 1 是一个默认的WAM执行器，负责从多模态观测、语言指令和本体感受中预测连续动作块；System 2 是一个可选的视觉‑语言子任务规划器，仅当粗略指令需要分解时才激活。
- **训练‑推理解耦**：执行器在训练时使用动作预测和视频协同训练（flow matching损失），获取世界建模监督；但在推理时直接预测动作块，无需显式的未来视频生成，降低延迟。
- **高效部署**：集成TensorRT加速、异步执行和实时分块（RTC），使策略查询不阻塞机器人控制。

### 关键技术细节
- **观测建模**：\( O_t = [I^1_t, I^2_t, I^3_t, \ell_t, q_t] \)（三视角RGB图像、语言指令/子任务、本体状态）。
- **动作预测**：预测长度为 \( H \) 的动作块 \( A_t = [a_t, a_{t+1}, \dots, a_{t+H-1}] \)，使用条件流匹配（conditional flow matching）生成连续动作。
- **System 2 规划器**：基于Rynnbrain4B风格的视觉‑语言模型，输入最近5帧（1Hz采样）和全局任务提示 \( p \)，输出下一个子任务指令 \( s_t = P_\phi(X_t, p) \)。训练时采用转换感知监督（transition-aware supervision），使其既识别当前子任务，又能在子任务边界处发出下一个指令。
- **System 1 执行器**：编码观测到潜在世界特征 \( z_t = f_\theta(I^{1:3}_t, \ell_t, q_t) \)，再预测动作块。训练损失：\( L = L_{\text{act}} + \lambda_{\text{vid}} L_{\text{vid}} \)，其中 \( L_{\text{act}} = \text{LFM}(A_t) \)，\( L_{\text{vid}} = \text{LFM}(V_{t+1:t+T}) \)，两者均使用流匹配损失 \( \text{LFM}(y) = \mathbb{E}[\|v_\theta(y_\tau, \tau, O_t) - (\epsilon - y)\|^2] \)。
- **部署优化**：拆分TensorRT引擎（视觉上下文引擎 + 动作去噪引擎），BF16精度下延迟从198.2ms降至73.8ms（2.69×加速）；结合RTC异步执行。

## 3. 实验设计

### 数据集/场景
1. **RoboTwin 2.0 模拟基准**：包含50个双手机器人操作任务，分“clean”（固定初始配置）和“randomized”（随机物体位姿和场景布局）两种设置。
2. **真实世界DeMaVLA折叠基准**：使用ALOHA式双臂平台，包括衬衫、裙子、裤子、毛巾四种衣物，每类两个实例（一个较易、一个较难），每实例10次试验，共80次试验。任务流程：从篮子中随机取出衣物 → 放置于桌面 → 展开 → 完成目标折叠。
3. **System 2 排序任务**：真实桌面排序任务，指令“Sort objects on the table”，分解为两个子任务“Pick up the toy car and place it into the box”和“Pick up the bread and place it on the plate”。

### 对比方法
- **模拟基准**：π0、π0.5、DeMaVLA、Motus、Fast-WAM。
- **真实折叠基准**：π0、DeMaVLA（与DSWAM在同一平台、数据、评估准则下对比）。
- **消融实验**：DSWAM的WAM‑only模式 vs 加入System 2；同步TensorRT vs 异步TensorRT+RTC。

## 4. 资源与算力

- **文中明确提及**：TensorRT加速测试使用NVIDIA GeForce RTX 5090 GPU，CUDA 12.9，TensorRT 10.16.1。
- **未明确说明**：训练阶段的GPU数量、型号、训练时长及总算力消耗。论文仅提及使用5,000+小时真实世界数据进行预训练，但未给出具体训练资源细节。

## 5. 实验数量与充分性

- **实验数量**：
  - 模拟任务：50个任务 × 2种设置（clean/randomized），每个方法报告平均成功率。
  - 真实折叠：4类衣物 × 2实例 × 10 trials = 80 trials，报告成功率与平均完成时间。
  - System 2 排序：4组条件（原始指令 vs 子任务指令，训练步数6000 vs 18000），报告成功率和每轮滚动错误数。
  - 效率实验：TensorRT延迟对比（PyTorch vs BF16 TensorRT），同步/异步部署在衬衫和裤子上各10 trials。
- **充分性与公平性**：
  - 真实折叠基准严格匹配DeMaVLA设置（平台、数据、任务、准则），DSWAM在WAM‑only模式下对比，排除了高层规划的干扰，公平性强。
  - 模拟任务涵盖多种双人操作技能，且评估了随机化鲁棒性。
  - System 2实验清晰展示了子任务监督的效果，但样本数较少（每组仅少数rollout次数），统计显著性未明确给出。
  - 总体实验设计系统、对比设置公平，但未对不同随机种子进行多次重复报告具体方差。

## 6. 论文的主要结论与发现

- **WAM式执行优于VLA**：在DeMaVLA真实折叠基准中，DSWAM（WAM‑only）的平均成功率从DeMaVLA的92.5%提升至96.3%，完成时间从2′18″缩短至1′44″。在裤子任务上提升最显著（成功率75%→90%，时间3′01″→2′19″）。
- **模拟任务泛化性强**：RoboTwin 2.0上DSWAM在clean设置平均92.38%，randomized设置91.90%，超越所有基线，包括Fast-WAM（91.88%/91.78%）和DeMaVLA（88.42%/86.78%）。
- **System 2可选规划器提升稳定性**：在排序任务中，子任务级别的指令监督使成功率从原始指令的75.7%提升至100%，每轮滚动错误从3.53降至0.65。
- **高效部署可行**：BF16 TensorRT加速2.69倍，异步RTC进一步缩短执行时间，在衬衫和裤子任务上均实现100%成功率且时间更短。

## 7. 优点

- **双系统解耦设计**：将高层语义规划与低层物理执行分离，按需激活，不增加原子指令的额外开销，灵活适应不同复杂度的任务。
- **公平比较的基准**：首次在完全匹配的条件下（相同平台、数据、任务、评估）对比WAM与VLA策略，提供了WAM执行能力优于VLA的可靠证据。
- **推理高效**：训练时用视频协同学习世界模型，推理时直接预测动作块，省去未来视频生成，结合TensorRT+RTC实现实时控制。
- **实验覆盖较广**：涵盖模拟50任务、真实折叠4类衣物、排序任务及效率测量，验证了方法的通用性和实用性。
- **设计简洁实用**：无需复杂的世界状态预测，依赖于成熟的流匹配和Transformer架构，易于工程实现。

## 8. 不足与局限

- **实验覆盖有限**：真实世界只评估了折叠任务，未涉及其他类型的操作（如刚体装配、精细抓取等），泛化性有待进一步验证。
- **System 2依赖预定义子任务边界**：规划器训练需要预先对轨迹进行子任务标注，在开放场景中自动获取边界可能困难。
- **未提供训练资源细节**：文中未说明训练所用的GPU数量、时长，难以复现训练成本。
- **统计显著性未明确**：成功率提升（如96.3% vs 92.5%）未报告方差或置信区间，部分实验trial数较少（排序任务仅几组），可能存在偶然性。
- **长时序依赖性**：动作块长度H固定，对于需要长时间静态保持或复杂序列交互的任务，实时分块策略的效果未讨论。
- **部署仍需高端GPU**：RTX 5090保证了低延迟，但普通机器人平台上可能难以复现相同性能。

（完）
