---
title: Explicit Language Memory for Long-Horizon Planning in Vision-Language-Action Models
title_zh: 面向视觉-语言-动作模型长时程规划的显式语言记忆
authors: "Houze Xu, Jizhong Li, Ziyi Ye"
date: 2026-08-05
pdf: "https://arxiv.org/pdf/2608.04765v1"
tags: ["query:vla"]
score: 10.0
evidence: 具有显式语言记忆的VLA架构用于长期任务
tldr: 现有VLA模型在长程任务中面临时序一致性差、闭环纠错能力不足、语义表示易被端到端微调削弱等挑战。本文提出分层架构，引入显式语言记忆模块，将离散时序观测转化为具有时序逻辑的连贯文本记忆，并由高层VLM递归更新记忆与子任务指令。在多个模拟环境和真实机器人平台上实验显示，该方法显著提升复杂长程任务的成功率和鲁棒性。同时为决策过程提供了可解释的语义依据，增强了跨任务组合泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.04765v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1699, \"height\": 711, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1542, \"height\": 715, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1623, \"height\": 765, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.04765v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1597, \"height\": 1048, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.04765v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1330, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04765v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1714, \"height\": 395, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.04765v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1249, \"height\": 210, \"label\": \"Table\"}]"
motivation: 现有VLA模型在长程任务中受时序一致性差、纠错弱和语义退化困扰，需要引入显式记忆机制。
method: 构建分层架构，高层VLM通过视觉问答范式递归更新显式语言记忆和子任务指令，低层VLA执行连续控制。
result: 在多个模拟环境和真实机器人实验中，该方法提升长程任务成功率和鲁棒性，并提供可解释语义。
conclusion: 显式语言记忆能够增强VLA模型的长程规划与纠错能力，为复杂任务提供可解释决策依据。
---

## 摘要
视觉-语言-动作（VLA）模型为连接视觉感知、语言理解和机器人控制提供了统一范式。然而，现有VLA模型在长时程任务中仍面临主要挑战：稀疏的专家示范制约了跨任务组合泛化；长时程任务的非马尔可夫性质使仅以当前观测为条件的策略难以保持时间一致性；有限的闭环纠错导致执行误差不断累积；端到端动作微调可能削弱视觉-语言模型（VLM）主干的高层语义表征。为解决这些问题，我们提出了一种具有显式语言记忆模块的分层长时程VLA架构。其核心思想是将离散的时间观测转化为具有时间逻辑的连贯文本记忆序列。该系统解耦为高层VLM和低层VLA：高层VLM通过视觉问答训练范式进行语义推理，而低层VLA根据子任务指令和视觉观测执行精确的连续控制。高层VLM以先前的记忆作为上下文锚点，递归更新语言记忆和子任务指令，从而在长时程执行过程中实现持续的时间跟踪与动态纠错。我们在多个仿真环境中评估了所提方法，并在真实机器人平台上进行了从仿真到现实（sim-to-real）的实验。结果表明，显式语言记忆提高了VLA模型在复杂长时程任务上的成功率和鲁棒性，同时为决策过程提供了可解释的语义说明。

## Abstract
Vision-language-action (VLA) models provide a unified paradigm for connecting visual perception, language understanding, and robotic control. However, existing VLA models still face major challenges in long-horizon tasks: sparse expert demonstrations constrain cross-task compositional generalization; the non-Markovian nature of long-horizon tasks makes it difficult for policies conditioned only on current observations to maintain temporal consistency; limited closed-loop error correction allows execution errors to accumulate; and end-to-end action fine-tuning may weaken the high-level semantic representations of vision-language model (VLM) backbones. To address these issues, we propose a hierarchical long-horizon VLA architecture with an explicit language-memory module. The central idea is to convert discrete temporal observations into a coherent textual memory sequence with temporal logic. The system is decoupled into a high-level VLM and a low-level VLA: the high-level VLM performs semantic reasoning through a visual question answering training paradigm, while the low-level VLA executes precise continuous control conditioned on subtask instructions and visual observations. The high-level VLM recursively updates both language memory and subtask instructions using the previous memory as a contextual anchor, enabling persistent temporal tracking and dynamic correction during long-horizon execution. We evaluate the proposed method in multiple simulation environments and conduct sim-to-real experiments on a real robotic platform. The results demonstrate that explicit language memory improves the success rate and robustness of VLA models on complex long-horizon tasks while providing an interpretable semantic account of the decision process.

---

## 论文详细总结（自动生成）

# 论文总结：显式语言记忆用于 VLA 长时程规划

## 1. 核心问题与整体含义

- **研究背景**：视觉-语言-动作（VLA）模型将视觉感知、语言理解与机器人控制统一到同一策略中，在短时程动作生成上已展现出较强能力，但长时程、多阶段任务仍然困难。
- **核心问题**：长时程任务具有非马尔可夫性，即当前观测本身不足以判断任务阶段；仅依赖当前帧的 VLA 策略容易跳过阶段、重复已完成动作或在未观测到的失败后继续执行，误差随动作序列不断累积。
- **整体含义**：论文主张把任务历史维护为**显式自然语言记忆**，而不是拼接所有历史帧。语言记忆作为紧凑、可读、可解释的状态变量，记录已完成阶段、当前状态和下一步意图，从而使策略具备跨时间的一致性。
- **研究动机**：端到端动作微调可能削弱 VLM 主干的语义表征；稀疏专家演示制约跨任务泛化；闭环纠错能力不足。论文希望以语言记忆缓解上述问题，并将语义规划与连续控制解耦。

## 2. 方法论

- **分层策略分解**：将完整策略分解为高层语义分支 \(\pi_{\mathrm{HL}}\) 和低层动作分支 \(\pi_{\mathrm{LL}}\)：
  
  \[
  \pi(a_{t:t+H}, s_t, m_t \mid o_t, m_{t-1}, l) =
  \pi_{\mathrm{LL}}(a_{t:t+H} \mid o_t, s_t)
  \cdot
  \pi_{\mathrm{HL}}(s_t, m_t \mid o_t, m_{t-1}, l)
  \]
  
  其中 \(o_t\) 为当前观测，\(m_{t-1}\) 为上一时刻语言记忆，\(l\) 为全局指令，\(s_t\) 为当前子任务，\(a_{t:t+H}\) 为固定长度动作块。

- **递归语言记忆**：
  - 记忆 \(m_t\) 是结构化的自然语言摘要，记录已完成里程碑、完成证据、关键物体属性、观测到的失败以及下一待完成步骤。
  - 采用“滚动压缩”规则：子任务完成前保留目标细节（类别、颜色、位置等）；完成后将细节压缩为过去时陈述，并将注意力转向下一动作。
  - 记忆只在观测支持当前里程碑完成后更新，从而保证阶段推进以证据为条件。

- **自动记忆监督**：
  - 利用专家视频、技能标签和起止时间戳，将每个视频帧对齐到活动技能索引。
  - 使用通用 VLM 对当前帧和完整技能标注生成两类监督目标：简洁子任务（动作 + 目标物体 + 上下文，不带智能体主语）和第一人称语言记忆（过去时描述已完成技能 + 未来意图）。
  - 标注可通过云端 Gemini 3.5 Flash API 或本地 Qwen3.5-9B 完成。

- **独立分支训练**：
  - 高层分支使用 PaliGemma，自回归预测更新后的记忆 \(m_t\) 和子任务 \(s_t\)，损失为两者的对数似然之和。
  - 低层分支接收观测 \(o_t\) 和子任务 \(s_t\)，基于 flow-matching 动作专家生成连续动作块，文中给出监督动作损失 \(\|a_{t:t+H} - \pi_{\mathrm{LL}}(o_t,s_t)\|_2^2\) 的简化形式。
  - 两个分支独立训练，避免在单一整体目标中手动平衡语义生成损失与连续控制损失。

- **闭环推理流程**：
  1. 初始化 \(m_0 = \emptyset\)。
  2. 每个时间步：获取观测 \(o_t\)。
  3. 高层模型更新 \((m_t, s_t) = \pi_{\mathrm{HL}}(o_t, l, m_{t-1})\)。
  4. 低层模型生成动作块 \(a_{t:t+H} = \pi_{\mathrm{LL}}(o_t, s_t)\)。
  5. 执行动作块，若 \(m_t\) 指示任务完成则终止。

## 3. 实验设计

- **数据集与场景**：
  - **BEHAVIOR-1K 开收音机任务**：指令为“打开客厅桌子上的收音机接收器”。阶段划分为 MoveTo、PickUp、PressOn、PlaceOn 四个子阶段。
  - **Genie Sim 3.0 包裹分拣**：单包裹任务包含 Follow、PickUp-1、Inside-1、Upright、PickUp-2、Inside-2 六个阶段；连续分拣任务要求对四个不同颜色包裹分别完成“放入扫描区且条码朝上”的放置。
  - **真实 XLeRobot 拾放平台**：使用双 SO-ARM 101 机械臂、LeKiwi 移动底座、RGB 头部相机和手部相机。任务为从桌面拾取方块并放到盘子上，阶段为 Follow、PickUp、MoveTo、Place。数据集包含 25 条人工采集轨迹，评估 20 次。

- **基准与对比方法**：
  - 基线为原生 \(\pi_{0.5}\) 策略。
  - 提出方法为 \(\pi_{0.5} +\) 显式语言记忆的分层架构。
  - 未直接与其他带记忆或推理链的 VLA 方法（如 HAMLET、MemoryVLA、MAP-VLA、Embodied CoT 等）进行数值对比。

- **评价指标**：
  - 采用阶段成功率 \(SR = \frac{1}{N}\sum_{i=1}^{N}\frac{1}{M_i}\sum_{j=1}^{M_i} SR_{ij}\)，其中 \(SR_{ij}\in\{0,1\}\) 表示第 \(i\) 个样例的第 \(j\) 阶段是否完成。
  - 该指标能暴露长序列中具体失败在哪个阶段，而不是只看最终是否完成。

## 4. 资源与算力

- **训练与微调**：使用 **4 块 NVIDIA A100 GPU**。
- **推理**：使用 **1 块 NVIDIA RTX 5880 Ada Generation GPU**。
- **训练配置**：动作块长度 \(H=30\)；高层 PaliGemma 及低层 VLA 中的 VLM 主干使用 LoRA 适配；300M 参数 Gemma action expert 全量微调；优化器为 AdamW，配合 warmup 和余弦学习率衰减；峰值学习率 \(2.5\times10^{-5}\)；batch size 64；训练步数 50,000 步。
- **未明确信息**：论文没有给出总训练时长（小时/天），也没有说明 50,000 步对应的具体 wall-clock 时间。

## 5. 实验数量与充分性

- **实验组数**：
  - 三个主要环境：BEHAVIOR-1K 开收音机（单任务）；Genie Sim 3.0 单包裹分拣和连续四包裹分拣；真实 XLeRobot 拾放任务。
  - 每个环境只对比两个模型：\(\pi_{0.5}\) 和 \(\pi_{0.5}+\) 语言记忆。
  - 未提供正式的消融实验（例如移除记忆、不同记忆长度、不同压缩规则、不同标注来源等）。

- **样本量与统计充分性**：
  - 真实机器人任务仅 25 条训练轨迹、20 次评估，样本量较小。
  - 仿真实验未明确报告每个任务的评估样例数、随机种子数或置信区间，只给出阶段成功率均值。
  - 缺少与其他 memory/CoT 类方法的直接比较，因此无法判断所提语言记忆相对于其他历史建模方法的优势。
  - 总体而言，实验验证了方法的可行性，但统计稳健性和泛化性证据仍较有限。

## 6. 主要结论与发现

- **整体性能提升**：
  - BEHAVIOR-1K 开收音机平均阶段成功率从 30.0% 提升到 40.0%，主要提升在 PickUp 阶段（10.0%→30.0%）；MoveTo 已达到 100% 饱和；PressOn 仍很低（0.0%→10.0%）。
  - Genie Sim 单包裹分拣平均成功率从 41.7% 提升到 63.9%，其中 Follow 与 PickUp-1 从 50.0% 提升到 100.0%；连续分拣平均成功率从 31.3% 提升到 46.9%，且基线从未完成第三、四次放置，语言记忆模型有 12.5% 的评估完成全部四次放置。
  - 真实 XLeRobot 拾放平均成功率从 62.5% 提升到 66.3%，提升较小。

- **阶段一致性与减少跳步**：
