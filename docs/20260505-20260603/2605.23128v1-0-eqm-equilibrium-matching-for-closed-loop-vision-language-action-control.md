---
title: "$π_0$-EqM: Equilibrium Matching for Closed-Loop Vision-Language-Action Control"
title_zh: "$π_0$-EqM: 闭环视觉-语言-动作控制的均衡匹配"
authors: "Huanming Liu, Congsheng Xu, Jianmin Ji, Yao Mu"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23128v1"
tags: ["query:vla"]
score: 10.0
evidence: 用于闭环VLA控制的平衡匹配解码器
tldr: "当前视觉-语言-动作模型常采用固定采样步数的流匹配解码器，限制了状态依赖计算与跨周期重用。本文提出π₀-EqM，将π₀中的流匹配专家替换为平衡匹配解码器，保持上游VLA不变。在300步预算下，RoboTwin 19个任务平均成功率从40.4%提升至50.2%，LIBERO-10达87.0%。阈值扫描揭示了残差与成功之间的非单调关系，即平稳性-可执行性差距，表明推理深度是策略设计的一部分，并为组件化动作生成提供了能量视角。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23128v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 891, \"height\": 1033, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23128v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 861, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23128v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 855, \"height\": 657, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.23128v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1563, \"height\": 517, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.23128v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 679, \"height\": 298, \"label\": \"Table\"}]"
motivation: 现有VLA模型的流匹配解码器固定采样步数，无法根据状态调整计算量或跨控制周期复用，限制了闭环控制效率与性能。
method: 用平衡匹配解码器替代π₀中的流匹配专家，通过迭代求解能量最小化生成动作，保留上游VLA编码器不变，实现状态依赖的推理深度。
result: "在300步预算下，RoboTwin平均成功率从40.4%提升至50.2%，LIBERO-10达到87.0%；阈值扫描显示残差与成功存在非单调关系。"
conclusion: 推理深度应作为策略设计的一部分，能量视角的VLA有助于未来跨任务与跨本体的组件化动作生成。
---

## 摘要
当前，视觉-语言-动作（VLA）模型因其在任务泛化方面的巨大潜力，已成为机器人操作领域最常用的范式。然而，用于VLA控制的大多数生成式流匹配动作解码器通常采用固定的采样视界，限制了跨控制周期的状态相关计算和时间复用。我们提出了$π_0$-EqM，它用均衡匹配（EqM）解码器替换了$π_0$中的流匹配专家，同时保持上游VLA堆栈不变。在匹配的300步预算下，$π_0$-EqM在19个任务中将RoboTwin的平均成功率从40.4%提升至50.2%，并在LIBERO上保持竞争力，其中在LIBERO-10上获得最显著提升（87.0%）。两次阈值扫描揭示了残差与成功率之间存在任务依赖的非单调关系，我们称之为平稳性-可执行性鸿沟。结果表明，迭代VLA控制中的推理深度是策略设计的一部分，并引入了基于能量的VLA视角，这可能为未来跨任务和本体的可组合动作生成研究提供参考。

## Abstract
Currently, Vision-Language-Action (VLA) models have become the most adopted paradigm for robotic manipulation for its great potential for task generalization. While most generative flow-matching action decoders for VLA control are often deployed with fixed sampling horizons, limiting state-dependent compute and temporal reuse across control cycles. We present $π_0$-EqM, which replaces the flow-matching expert in $π_0$ with an Equilibrium Matching (EqM) decoder while leaving the upstream VLA stack unchanged. Under a matched 300-step budget, $π_0$-EqM improves RoboTwin average success from 40.4% to 50.2% across 19 tasks and remains competitive on LIBERO, with its clearest gain on LIBERO-10 (87.0%). Two threshold scans reveal a task-dependent non-monotonic relation between residual and success, which we term the stationarity--executability gap. The results suggest that inference depth in iterative VLA control is part of policy design and introduce an energy-based VLA perspective that may inform future work on composable action generation across tasks and embodiments.

---

## 论文详细总结（自动生成）

## 论文总结：π₀-EqM: 闭环视觉-语言-动作控制的均衡匹配

### 1. 核心问题与整体含义（研究动机和背景）
当前 Vision-Language-Action (VLA) 模型（如 π₀）广泛使用流匹配（flow-matching）或扩散（diffusion）动作解码器，但这些解码器通常采用 **固定采样步数** 的推理方式，无法根据当前状态自适应调整计算量，也难以在相邻控制周期之间高效复用迭代结果（如冷启动）。这种限制使得推理深度成为固定参数，而非策略设计的可控变量。  
本文提出 **π₀-EqM**，将 π₀ 中的流匹配专家替换为 **均衡匹配（Equilibrium Matching, EqM）** 解码器，将动作生成视为迭代求解均衡点，使得推理深度可自适应、可热启动，从而将 **推理深度纳入策略设计的一部分**，并引入 **基于能量的 VLA 视角**，为未来跨任务、跨本体的可组合动作生成提供新思路。

### 2. 方法论：核心思想、关键技术细节
- **核心思想**：学习一个时间无关（自治）的条件向量场 \( f_\theta(A; c_t) \)，其稳态（根）对应目标动作块。通过迭代求解 \( f_\theta = 0 \) 生成动作，而非遵循固定噪声调度的时间索引传输。
- **训练目标**：定义插值变量 \( A_\gamma = \gamma A + (1 - \gamma)\varepsilon \)，训练 \( f_\theta \) 逼近目标向量 \( w(\gamma)(A - \varepsilon) \)，损失函数为 EqM loss（式 1），其中 \( w(\gamma) \) 在 \( \gamma \to 1 \) 时衰减到零，迫使数据流形成为稳定均衡集。
- **推理过程**：使用 Nesterov 加速梯度下降法（式 2-3）迭代求解 \( f_\theta = 0 \)，动量因子 \( \mu \)，步长 \( \eta \)。
- **自适应停止**：定义归一化 lookahead 残差 \( r_k = \| f_\theta(\tilde{A}^{(k)}_t; c_t) \|_2 / \sqrt{Hd} \)，当 \( r_k \leq \tau \) 或达到最大步数时停止，实现状态依赖的计算分配。
- **结构性热启动**：将上一控制周期的动作块前半部分复制到当前初始化的前半段，后半段用随机噪声填充（式 5），利用时序相关性减少迭代次数。
- **理论解释**：局部假设下，迭代遵循势能下降（Proposition 1），且若映射是局部收缩的，则残差指数衰减，给出到达给定阈值所需迭代次数的上界（式 9），热启动通过减小初始距离来降低所需步数。

### 3. 实验设计
- **数据集/场景**：
  - **RoboTwin** 基准：19 个任务，每个任务 500 条演示（100 干净 + 400 随机），共 9500 条。
  - **LIBERO** 基准：包含四个套件：LIBERO-Spatial、LIBERO-Object、LIBERO-Goal、LIBERO-10。
- **基准方法**：原始 π₀（使用流匹配解码器），保持上游 VLA 堆栈和条件接口不变，仅替换动作解码器。
- **对比设置**：主要对比在 **匹配 300 步推理预算** 下进行。阈值扫描实验允许最多 1000 步，并根据残差阈值提前停止。
- **评估指标**：任务成功率（%）。

### 4. 资源与算力
**论文未明确说明**训练或推断所使用的 GPU 型号、数量、训练时长等信息。仅提及在匹配的 300 步预算下比较性能，未报告具体硬件资源。

### 5. 实验数量与充分性
- **实验数量**：
  - 主对比：19 个 RoboTwin 任务 + 4 个 LIBERO 套件，共 23 个任务/套件。
  - 阈值扫描：在 RoboTwin 的 2 个任务（place_dual_shoes 和 click_bell）上进行详细扫描，另有一个示范性可视化（click_alarmclock）。
  - 未进行专门的消融实验来分离 EqM 目标、停止规则、热启动各自贡献。
- **充分性与公平性**：
  - **优势**：对比了多个数据集（RoboTwin 和 LIBERO），且保持上游网络完全一致，仅改动解码器，实验设计公平。
  - **不足**：阈值扫描仅覆盖 2 个任务，未能广泛展示非单调现象；缺少对 EqM 不同组件（如是否使用热启动、不同动量参数）的消融分析；理论分析（Proposition 1）仅为局部假设，未提供全局收敛保证。

### 6. 主要结论与发现
- **性能提升**：在匹配 300 步预算下，π₀-EqM 在 RoboTwin 19 个任务上平均成功率从 40.4% 提升至 **50.2%**（+9.8 个点）；在 LIBERO-10 上从 85.2% 提升至 **87.0%**，其余 LIBERO 套件略有升降，四套件平均从 94.15% 升至 94.35%。
- **平稳性-可执行性鸿沟**：阈值扫描表明，**残差最低的迭代不一定产生最佳执行结果**，残差与成功率之间存在任务依赖的非单调关系（最优阈值因任务而异）。这说明在闭环控制中，停止阈值不仅影响求解精度，更直接改变部署策略的行为。
- **推理深度的策略性**：当解码器成为迭代求解过程时，停止深度成为可调节的策略变量，可独立于上游 VLA 网络进行调优。

### 7. 优点
- **方法简洁有效**：仅替换解码器，保留上游 VLA 堆栈，易于集成，且显著提升性能。
- **引入自适应计算**：通过残差阈值实现状态依赖的推理深度，避免固定步数的浪费或不足。
- **结构热启动**：利用时序相关性减少迭代，提高效率。
- **提供理论支撑**：局部势函数下降和收缩性分析解释了阈值与迭代次数的关系，使实验设置具有理论依据。
- **提出新的视角**：将 VLA 解码器视为能量最小化过程，为后续可组合动作生成（跨任务、跨本体）奠定基础。

### 8. 不足与局限
- **实验覆盖有限**：阈值扫描仅两个任务，非单调现象未被系统验证；缺少对 EqM 不同超参数（如动量、步长）的敏感性分析。
- **理论分析局部性**：Proposition 1 假设局部梯度场行为，未处理非保守场的真实情况，也未提供全局收敛性定理。
- **缺乏成分消融**：未能分离 EqM 目标本身、自适应停止规则、热启动结构各自的贡献。
- **未考虑实际部署延迟**：虽然迭代步数可调，但未分析在不同计算预算下的延迟与性能权衡。
- **缺少跨本体/多任务泛化实验**：仅在单一机器人设置（RoboTwin 和 LIBERO）上验证，未推广到不同机械臂形态或多任务组合场景。

（完）
