---
title: "ProgVLA: Progress-Aware Robot Manipulation Skill Learning"
title_zh: "ProgVLA: 进度感知的机器人操作技能学习"
authors: "Seungsu Kim, Jinyoung Choi, Seungmin Baek, Jean-Michel Renders"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28231v1"
tags: ["query:vla"]
score: 9.0
evidence: 用于操作的进度感知VLA模型
tldr: 机器人操作需要紧凑模型处理长多模态序列。ProgVLA通过两阶段Perceiver重采样压缩序列，并引入离线RL进度头估计剩余任务进度，实现优势加权流匹配学习。0.1B参数模型在基准上超越大基线，尤其在长程任务中表现更优。该方法验证了紧凑架构与进度感知训练结合的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28231v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 539, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28231v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 791, \"height\": 503, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28231v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1269, \"height\": 869, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28231v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1168, \"height\": 367, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28231v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1485, \"height\": 1023, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28231v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1087, \"height\": 671, \"label\": \"Table\"}]"
motivation: 在计算和内存受限下，机器人操作需要紧凑模型高效处理长多模态序列并保持任务进度感知。
method: 提出ProgVLA，包含两阶段Perceiver重采样压缩序列为固定上下文令牌，以及基于离线RL的进度头学习剩余时域批评，实现优势加权流匹配模仿学习。
result: 0.1B参数ProgVLA在标准基准上竞争或超越更大预训练模型，尤其在长程和多对象任务上表现更优，真实环境验证有效。
conclusion: ProgVLA展示了紧凑VLA模型通过进度感知训练和高效重采样，在资源受限下实现可靠长程机器人操作。
---

## 摘要
我们提出了ProgVLA，一个紧凑的视觉-语言-动作（VLA）模型，设计用于在严格的计算和内存预算下实现可靠的机器人操作。该模型专注于高效处理长多模态序列，通过在扩展的时间范围内维护任务进度的显式表示来实现这一点。为此，ProgVLA整合了两个关键组件。首先，一个具有两阶段Perceiver重采样方案的多模态编码器将可变长度的视觉、语言和本体感觉流压缩为一组固定的控制就绪上下文标记，大大减少了序列长度，同时保持了跨模态的对齐。其次，一组辅助的进度头通过离线强化学习（RL）目标进行训练，以联合学习关于归一化剩余时间目标的批评者。这为策略提供了任务进度的内部估计，并实现了优势加权和成功加权的流匹配模仿学习。在两个成熟的多任务机器人操作基准测试中，一个0.1B参数的ProgVLA模型达到了与显著更大的预训练基线相当的成功率，并且在长时域和更难的任务层级上超过了它们。消融研究表明，学习到的上下文重采样器和任务自适应视觉微调是最大的单一贡献者，而进度感知训练提供了持续的额外增益，主要集中在长时域和多对象任务上。我们进一步在现实世界的玩具厨房环境中验证了该方法。

## Abstract
We present ProgVLA, a compact vision-language-action (VLA) model designed for reliable robot manipulation under tight compute and memory budgets. The model specifically focuses on efficiently processing long multi-modal sequences by maintaining an explicit representation of task progress over extended horizons. To this end, ProgVLA integrates two key components. First, a multi-modal encoder with a two-stage Perceiver resampling scheme compresses variable-length visual, language, and proprioceptive streams into a fixed set of control-ready context tokens, substantially reducing sequence length while preserving cross-modal grounding. Second, an auxiliary set of progress heads is trained with offline reinforcement learning (RL) objectives to jointly learn critics over normalized remaining-horizon targets. This provides the policy with an internal estimate of task progress and enables advantage- and success-weighted flow-matching imitation learning. On two well-established multi-task robot manipulation benchmarks, a 0.1B-parameter ProgVLA model reaches success rates that are competitive with, and on long-horizon and harder task tiers exceed, substantially larger pretrained baselines. Ablations indicate that the learned context resampler and task-adaptive visual fine-tuning are the largest single contributors, while progress-aware training provides a consistent additional gain that is concentrated on long-horizon and multi-object tasks. We further validate the approach in real-world toy-kitchen environments.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有大型视觉-语言-动作（VLA）模型（如OpenVLA、π0）依赖数十亿参数和大规模跨具身机器人预训练，计算和存储成本极高，限制了其在实际部署中的普及。而紧凑型VLA（如SmolVLA）虽能在较低硬件上运行，但在长时域任务（需长时间保持连贯行为、跟踪多个对象）上性能显著下降。
- **研究动机**：能否设计一个紧凑模型（0.1B参数），不依赖大规模机器人预训练，仅使用目标基准的示范数据，即可在长时域、多对象等困难任务上匹配甚至超越大型预训练基线？
- **整体含义**：论文提出ProgVLA，通过高效序列压缩、通用视觉先验和内部进度感知训练，证明紧凑架构结合进度监督可以替代大规模机器人预训练，实现资源受限下的可靠操作。

### 2. 论文提出的方法论

- **核心思想**：在紧凑计算预算下，通过三方面设计提升长时域性能：
  1. **两阶段Perceiver重采样**：先对每个模态（视觉、语言、本体感觉）分别用Perceiver Resampler压缩为固定数量令牌，再通过融合Transformer和第二个后融合Resampler进一步压缩为少量控制就绪的上下文令牌（context tokens），大幅缩短序列长度。
  2. **通用视觉骨干与微调**：使用DUNE（从多种2D和3D任务蒸馏的ViT-Small）作为视觉编码器，并在训练过程中联合微调，使其适应机器人手腕视角和细粒度接触区域。
  3. **辅助进度头与离线RL目标**：从相同上下文令牌出发，训练三个辅助头：
     - **状态-动作价值函数 ˆQ**：回归蒙特卡洛回报`r_t`（基于剩余时域归一化的进度信号），使用Huber损失。
     - **状态价值函数 ˆV**：通过IQL风格的expectile回归（ρ=0.8）对同一`r_t`进行乐观基线估计。
     - **成功分类头 ˆS**：二分类判断是否接近完成（`r_t ≥ 0.966`），使用二值交叉熵损失。
   - 将优势`A_t = ˆQ - ˆV`（detached）转化为权重`w_A`，成功概率`p_succ`转化为权重`w_S`，二者相乘后对每个样本的流匹配损失进行重加权，形成优势-成功加权模仿学习。
- **关键技术细节**：
  - 流匹配动作专家：采用SmolVLA架构，预测16步动作块，执行前8步后重新规划，训练时采样`τ ~ Beta(2,2)`。
  - 总损失：`L_total = E[w_A w_S ℓ_FM] + λ_V L_V + λ_Q L_Q + λ_S L_S`，其中`λ_V=0.5, λ_Q=1.0, λ_S=0.2`。
  - 温度`β=0.5`，剪裁`C=20`，期望值参数`ρ=0.8`。

### 3. 实验设计

- **数据集与场景**：
  - **LIBERO**：40个任务（Spatial/Object/Goal/Long四类），每任务50条示范，共2000条。使用手腕和agent相机RGB图像、本体感觉、自然语言指令。
  - **Meta-World MT50**：公开LeRobot数据集，49个任务（Easy/Medium/Hard/Very Hard四类），每任务50条示范。
  - **真实世界**：6-DOF PiPER机械臂，玩具厨房环境（8任务）和白桌环境（2任务），共10个任务，每任务50条示范。
- **Benchmark与对比方法**：
  - 对比Octo (0.09B)、π0 (Paligemma-3B及3.3B)、OpenVLA (7B)、SmolVLA (0.24B及2.25B)。基线结果从原论文引用，并非在完全相同协议下重跑（存在示范数量、分辨率差异）。
- **评估指标**：成功率（SR），每个任务50次评估（LIBERO），Meta-World和真实世界类似。

### 4. 资源与算力

- **训练硬件**：单块NVIDIA H100 GPU。
- **训练时长**：LIBERO 25小时，Meta-World 32小时，真实世界模型约8.5小时。
- **模型规模**：总参数量0.1B（109M，Meta-World版100M），其中约74M（Meta-World 65M）在训练中更新。仅预训练组件为DUNE ViT-Small和冻结T5文本编码器。

### 5. 实验数量与充分性

- **实验数量**：
  - 两个模拟基准（LIBERO + Meta-World）的主实验。
  - LIBERO上的4组消融实验（移除进度头、移除后融合重采样器、替换DUNE为DINOv3、冻结DUNE）。
  - 真实世界100次试验（10任务×10次）。
- **充分性与公平性**：
  - **优势**：覆盖多种难度场景，消融设计清晰，验证了各组件贡献。
  - **不足**：
    - 所有结果为单次运行（作者提及约±1%变异，但未提供多种子统计）。
    - 基线结果引用自其他论文，存在协议差异（示范数量、图像分辨率、评估次数），并非严格头对头比较（论文已说明）。
    - 未进行超参数敏感度分析（如`T_M, ρ, β, C`）。
    - 真实世界评估仅在相同环境（即训练环境）内进行，未测试跨环境泛化。
    - 未详细报告推理延迟、控制频率、显存占用等效率指标。

### 6. 论文的主要结论与发现

- **核心结论**：ProgVLA (0.1B参数) 在LIBERO上平均成功率91.1%，超越OpenVLA (7B, 76.5%) 和 SmolVLA (2.25B, 88.75%)，尤其在长时域任务上领先11.6-34.9个百分点。在Meta-World上，ProgVLA平均78.5%，比SmolVLA 2.25B高10.3个百分点，在Hard和Very Hard层级提升显著。
- **消融发现**：后融合上下文重采样器是最大贡献者（移除后长时域成功率从88.6%降至51.2%）；视觉骨干微调为第二大贡献者（冻结DUNE导致平均降低13.5%）；进度感知训练提供额外2.3%增益，主要集中在Object和Long类别。
- **真实世界验证**：在10个任务上平均成功率68%，失败模式主要为遮挡、夹爪超时、抓错物体。

### 7. 优点（方法或实验设计亮点）

- **架构创新**：两阶段Perceiver重采样有效压缩多模态序列，同时保持跨模态对齐；进度头与策略共享上下文表示，不增加推理负担。
- **数据效率**：无需大规模机器人预训练，仅用目标基准的示范数据即可达到竞争性性能，降低了数据门槛。
- **实用导向**：模型体积极小（0.1B），可在便携设备上实时运行；真实世界部署无需架构改动。
- **实验设计**：在多个基准上验证，消融实验完整，并提供了真实世界失败模式分析，透明度高。

### 8. 不足与局限

- **实验覆盖**：
  - 基线对比存在协议差异，结论需谨慎解读。
  - 仅单次运行，缺乏统计置信区间。
  - 未进行跨环境泛化测试（真实世界训练和评估使用相同环境）。
  - 未分析进度头的内在有效性（如校准曲线、单调性验证），进度信号仅基于时间，而非语义。
- **方法局限**：
  - 进度目标`r_t`为纯时域函数，无法区分不同长度的任务阶段，对超出固定上限`T_M=500`的轨迹处理相同。
  - 辅助目标仅使用成功示范，无法利用失败数据；优势信号在严格离线RL意义上弱识别。
  - 未探索将进度头扩展到在线策略改进或不确定性估计。
- **应用限制**：
  - 真实世界失败模式包括抓错物体、夹爪超时，存在安全隐患，需人工监督。
  - 模型学习到的偏差（如偏好的抓取方式）可能从示范数据继承，需审计后方可用于安全关键部署。

---

（完）
