Title: Compliance for Free: Learning Identifiable Impedance via Bilateral Teleoperation

URL Source: https://arxiv.org/pdf/2609.19976v1

Published Time: Fri, 18 Sep 2026 00:57:35 GMT

Number of Pages: 8

Markdown Content:
# Compliance for Free: Learning Identifiable Impedance via Bilateral Teleoperation 

Harsha Guda, Adri` a Colom´ e, and Carme Torras 

Institut de Rob` otica i Inform` atica Industrial, CSIC-UPC 

Barcelona, Spain 

{hguda, acolome, torras }@iri.upc.edu 

Abstract —Vision-language-action models tell a robot where to move, but not how hard to push. Contact-rich tasks depend on that second quantity, compliance, yet no widely used demonstra-tion interface records it. The obstacle is identifiability as realized pose and measured force cannot separate the operator’s intended equilibrium from their stiffness, so VR controllers, SpaceMouse and handheld grippers cannot supply compliance supervision even in principle. Prior compliance-output policies work around this with hand-specified task structure, privileged simulation contact state, or dedicated force and tactile hardware. Four-channel bilateral teleoperation removes the ambiguity directly by using the leader arm as a separate measurement of the intended equilibrium, making per-axis stiffness identifiable by regression using only the joint-torque sensing already on the manipulator. This yields per-timestep, direction-dependent compliance labels at zero annotation cost, which we use to fine-tune a VLA to emit stiffness alongside pose. On a Franka Research 3 wiping task, ours is the only policy of five whose contact force changes when the instruction asks for a firm wipe rather than a normal one ( 6.4 N (normal) → 9.1 N (firm) RMS, Cohen’s d = 0 .89 ,

p = 0 .023 )

I. INTRODUCTION Vision-Language-Action (VLA) models have revolutionized robot manipulation [1]–[5], yet they consistently fail at the last millimeter. Contact-rich manipulation tasks such as wiping, tight tolerance insertion, and assembly require a robot to modulate how hard a robot pushes rather than just where it goes. In these scenarios, compliance is a first class action, every bit as critical as the target pose itself. The robotics community remains trapped in a data bottle-neck regarding this physical interaction. Recent models have attempted to close the gap by injecting contact force as an additional input observation via a force-aware mixture of experts [6] or hybrid control frameworks [7]. However, the few architectures that actively output force or compliance targets still lean heavily on structured constraints. They require hand-specified interaction frames [8], infer stiffness matrices from privileged simulation contact data [9], or distill policies from dedicated inference-time tactile hardware [10]. This barrier is not a failure of model architecture, but a fundamental mathematical flaw in the demonstration inter-faces. The standard setups driving modern visuomotor policy learning such as low-cost bimanual rigs [11], universal manip-ulation interfaces [12], or VR controllers [13] record only the 

> Generative AI (Claude Code Sonnet 5 High) was used to generate code. Project website and code at https://icra2027.github.io/free-compliance/

robot’s realized pose. The consequence is easiest to understand as a spring. A soft spring pulled a long way and a stiff spring pulled slightly exert exactly the same force. Watching only where the robot ended up and how hard it pushed, one cannot tell which of the two the operator intended, unless something independently records where they were aiming . Pose-only interfaces record no such signal, so the operator’s intended pose and their stiffness remain confounded. This is a strict identifiability problem, these interfaces have never measured the impedance the human intended, and mathematically, they never will. To resolve this ambiguity, we turn to four-channel bilateral control [14]. Unlike passive recording interfaces, a bilateral setup actively couples a leader and a follower arm. We demon-strate that the leader-arm trajectory serves as an independent, physical measurement of the operator’s intended equilibrium pose. By explicitly measuring what the human operator com-mands rather than just where the robot ends up, we collapse the equilibrium and stiffness ambiguity that has forced prior work into simulation. Critically, this resolution allows us to extract compliance without relying on dedicated force-torque or tactile sensors. Utilizing only the joint-torque sensing already present on the manipulator, we introduce a masked extraction procedure that yields per-timestep, anisotropic, real-world compliance labels. We subsequently use these labels to fine-tune a pretrained VLA to emit a stiffness target alongside each pose target. We validate on a whiteboard-wiping task instructed as “wipe the {left, right } mark {normally, firmly }”, so each demonstration carries both a spatial target, which we call the referent , and an instructed force character, which we call the manner .In summary, this paper makes the following contributions:  

> •

A sensorless extraction procedure that resolves the pose-only identifiability ambiguity via bilateral teleoperation, yielding per-timestep compliance labels using only native joint-torque sensors.  

> •

An offline benchmark showing that the labels carry learnable signal: a trained predictor beats a per-axis constant predictor on every translational axis, for a 1.25 ×

aggregate improvement.  

> •

A compliance-output VLA policy trained on these ex-tracted labels that safely tracks chunked impedance tar-gets and is the only policy of five whose realized contact force responds to the manner instruction. arXiv:2609.19976v1 [cs.RO] 17 Sep 2026 leader follower                

> bilateral leader– follower teleoperation
> xl(t)
> leader pose
> xf(t), τ meas
> follower + torque
> windowed regression
> →K(t), D (t), m (t)
> sensorless wrench + masked regression
> →per-axis compliance labels
> “wipe the left mark firmly”
> force history (500 ms)
> SmolVLM2 backbone SigLIP vision +
> SmolLM2 text
> frozen
> flow-matching
> action expert
> H=32 trained
> 500 ms
> →1 token
> xeq (6) log K(6)
> Post-VLM force token & six log Koutput dims is added to a frozen VLA
> Rollout with control on the manner of force — normal/firm

Fig. 1: From bilateral demonstrations to instruction-conditioned compliance. II. RELATED WORK 

A. Force-aware and compliance-output policies 

A large body of vision-language-action (VLA) policies such as RT-1 [1], RT-2 [2], Octo [3], π0 [4], and SmolVLA [5] learn a generalist policy mapping from vision and language to robot actions at scale. However, each of them only outputs pose, joint, or gripper targets. The stiffness for the low-level controller is fixed, not usually predicted by the policy. Recent attempts to close this gap have moved beyond fixed stiffness. Models like ForceVLA [6] and FD-VLA [15] incorporate contact force as an input feature, either via mixture-of-experts or cross-modal distillation, to make posi-tion outputs ’force-aware’. Meanwhile, hybrid frameworks like ForceVLA2 [7] predict target forces alongside position. How-ever, these methods either treat force as a rigid target rather than variable compliance, or require a dedicated force/torque (F/T) sensor at the wrist during both training and inference. Among policies that produce compliance without F/T hard-ware, each leans on structure this paper’s setting lacks. Force Policy [8] relies on a hand-specified interaction frame. Stiff-ness Copilot [9] infers direction-dependent stiffness matrices in simulation using privileged contact information. And recent approaches like PaCo-VLA [16] structure compliance via hand-designed task stages and semantic bindings. Although, HapticVLA [10] removes tactile sensing at inference by distill-ing from real tactile supervision, it still needs a tactile sensor at time of training. Our method bypasses these constraints by extracting pure, unrestricted impedance targets directly from human demonstrations. Bi-LAT [17] is closest in spirit, it also learns from bilateral-control demonstrations and modulates applied force by lan-guage (“softly grasp” vs. “strongly twist”), but it regresses joint torque directly rather than extracting an explicit, physi-cally interpretable stiffness label, so its notion of force control does not rest on an identifiability argument and cannot be reused to supervise a different backbone or task. None of these five requires zero dedicated force/tactile hardware at both training and inference time, and none derives compliance supervision from an identifiability argument applicable to any existing bilateral-teleoperation dataset, the gap this paper’s extraction procedure (Section III-B) and policy (Section III-F) fill. Closest to our setting, Xu et al. [18] also read impedance out of a bilateral leader–follower rig without force sensing, treating the mismatch between leader and follower response as an implicit encoding of interaction force, and cloning operator 

intent rather than realized motion. Their impedance remains implicit, no stiffness label is ever produced. Each of these workarounds substitutes for something the demonstrations never carried. The interfaces producing most VLA training data instrument only the robot’s realized trajec-tory, so the structure these methods import either from simula-tion, from a hand-specified frame, or from extra hardware, as a stand in for an equilibrium signal absent at collection time. Leader–follower rigs are not automatically exempt: ALOHA’s arms [11] are unilateral, rendering no contact force back to the operator, so the leader–follower deflection reflects the servo’s tracking error under its own gains rather than an operator yielding to felt contact. Bilateral coupling is what turns that deflection into a measurement. 

B. Bilateral Teleoperation and Control 

In a four-channel bilateral architecture, leader and follower exchange both position and force in both the directions: the follower tracks the operator’s hand, and the contact the follower feels is rendered back at the operator. This is a mature control technique, with stability and transparency thoroughly analyzed since Lawrence [14]. We leverage this theoretical maturity rather than developing bilateral control itself as a core contribution. Recent work has begun exploring bilateral setups for imita-tion learning data collection. Yamane et al. [19] build a sensor-less 4-channel framework using disturbance observers to esti-mate force for manipulators lacking joint-torque sensors, while Inami et al. [20] exploit position/force duality to retroactively edit recorded motions. Prometheus [21] introduces a mocap-based teleoperation system, but requires adding a dedicated compression force sensor at the gripper for operator feedback. Our approach diverges from this recent literature in two fundamental ways:  

> •

Unlike Prometheus [21], our wrench estimation and com-pliance labels utilize only the manipulator’s native joint-torque sensors, requiring zero additional force/torque hardware.  

> •

Unlike Yamane et al. [19], we do not merely estimate a scalar external force. We extract an explicit, interpretable per-axis stiffness matrix to supervise a compliance-output VLA policy. III. M ETHODOLOGY 

A. The Identifiability Problem 

An impedance law says the arm behaves like a spring and damper: the further it is displaced from where it is trying to be, the harder it pushes back, and the faster it is displaced, the more it resists. We model contact at the end-effector with a diagonal, time-varying Cartesian impedance law 

f (t) = K(t) e(t)+ D(t) ˙ e(t), e(t) = xeq (t)−xf (t), (1) where xf (t) is the measured follower pose and f (t) the measured contact wrench, the six-axis vector of forces and torques at the end-effector. K(t) = diag( k1, . . . , k 6) and 

D(t) = diag( d1, . . . , d 6) are stiffness and damping in the contact frame. The remaining term xeq (t) is the operator’s commanded equilibrium pose, where the arm is trying to be rather than where it actually is, and it is the one quantity in Eq. (1) that a pose-only interface never records. That omission is a strict identifiability failure, not a data shortage. Take a single axis i and drop damping for a moment, so fi = kiei. For any scale α > 0 the pair (αk i, e i/α )

reproduces the observed fi exactly, and since xif is measured, adopting the displacement ei/α just amounts to asserting the operator aimed at xi 

> eq

= xif + ei/α . Every positive stiffness is therefore consistent with the recording under some intended equilibrium. For a pose-only interface, then, the demonstrator’s intended equilibrium and the robot’s realized pose are the same recorded number, so no volume of such data recovers K after the fact. Doing so requires a separate measurement of xeq .

B. Resolving Identifiability via Bilateral Teleoperation 

Four-channel bilateral teleoperation fundamentally alters this paradigm. By actively coupling a leader and follower arm, the system records a second, separately instrumented pose stream that the follower’s own state does not determine, and we take that stream as the operator’s commanded equilibrium: 

xeq (t) := xl(t), the leader pose. Substituting it collapses the ambiguity, e(t) = xl(t) − xf (t) is now directly observed, and 

(K(t), D(t)) become identifiable by regression on (e, ˙e, f ).In other terms, we can now see both how far the spring was pulled and how hard it pulled back, which fixes its stiffness. This holds only where the data is in the direction of the axis. If the arm barely deflects, or barely pushes, the regression has nothing to work with. The identifiability mask defined below makes that requirement explicit, per axis and per timestep. 

C. Compliance label extraction Contact-frame definition: Stiffness is only meaningful rela-tive to a frame, so we first recover the geometry of the contact. Wiping strokes sweep out a plane. The one direction they never explore is the direction pointing out of the board. The smallest singular vector of the contact positions therefore recovers the surface normal, and the ratio of smallest to largest singular value measures how plane-like the motion really was. We fit a contact frame for each demonstration, or pooled across a session when the physical contact surface is fixed throughout. Given a set of in-contact follower positions pj ∈

R3 with mean ¯p, we take the singular value decomposition of the centered positions {pj − ¯p} and set ˆn = v3, the right singular vector associated with the smallest singular value σ3,with planarity = σ3/σ 1. A fit is rejected when planarity >

0.15 , i.e. when the motion was not planar enough to define a surface. 

Windowed regression for (k, d ): For each short slice of the demonstration we ask which spring-and-damper pair best explains the force measured over that slice. Concretely, for each axis i, we estimate (ki, d i) over a sliding 300ms window directly at the 1kHz control rate by solving a regularized, box-constrained nonlinear least-squares problem: 

min 

> ki,d i

X

> t∈W

(fi(t) − kiei(t) − di ˙ei(t)) 2 +λ∥ log ki −log kprior i ∥2

(2) subject to ki ∈ [kmin , k max ] and di ≥ 0. Because the regularizer is nonlinear in ki, this does not admit a closed-form ridge solution and is solved in log-space. The bounds 

[kmin , k max ] are strictly defined by the realistic safety limits of the physical Cartesian impedance controller. We use critically damped D values derived from K (di = 2 √ki). 

Identifiability mask: A regression will always return some 

answer, even where the data cannot support one. We therefore keep a label only where it could genuinely have been revealed that the arm must be in contact, deflected far enough for the displacement to mean something, pushing harder than the sensor’s noise, and in a configuration where the regression is numerically well conditioned. For each axis i and timestep t,a binary validity mask is defined as follows, 

mi(t) = 



1 if κ(J⊤ 

> W

JW ) < κ max , |ei(t)| > σ e,

|fi(t)| > σ f,i , and contact( t),

0 otherwise. (3) combining, in that order, a regression-conditioning term ( κ

is the condition number of the window’s regressor matrix, large when the two columns are nearly collinear and the fit is unstable), a minimum-displacement term, a per-axis force-above-noise-floor term (from σf,i by Eq. (6) ), and a contact indicator. Figure 2b shows per-axis mask coverage, computed within in-contact timesteps excluding free-space and reset time motion. 

D. Wrench Estimation 

Everything above assumes we can measure the contact wrench f . We obtain it without a force sensor, from a simple observation. If a joint needs more torque than gravity alone would account for, something external must be pushing on the tool. Turning that into a calibrated wrench takes two steps — identifying the tool’s own weight, so it is not mistaken for contact, and then modelling what residual error remains. At static poses, ( ˙ q = ¨ q = 0), the residual between measured joint torque and the arm’s zero-payload gravity model is linear in the unknown payload’s mass and mass-weighted center of mass: (a) Extracted stiffness traces K(t) throughout in-contact phase.      

> (b) Identifiability mask coverage, per axis, dataset-wide, broken out by manner.
> (c) Sensorless wrench noise floor σf,i
> (Eq. (6)), measured from free-space residuals on held-out sweep sessions.

Fig. 2: Dataset validity, dataset-wide. Together these establish that the extracted compliance labels are identifiable with real margin (a), retained at high coverage after masking (b), and measured against a real, held-out noise floor well below the labels’ working range rather than assumed or hand-tuned (c). Hatched bars mark axes where the bias-corrected estimate did not improve over the raw RMS ( fx, τy , τz ), for which σf,i is the raw value; this is a free-space noise floor. 

∆τ (q) = τ meas (q) − τ grav ,0(q) = A(q)



mmc x

mc y

mc z

 , (4) where A(q) ∈ R7×4 is a gravity regressor. We solve it by least squares over 15 static poses, in the sign convention in which a positive m is an added mass pulling down. This is the same identifiability logic as Sections III-A, III-B. Immediately before every session we sample the wrench bias at a fixed free-space pose and subtract it as a per-axis constant offset for the remainder of the session. What remains after that offset is a slowly varying error that depends on where the arm is and how fast it is moving. We learn it from free-space sweeps, where by construction there is no contact and any reported force is error. Projecting the input through a bank of Df random Fourier features φ(x) [22] makes a flexible nonlinear fit available in closed form, at a fraction of the cost of the Gaussian-process regression it approximates: 

fbias (x) ≈ Wφ(x), W = argmin 

> W

∥F−WΦ ∥2+λ∥W∥2,

(5) solved in closed form and fit independently per axis on x =(q, ˙q) ∈ R14 , with Df = 512 features, length scale ℓ = 30 

and ridge penalty λ = 0 .1.Because estimation error is strongly configuration-dependent, we pin a fixed nullspace — a seven-joint arm can hold the same tool pose with many different elbow configurations, and we always choose the same one —posture and constrain the working volume during both calibration and demonstration collection. We define the per-axis noise floor, evaluated on held-out free-space sweep sessions, as 

σf,i = min 



RMS raw  

> i

, RMS bias -corrected 

> i



, (6) i.e. the bias-corrected estimate is used only on axes where it measurably improves over the raw estimate. σf,i is used by mask condition (iii) in Eq. (3). 

E. Label-quality validation: Offline compliance prediction benchmark 

This benchmark tests whether the extracted labels carry any meaningful signal. The task is to predict log Ki(t), at masked identifiable timesteps only, from a 20 -dimensional feature vec-tor xt = [ qt, ˙qt, manner , referent ] that deliberately excludes force, so that the check cannot be circular. We compare two predictors: a constant baseline, the per-axis training-split mean of log ki, and a learned one, the random-Fourier-feature ridge model of Eq. (5) reused here for a second, distinct regression target. We report per-axis RMSE on log K over the masked test set, aggregated by averaging across axes. Taken together, Sections III-B–III-D turn one recorded bilateral demonstration into a pair of arrays at the policy’s own 30 Hz action rate: log K ∈ RN ×6, expressed in that episode’s fitted contact frame, and a Boolean identifiability mask of the same shape marking which of those entries the data actually supports. Each row is indexed to the raw frame nearest its window, so every label lines up with the image and proprioception the policy conditions on. 

F. Compliance-output Policy 

Backbone. SmolVLA [5] ( ∼ 450 M parameters from LeR-obot), with the pretrained vision-language model frozen and the action expert fine-tuned. 

Inputs. We utilize two 224 × 224 × 3 RGB streams: a scene camera capturing the global workspace and a rigidly mounted wrist camera. A language instruction of “wipe the 

{referent } mark {manner }”, where referent ∈ { left, right }

and manner ∈ { normally, firmly }. We identify the target by position (“left”/“right”) rather than by color, because the red and blue marks in our demonstrations occupy very few pixels and the model could not reliably tell them apart. A force-history token, a trailing 500ms of 6-DoF wrench downsampled to 20 samples (encoded by a 1D-convolutional network) is injected post-VLM (following the ForceVLA [6]). 

Output. The policy outputs an action chunk of H = 32 

future timesteps predicted in one forward pass, rather than a single next step, each carrying [xeq (6) , log K(6)] . xeq (6) is an absolute target pose in the same base frame convention as xf . We predict absolute rather than relative poses because the identifiability argument of this paper depends on xeq being directly comparable to an absolute pose. 

Loss. Flow-matching (L1) on xeq , and on log K a Huber loss [23] — quadratic near zero and linear in the tails, so occasional badly fit windows do not dominate the gradient — applied only at timesteps the mask marks identifiable, at a weight tuned on the validation split. 

Augmentation. Force dropout ( p = 0.15 ) and random wrench-bias injection ( ±1 N) during training, to guard against modal masking, a failure mode in which the high-dimensional visual stream dominates optimization and low-dimensional force is silently ignored despite being available during training. 

Training. All policies are fine-tuned identically: 2500 steps, batch size 64 , AdamW at a constant 10 −4 (β = (0 .9, 0.95) ,weight decay 10 −10 , gradient-norm clip 10 ), three seeds, 

99 .9M trainable parameters of 450 .1M. Proprioception is 

[q (7) , ˙q (7) , xf (6)] . Trained on a single NVIDIA L40S. 

G. Low-level controller 

The Franka Research 3 follower arm is driven at 1 kHz by a Cartesian impedance controller, built on the CRISP ROS 2 compliant-controller stack [24], 

τ = J⊤Kt(xeq −x)+ Dt( ˙ xeq − ˙x)+C(q, ˙q) ˙ q+τ null , (7) The controller tracks the policy’s chunked (xeq (t), K(t)) 

output at 1 kHz while the policy replans at ∼10 Hz (taking 3 actions per chunk to execute). 

Temporal ensembling. Successive chunks are inferred in-dependently, so nothing forces the end of one to line up with the start of the next. Discarding the previous chunk each time a new one arrives creating a discontinuity that a rigid, non-compliant policy cannot physically absorb, and which stalls trajectory execution. We use ACT’s temporal ensembling [11] unchanged, combining every buffered chunk’s prediction for a given low-level step by exponential recency weighting, 

ˆa(t) = 

P 

> i

wi ai(t)

P 

> i

wi

, wi = exp( −m · age i(t)) , (8) where ai(t) is chunk i’s prediction for step t and age i(t) is the number of low-level steps since chunk i was queried, so the freshest available prediction for a given step dominates, while older overlapping predictions still smooth the boundary rather than being discarded outright. Position, orientation (as a rotation vector), and log K are all blended this way. m is tuned empirically, not assumed correct a priori. 

Stiffness rate limiting. Piecewise-constant K arriving once per chunk injects energy at the boundary and can make the arm buzz or trip a protective stop, so we rate-limit in log-space, |d log K/dt| ≤ γ, with a final tuned γ = 3 s −1 on all six axes. 

Passivity. Raising K injects energy, so stiffness increases 

are gated by a standard energy tank [25], [26] ( E0 = 1 J, 

Emax = 5 J); decreases are always permitted. TABLE I: Policies compared. All five share demonstrations, low-level controller, seeds and replanning settings. They differ only in the inputs and outputs.                                                        

> Policy Force in Action output Basis Fixed-K —pose hand-set Ka
> Fitted-K —pose Kfit to our labels b
> Force-In ✓pose ForceVLA [6] Hybrid ✓pose + log KcForceVLA2 [7], [8]
> Ours ✓pose + log Kdextracted labels
> a(300 ,300 ,300) N/m, (20 ,20 ,20) N·m/rad — a conventional, well-posed constant impedance.
> bPer-axis constants best fitting our label distribution (Sec. IV-G).
> cPose decoded by flow matching, log Kby a separate one-shot linear regression head on the same suffix representation — the generative-pose / direct-regression-control split these systems use.
> dPose and log Kdecoded through asingle shared flow-matching target, split only at the loss. This is what separates Ours from Hybrid.

IV. EXPERIMENTS AND RESULTS 

A. Experimental Setup 

All policies are evaluated on a whiteboard wiping task while the board is at ∼45 ◦ from vertical, with a rigid eraser mounted to the end-effector of the manipulator, over a 2 × 2 instruction grid “wipe the {left, right } mark {normally, firmly }”, n = 8 

rollouts per cell, three training seeds, 24 rollouts per policy. Every rollout runs to a fixed 200 low-level steps ( ≈43 .5 s); the policy replans every three executed chunk steps at a 30 Hz action rate ( ≈10 Hz replanning), with temporal ensembling (Eq. (8), m = 0 .05 ) enabled identically for all policies. We report three quantities. Success is ink removal ≥ 50% , scored automatically by pixel difference from a fixed scene camera, so wiping the wrong mark is penalized rather than credited. Ink removal is that same percentage, continuous. Contact force 

is peak and RMS from the estimated wrench. Prot. Stops is a 

proxy for a protective stop when peak estimated contact force is above a 40 N safety threshold. Table I lists the five policies compared. Before evaluating policies we verify the controller responds to a commanded stiffness at all. Fig. 4 shows commanded vs. applied stiffness under a 20 s sinusoidal probe on all six axes together with the energy-tank trace, on the physical follower arm. Because all force metrics come from the same estimator on the same hardware, tool, payload and workspace, its bias is 

common-mode across policies. The relative comparisons are valid, absolute values carry an offset, and we report all such quantities as estimated wrench. 

B. Instruction-conditioned compliance 

The paper’s central claim is that compliance extracted from bilateral demonstrations is a learnable, instruction-conditioned action . We test it as a three-link chain, each link measured Fig. 3: Qualitative rollouts of our policy, scene camera (top sub-row) and wrist camera (bottom sub-row), across the 2 ×2referent ×manner instruction grid (“wipe the left/right mark firmly/normally”), five phases per rollout (approach, pre-contact, contact, wiping, retract). Each cell shows one representative rollout ( n = 1 ). Aggregate success rates across all rollouts are reported in Table IV. TABLE II: Instruction-conditioned compliance, measured end to end. Each link is a separate measurement on the same manner contrast; d is Cohen’s d and p is a two-sided Mann– Whitney U test. Stiffness rows are geometric means and their 

d is computed on log Kx. The force row is in linear units. Units: Kx in N/m, force in N.                                         

> Link firmly normally dp
> Demonstrated Kxa146.2 127.2 0.76 0.002 Commanded Kxb141.2 116.4 0.98 0.019 Realized force c9.12 6.40 0.89 0.023
> aGeometric mean of masked Kx: per episode, exp of the mean of masked log Kx; then pooled the same way across episodes. Primary operator’s demonstrations, n=48
> firmly and 39 normally episodes of the 96 collected (Sec. IV-F).
> bSame statistic on the policy’s own log Kxoutput over executed chunk steps ( n= 12 rollouts/manner, 3 seeds pooled).
> cPer-rollout RMS estimated contact force ( n= 12 roll-outs/manner, 3 seeds pooled).

separately on the manner contrast ( firmly vs. normally ) along the in-plane contact axis fx (Table II). All three links carry the instruction. The demonstrations en-code the adverb in the in-plane stiffness ( d = 0 .76 ); the policy reproduces that dependence in its own output ( d = 0 .98 ); and the dependence survives all the way to the arm’s realized contact force ( d = 0 .89 ). The effect appears on the in-plane axes and not on the board normal ( fz label d = 0 .05 ), which is what the physics predicts: against a rigid board, pressing harder principally moves the equilibrium penetration depth 

xeq , whereas wiping firmly manifests as increased resistance to being deflected off the stroke path. Two properties of the commanded stiffness are worth stat-ing, as both are free of any rollout-budget confound. First, the policy commits to genuine anisotropy, that it is stiff along some directions and soft along others, rather than uniformly stiff: median commanded K = (126 .2, 83 .0, 209 .1) N/m in TABLE III: Does the manner word change what the arm actually does? Per-rollout RMS estimated contact force (N), 

n = 12 per manner per policy; d is Cohen’s d and p atwo-sided Mann–Whitney U test, as in Table II. Only the compliance-output policy responds.                         

> Policy firmly normally dp
> Fixed-K 10.69 11.20 −0.22 0.977 Fitted-K 6.78 6.23 0.24 0.214 Force-In 13.23 12.58 0.10 0.436 Hybrid 7.40 6.87 0.17 0.507
> Ours 9.12 6.40 0.89 0.023

the contact frame, a per-timestep in-plane/normal ratio of 2.50 

(median; 6.63 at the 95 th percentile), against only ≈1.2× in the pooled marginal label distribution. The policy is therefore not regressing to the label mean, a constant predictor could not produce this. Second, the effect is specific to compliance out-put : Table III shows that no baseline modulates realized force with the instruction, including Force-In, which consumes force as an input, and Hybrid, which does move its commanded K

with manner ( d = 2 .26 on fy ) but whose realized force does not follow ( d = 0 .17 ). Predicting compliance is not sufficient; the rest of the policy has to track it. 

C. Task performance 

Table IV reports in-distribution performance for all five policies; Fig. 3 shows representative rollouts of our policy across the instruction grid. Contact-force behavior is more informative than success. Our policy attains the highest ink removal at a peak force 

8.6 N lower and an RMS force 3.1 N lower than Fixed-K, and trips one protective stop in 24 rollouts against Fixed-K’s six. Hybrid is gentler still but removes less than half as much ink: the interesting axis is task progress at matched force , and on that axis compliance output dominates both the fixed-stiffness and the force-input position policies. TABLE IV: In-distribution task performance and contact-force behavior ( n = 24 per policy, 3 seeds).                                                             

> Policy Success Ink rem. (%) Peak (N) RMS (N) Prot. Stops Fixed-K 33.3% 32.6 ±34.9 34.9 ±14.0 10.9 ±2.2 6/24 Fitted-K 0.0% 1.3 ±3.0 21.4 ±7.2 6.5 ±2.3 0/24 Force-In 41.7% 37.3 ±31.5 32.3 ±9.0 12.9 ±6.5 2/24 Hybrid 20.8% 18.4 ±28.8 19.6 ±8.2 7.1 ±3.1 0/24
> Ours 50.0% 40.8 ±35.3 26.3 ±9.1 7.8 ±3.3 1/24

TABLE V: Causal force ablation. Force history frozen at its first-observed value for the rest of the episode, vs. the live condition (Table IV). Fixed-K and Fitted-K are excluded — freezing is a no-op for both.                

> Policy Live Frozen ∆
> Force-In 41.7% (10/24) 35.7% (5/14) −6.0pts Hybrid 20.8% ( 5/24) 16.7% (2/12) −4.2pts Ours 50.0% (12/24) 41.7% (5/12) −8.3pts

D. Is the force channel used? 

A policy can be given force and still ignore it. We freeze the force-history token at its first-observed value for the remainder of the episode and re-run the three policies that consume force; freezing is a no-op for Fixed-K and Fitted-K. Success falls for all three, but no drop is significant (Table V). The result is inconclusive rather than negative. The three policies degrade almost identically in relative terms, so our policy’s larger absolute drop follows from its higher baseline rather than from any greater reliance on force. The paper’s central claim rests on Tables II and III, not on this ablation. 

E. Offline compliance-prediction benchmark 

A learned predictor beats a per-axis constant predictor on a held-out session on all three translational axes, for a 1.25 ×

aggregate improvement in log K RMSE ( 1.39 × on fx, 1.03 ×

on fy , 1.41 × on fz ). The rotational axes are excluded from the aggregate: their targets are box-saturated at the lower bound (Sec. VI), so their RMSE is small for reasons that carry no signal. 

F. Label validity on real hardware 

Applied to the primary operator’s demonstrations, the iden-tifiability mask retains 84.3–96.2% of in-contact timesteps on the three translational axes, and 71.5–82.3% on the rotational axes (Fig. 2b). The per-axis noise floor σf,i that the mask’s SNR condition consumes (Eq. (6)) is measured on held-out free-space sweep sessions and lies well below the labels’ working range on every axis (Fig. 2c). Extracted stiffness traces with phase annotation are shown in Fig. 2a. 

G. Constant-stiffness fit (Fitted-K) 

To place the label distribution in physically interpretable units we grid-search, per axis, the single constant stiffness minimizing validation RMSE. This is a property of the dataset ,

Fig. 4: Commanded vs. realized stiffness under a 20s si-nusoidal probe across all six axes, with the accompanying energy-tank trace, recorded on the physical follower arm. not of a trained policy. The three translational constants ( 141 .1,

149 .5 and 158 .4 N/m for fx, fy , fz ) are unremarkable and lie well inside the controller’s realizable [50 , 1500] N/m range. All three rotational constants, however, land on the extraction’s 

5 N·m/rad lower bound, for the reason given in Sec. VI. The rotational impedance present in these demonstrations is softer than the controller’s minimum commandable rotational stiffness. This is why Fitted-K must not be read as a tuned-impedance oracle. Deployed, its rotational constants leave the tool unable to hold orientation against the board, which accounts for its 

0% success rather than any property of constant impedance as such. The constant-impedance control in this study is Fixed-K, whose hand-set (300 , 300 , 300 , 20 , 20 , 20) is well posed and which reaches 33 .3% . We therefore do not claim that a well-tuned constant impedance is inadequate for this task; we claim, and Table III shows, that it cannot modulate force on instruction. V. DISCUSSION Observing force and acting compliantly are different capa-bilities. The ForceVLA-style baseline sees the contact wrench at every timestep, yet produces rigid position targets, leaving one way to change contact against an unyielding board is to drive the equilibrium deeper. It records the highest RMS force of any policy, 12 .9 N, without removing more ink than ours. Fixed-K, with no force channel, meets the board with a constant that cannot yield and trips six protective stops in 

24 rollouts, where in our case modulating K holds contact at 

8.6 N lower peak force with one stop. Predicting compliance is not by itself enough,although Hybrid changes its commanded stiffness with the instruction more strongly than we do, but decodes it through a regression head separate from its pose, and the two never co-vary at execution, so its realized force does not follow. Decoding both through one shared target is what closes that gap, and our policy commits to genuine anisotropy as a result, a median in-plane to normal ratio of 

2.50 against roughly 1.2 in the pooled labels. Task success separates the policies far less cleanly, 50 .0% against Force-In’s 41 .7% is two rollouts in 24 , and a well-posed Fixed-K still completes the task a third of the time. What survives the small sample is categorical rather than quantitative. Ours is the only policy whose realized contact force follows the language instruction (Table III), the policy learned that a firm wipe resists deflection off the stroke, not that it presses harder. VI. L IMITATIONS 

Sensorless wrench estimation is configuration-dependent, so we pin a fixed nullspace posture and constrain the working volume during calibration and collection. A wrist F/T sensor would remove that constraint at the cost of bulk, wiring and expense. The rotational channels are weakly identified as our wiping task barely excites orientation, and the rotational impedance the operators used is softer than our controller’s minimum commandable stiffness, so τx, τy and τz saturate at the lower bound and should be read as “at least this soft” rather than as calibrated values. That reflects this task and this controller’s realizable range, not the identifiability argument, which holds per axis wherever the axis is excited. The evidence is also narrow (one task, one platform) and the rollout budget is small enough that the force-channel ablation (Sec. IV-D) is underpowered by more than an order of magnitude, leaving the causal role of the force input open. VII. CONCLUSION Pose-only teleoperation cannot supply compliance super-vision, because realized pose and measured force leave the operator’s intended equilibrium and their stiffness confounded. Four-channel bilateral teleoperation breaks the tie as the leader arm measures the intended equilibrium directly, which makes per-axis stiffness identifiable from demonstrations that are already being collected, with no force-torque hardware and no additional annotation. The resulting labels are learnable enough to drive a VLA policy whose contact force responds to a language instruction, which no baseline here achieves. Extending the extraction to dynamic, impulsive contact, and to cheaper embodiments that lack joint-torque sensing, is left to future work. REFERENCES [1] A. Brohan, N. Brown, J. Carbajal, Y. Chebotar, J. Dabis, C. Finn et al. ,“Rt-1: Robotics transformer for real-world control at scale,” in Robotics: Science and Systems (RSS) , 2023. [2] B. Zitkovich, T. Yu, S. Xu, P. Xu, T. Xiao, Q. Vuong et al. , “Rt-2: Vision-language-action models transfer web knowledge to robotic control,” in 

Proceedings of The 7th Conference on Robot Learning , vol. 229, 2023, pp. 2165–2183. [3] Octo Model Team, D. Ghosh et al. , “Octo: An open-source generalist robot policy,” in Robotics: Science and Systems (RSS) , 2024. [4] K. Black, N. Brown, D. Driess, A. Esmail, M. Equi et al. , “ π0: A vision-language-action flow model for general robot control,” in Robotics: Science and Systems (RSS) , 2025. [5] M. Shukor, D. Aubakirova, F. Capuano, P. Kooijmans, S. Palma, A. Zouitine et al. , “Smolvla: A vision-language-action model for af-fordable and efficient robotics,” arXiv preprint arXiv:2506.01844 , 2025. [6] J. Yu, H. Liu, Q. Yu, J. Ren, C. Hao, H. Ding, and et al, “Forcevla: Enhancing vla models with a force-aware moe for contact-rich manip-ulation,” in Proceedings of the 39th Conference on Neural Information Processing Systems (NeurIPS 2025) , 2025. [7] Y. Li, Z. Zhaxizhuoma, H. Jiang, J. Xia, H. Zhang, J. Du et al. ,“Forcevla2: Unleashing hybrid force-position control with force aware-ness for contact-rich manipulation,” in Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition , 2026, pp. 8911–8920. [8] H. Fang, S. Tang, M. Mei, H. Qin, Z. He, J. Chen et al. , “Force policy: Learning hybrid force-position control policy under interaction frame for contact-rich manipulation,” arXiv preprint arXiv:2602.22088 , 2026. [9] Y. Wang, Z. Xu, P. Preechayasomboon, B. Abbatematteo, A. H. Memar, N. Colonnese, and S. Chan, “Stiffness copilot: An impedance policy for contact-rich teleoperation,” in IEEE/RSJ International Conference on Intelligent Robots and Systems (IROS) , 2026. [10] K. Gubernatorov, M. Sannikov, I. Mikhalchuk, E. Kuznetsov, M. Arte-mov, O. Ouwatobi et al. , “Hapticvla: Contact-rich manipulation via vision-language-action model without inference-time tactile sensing,” in 

IEEE/RSJ International Conference on Intelligent Robots and Systems (IROS) , 2026. [11] T. Z. Zhao, V. Kumar, S. Levine, and C. Finn, “Learning fine-grained bimanual manipulation with low-cost hardware,” in Robotics: Science and Systems (RSS) , 2023. [12] C. Chi, Z. Xu, C. Pan, E. Cousineau, B. Burchfiel, S. Feng, R. Tedrake, and S. Song, “Universal manipulation interface: In-the-wild robot teach-ing without in-the-wild robots,” in Robotics: Science and Systems (RSS) ,2024. [13] A. Khazatsky, K. Pertsch et al. , “Droid: A large-scale in-the-wild robot manipulation dataset,” in Robotics: Science and Systems (RSS) , 2024. [14] D. A. Lawrence, “Stability and transparency in bilateral teleoperation,” 

IEEE Transactions on Robotics and Automation , vol. 9, no. 5, pp. 624– 637, 1993. [15] R. Zhao, W. Wang, Y. Ma, X. Li, F. E. Tay, M. H. Ang Jr, and H. Zhu, “Fd-vla: Force-distilled vision-language-action model for contact-rich manipulation,” arXiv preprint arXiv:2602.02142 , 2026. [16] H. Cao, Z. Li, Z. You, L. Guo, and T. Li, “Paco-vla: Passivity-shielded compliance prior for contact-rich vision-language-action manipulation,” 

arXiv preprint arXiv:2606.00515 , 2026. [17] T. Kobayashi, M. Kobayashi, T. Buamanee, and Y. Uranishi, “Bi-lat: Bilateral control-based imitation learning via natural language and action chunking with transformers,” in 2025 34th IEEE International Conference on Robot and Human Interactive Communication (RO-MAN) . IEEE, 2025, pp. 1609–1616. [18] C. Xu, S. Zheng, Z. Su, Y. Xu, T. Yi, X. Zhang, J. Wang, Y. Wang, and J. Yu, “Mind the gap: Learning implicit impedance in visuomotor poli-cies via intent-execution mismatch,” arXiv preprint arXiv:2602.08776 ,2026. [19] K. Yamane, Y. Li, M. Konosu, K. Inami, J. Oaki, T. Tsuji, and S. Sakaino, “Design and experimental validation of sensorless 4-channel bilateral teleoperation for low-cost manipulators,” IEEE Access , 2026. [20] K. Inami, S. Sakaino, and T. Tsuji, “Motion retouch: Motion modifi-cation using four-channel bilateral control,” in 2025 IEEE International Conference on Mechatronics (ICM) . IEEE, 2025, pp. 1–6. [21] S. Satsevich, A. Bazhenov, S. Egorov, A. Erkhov, M. Gromakov, A. Fedoseev, and D. Tsetserukou, “Prometheus: Universal, open-source mocap-based teleoperation system with force feedback for dataset col-lection in robot learning,” in 2025 IEEE International Conference on Robotics and Biomimetics (ROBIO) . IEEE, 2025, pp. 1578–1583. [22] A. Rahimi and B. Recht, “Random features for large-scale kernel machines,” in Advances in Neural Information Processing Systems (NeurIPS) , vol. 20, 2007. [23] P. J. Huber, “Robust estimation of a location parameter,” in Break-throughs in statistics: Methodology and distribution . Springer, 1992, pp. 492–518. [24] D. S. J. Pro, O. Hausd¨ orfer, R. R¨ omer, M. D¨ osch, M. Schuck, and A. P. Schoellig, “Crisp-compliant ros2 controllers for learning-based manipulation policies and teleoperation,” IEEE Robotics and Automation Practice , 2026. [25] F. Ferraguti, C. Secchi, and C. Fantuzzi, “A tank-based approach to impedance control with variable stiffness,” in 2013 IEEE international conference on robotics and automation . IEEE, 2013, pp. 4948–4953. [26] C. Schindlbeck and S. Haddadin, “Unified passivity-based cartesian force/impedance control for rigid and flexible joint robots via task-energy tanks,” in 2015 IEEE international conference on robotics and automation (ICRA) . IEEE, 2015, pp. 440–447.