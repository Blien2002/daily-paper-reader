---
title: "IMPACT-VLA: Interaction-aware Multimodal Propagation Attribution via Counterfactual Trajectories for Vision-Language-Action Policies"
title_zh: IMPACT-VLA：面向视觉-语言-动作策略的基于反事实轨迹的交互感知多模态传播归因
authors: "Jinwoong Kim, Sangjin Park"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15005v1"
tags: ["query:vla"]
score: 9.0
evidence: VLA策略中多模态贡献的归因分析
tldr: "现有归因方法难以揭示VLA策略中多模态输入在哪个执行阶段发挥关键作用及跨阶段依赖。IMPACT-VLA从成功轨迹构建行为阶段，定义阶段-模态块为归因单元，通过闭环反事实重执行量化贡献。在30个LIBERO任务中，83.3%出现主导模态转移，闭环归因比静态扰动更准确识别任务关键信息。该方法揭示多模态贡献的条件耦合，为理解VLA策略提供了新框架。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.15005v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 825, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15005v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 644, \"height\": 806, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15005v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 733, \"height\": 934, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15005v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 613, \"height\": 465, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15005v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 597, \"height\": 438, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.15005v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 819, \"height\": 423, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15005v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 511, \"height\": 162, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15005v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 680, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15005v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 804, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15005v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 760, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15005v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 853, \"height\": 181, \"label\": \"Table\"}]"
motivation: 现有归因方法仅测量局部敏感性或时间聚合重要性，无法捕捉VLA策略中多模态贡献的阶段性变化及跨阶段依赖。
method: IMPACT-VLA从成功参考轨迹构建行为阶段并与查询边界对齐，将阶段-模态块作为归因单元，通过闭环反事实重执行量化贡献。
result: "在30个LIBERO任务上，83.3%出现主导模态转换，闭环归因比静态动作扰动更准确，早期输入替换使负交互对后期增益提高约3.3倍。"
conclusion: 研究表明多模态输入的贡献在闭环执行中阶段依赖且条件耦合，功能恢复可独立于行为恢复发生，为可靠归因提供了新视角。
---

## 摘要
视觉-语言-动作（VLA）策略利用视觉观测、本体感受状态和语言指令等多模态输入执行机器人操作任务。然而，目前尚不清楚每种模态在哪些执行阶段对最终任务成功有所贡献，以及输入干预如何通过后续状态、观测和动作传播。现有归因方法主要衡量局部敏感性或时间聚合重要性，限制了它们捕捉阶段依赖性贡献和跨阶段依赖的能力。我们提出了IMPACT-VLA，即通过反事实轨迹实现的交互感知多模态传播归因方法。IMPACT-VLA从成功参考推演中的动作转换构建行为阶段，将其与策略查询边界对齐，并定义阶段-模态块作为归因单元。然后进行闭环反事实重新执行，以量化每个块对最终任务成功的贡献。我们进一步分析了跨阶段非加性交互和轨迹传播，同时区分行为恢复与功能恢复。在使用OpenVLA-OFT的30个LIBERO机器人操作任务中，主导模态转换发生在25个任务中（83.3%），闭环归因比静态动作扰动（Static Action Perturbation）更忠实地识别了任务关键信息。在早期阶段输入替换下，负交互对的后续块边际增益增加了约3.3倍，而功能恢复可以在没有行为恢复的情况下发生。这些结果揭示了多模态输入何时支持任务成功，以及它们的贡献在闭环执行过程中如何条件性耦合。

## Abstract
Vision-Language-Action (VLA) policies perform robot manipulation tasks using multimodal inputs such as visual observations, proprioceptive states, and language instructions. However, it remains unclear at which execution stages each modality contributes to final task success and how input interventions propagate through subsequent states, observations, and actions. Existing attribution approaches primarily measure local sensitivity or temporally aggregated importance, limiting their ability to capture phase-dependent contributions and cross-phase dependencies. We propose Interaction-aware Multimodal Propagation Attribution via Counterfactual Trajectories for Vision-Language-Action Policies (IMPACT-VLA). IMPACT-VLA constructs behavioral phases from action transitions in a successful reference rollout, aligns them with policy query boundaries, and defines phase-modality blocks as attribution units. It then performs closed-loop counterfactual re-execution to quantify each block's contribution to final task success. We further analyze cross-phase non-additive interactions and trajectory propagation while distinguishing behavioral from functional recovery. Across 30 LIBERO robot manipulation tasks using OpenVLA-OFT, dominant-modality transitions occurred in 25 tasks (83.3%), and closed-loop attribution identified task-critical information more faithfully than Static Action Perturbation. Later-block marginal gains for negatively interacting pairs increased by approximately 3.3x under early-phase input replacement, while functional recovery could occur without behavioral recovery. These results reveal when multimodal inputs support task success and how their contributions become conditionally coupled during closed-loop execution.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景**：Vision-Language-Action（VLA）策略在机器人操作中依赖视觉观测、本体感受状态和语言指令等多模态输入，但不同模态在任务执行的不同阶段如何贡献、输入干预如何通过后续状态和动作传播，仍不清楚。
- **现有方法局限**：已有归因方法主要衡量模型预测的局部敏感性或整个时间范围内的聚合重要性，难以刻画“阶段依赖性贡献”和“跨阶段依赖关系”；特别是对于 chunk-based VLA，一个查询会生成多个未来动作，时间步级分析不一定对应真实的干预单元。
- **整体含义**：论文提出 IMPACT-VLA，把行为阶段与策略查询/动作块边界对齐，构造“阶段-模态块”作为归因单元，并通过闭环反事实重执行来评估每个块对最终任务成功的影响；同时分析跨相位交互、轨迹传播，并区分行为恢复与功能恢复，从而更完整地理解多模态信息在闭环执行中的条件和时序作用。

## 2. 论文提出的方法论

- **行为阶段构建**：
  - 基于成功参考 rollout 中实际执行动作的变化，用 CUSUM 统计量检测动作转变点。
  - 每个动作通道先按参考 rollout 标准差归一化；对候选转变点计算前后区间动作均值差异，CUSUM 阈值设为 5.0，最小段长为 8 个动作步。
  - 将相邻转变合并，并把代表转变位置对齐到最近的策略查询边界（动作块长度 K=8），得到最终阶段边界 β0 < β1 < … < βP。
  - 每个“阶段 × 模态”组合定义为一个归因块，同一模态在不同阶段被视为不同玩家。

- **闭环反事实归因**：
  - 对任意块子集 S，属于 S 的模态保持原输入，不属于 S 的模态用替换函数 rρm 替换（如均值、灰度、模糊、噪声等）。
  - 关键设计：不重放存储的参考观测，而是从当前可能已被干预改变的环境中实时获取输入，因此干预会通过后续状态、观测和动作闭环传播。
  - 定义联盟价值 Vρ(S) 为在固定初始状态下、相同种子条件下反事实 rollout 的最终任务成功率的期望。
  - 使用 Shapley 值公式计算每个块在所有联盟上下文中的平均边际贡献；为降低成本，采用均匀采样的块排列估计，实验中每个任务使用 128 个排列。

- **跨相位交互分析**：
  - 对较早块 i 和较晚块 j，计算联合效应是否偏离二者单独效应之和：
    I(i,j) = E[V(S∪{i,j}) − V(S∪{i}) − V(S∪{j}) + V(S)]。
  - 正值解释为互补性，负值解释为冗余或抑制性依赖。

- **传播与恢复分析**：
  - 轨迹距离 Dp 衡量反事实 rollout 与参考 rollout 在阶段 p 内的 3D 末端执行器位置偏差。
  - 行为恢复：用差分-in-differences 形式 Rec(i→j)，衡量后续块 j 是否使轨迹偏差相对于自然收敛进一步缩小。
  - 功能恢复：用 FRec(i→j) 衡量后续块 j 是否补偿早期块 i 丢失后对最终任务成功率的影响。
  - 明确区分“轨迹恢复/行为恢复”和“任务成功恢复/功能恢复”，二者可以独立发生。

## 3. 实验设计

- **数据集与场景**：
  - 使用 LIBERO 基准中的 30 个机器人操作任务：Spatial 10 个、Object 10 个、Goal 10 个。
  - 策略为 OpenVLA-OFT，动作块长度 K=8；每个任务使用固定初始状态和参考种子，先获得一个成功参考 rollout。

- **模态与干预核**：
  - 模态包括：第三人称视觉、手腕视觉、本体感受状态、语言指令。
  - 干预核包括：均值、灰度、模糊、噪声。主要归因结果使用均值干预，其他核用于稳健性检验。
  - 替换详细设置：视觉均值图像为 suite 特定像素均值；灰度设 RGB=128；模糊高斯半径 12；噪声标准差 40；本体感受使用 suite 特定均值 8D 状态；语言替换为空字符串。

- **对比方法**：
  - LOO（leave-one-out）：在全联盟中替换单个块，测量成功率下降。
  - Static Action Perturbation：基于扰动后的即时动作变化进行归因。
  - Random：随机排序。
  - 结构消融：Phase-restricted Shapley、Modality-only Shapley、Uniform query-aligned phases。

- **评估指标**：
  - 删除/插入测试的 partial AUC（pAUC），衡量归因排序是否忠实于闭环任务结果。
  - Top-modality removal effect：替换被识别为最重要的模态后，闭环成功率的下降幅度。
  - 交互 held-out 验证、行为/功能恢复率、阶段级轨迹距离等。

## 4. 资源与算力

- **论文未明确说明 GPU 型号、数量、训练时长或推理总计算量**。
- 文中仅给出了实验配置层面的采样预算：例如归因使用 128 个排列；交互筛选使用 32 contexts/pair，held-out 32 contexts/pair；传播/恢复使用 32 个 paired rollout seeds；faithfulness 测试对 k={1,2,3,4,5} 使用 16 replicates/task。
- 由于所有分析依赖大量闭环反事实 rollout，计算成本较高，但论文没有披露具体硬件资源和总体耗时。

## 5. 实验数量与充分性

- **主要实验规模**：
  - 30 个 LIBERO 任务，每个任务包含多个阶段和 4 种模态，因此归因块数量较多。
  - 4 种干预核，每种核下进行 128 排列的 Shapley 估计。
  - 受控验证实验：3 种数据生成情形 × 6 种采样预算（2、6、12、24、128、512）× 64 次重复。
  - Faithfulness：k=1…5，16 replicates/task。
  - 交互分析：1,046 个负交互跨相位对参与聚合统计；互补、冗余、阴性对照分别进行 held-out 验证。
  - 传播/恢复分析：每个条件使用 32 个 paired rollout seeds。
  - 统计检验：10,000 次 task-cluster bootstrap，报告 95% 百分位置信区间。

- **充分性与公平性评价**：
  - 实验设计较为系统，包含受控验证、多干预核稳健性检验、结构消融和 held-out 验证，方法论验证相对充分。
  - 以任务为统计单元、用 task-paired difference 和 bootstrap 进行方法比较，统计上较规范。
  - 但仍存在局限：每个任务只使用一个成功参考 rollout 定义阶段结构；插入 pAUC 绝对值较低；与 Phase-restricted Shapley 的 faithful 差异不显著，因此全局闭环归因的优势主要体现在交互和恢复分析上，而不是所有指标都全面领先。

## 6. 论文的主要结论与发现

- **阶段依赖模态贡献**：在 30 个任务中，25 个（83.3%）出现至少一次相邻阶段主导模态转换；在 92 次主导集变更中，37 次（40.2%）前后主导集无重叠。不同 LIBERO 套件呈现不同模式，如 Spatial 手腕视觉早期重要、本体感受后期重要；Goal 语言早期重要、手腕视觉和本体感受随后增加。
- **闭环归因更忠实**：在删除和插入 pAUC 上，IMPACT-VLA 均优于 Static Action Perturbation，且置信区间排除零；相比 LOO，删除差异不明确，但插入 pAUC 更高。说明基于最终任务成功的闭环反事实评估比即时动作敏感性更能识别任务关键信息。
- **时间分辨率有价值**：Action-transition-aligned phases 的 top-modality removal effect 高于 Uniform query-aligned phases 和 Modality-only Shap

- **时间分辨率有价值**：Action-transition-aligned phases 的 top-modality removal effect 高于 Uniform query-aligned phases 和 Modality-only Shapley，说明按动作转变对齐阶段能更有效捕捉任务关键时间信息；但在 faithful pAUC 上与 Phase-restricted Shapley 的差异未达到显著水平，表明其优势主要体现在对真实任务成功的影响和交互/恢复分析上，而非所有忠实度指标全面领先。
- **跨相位交互以负交互为主**：在检测到的跨相位交互中，负交互（抑制性依赖）占主导，尤其是早期阶段关键模态丢失会通过闭环状态传播削弱后续阶段的可用信息，表现为联合价值低于单独贡献之和；互补性正交互较少，主要出现在涉及语言指令与视觉/本体感受的跨阶段组合中。
- **行为恢复与功能恢复可分离**：传播分析显示，后续阶段有时能降低轨迹偏差（行为恢复），但无法恢复最终任务成功率（功能恢复）；功能恢复率显著低于行为恢复率，说明闭环任务成功对早期关键信息的丢失更加敏感，单纯轨迹层面的补偿不足以保证任务完成。
- **干预核稳健性与结构消融**：均值、灰度、模糊和噪声四种干预核下，阶段依赖贡献趋势总体一致，但模糊和噪声干预对视觉模态的归因影响更大；结构消融表明同时考虑阶段和模态的块定义优于仅按模态聚合或均匀分块，进一步支持了行为阶段对齐的必要性。
- **总体结论**：IMPACT-VLA 通过阶段对齐的块定义和闭环反事实 Shapley 归因，揭示了 VLA 策略中多模态贡献的阶段依赖性、跨相位抑制性交互以及行为/功能恢复的分离；这些发现无法由传统局部敏感性或聚合归因方法捕捉，为理解机器人操作策略的闭环决策机制提供了新视角，并可用于指导模态冗余设计、在线故障诊断和针对性策略改进。

（完）
