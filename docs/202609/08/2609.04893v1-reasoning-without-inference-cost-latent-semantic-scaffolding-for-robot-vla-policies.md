---
title: "Reasoning Without Inference Cost: Latent Semantic Scaffolding for Robot VLA Policies"
title_zh: 无推理成本推理：机器人VLA策略的潜在语义脚手架
authors: "Andrew Ting Yan Li, Zhuo Li, Zhelin Yang, Zhipeng Dong, Quentin Rouxel, Fei Chen"
date: 2026-09-04
pdf: "https://arxiv.org/pdf/2609.04893v1"
tags: ["query:vla"]
score: 9.0
evidence: 潜在语义脚手架将VLA动作标记与推理理由对齐，无推理成本
tldr: VLA模型仅模仿动作而缺乏因果推理，现有推理增强方法在推理时逐步生成推理令牌或预测未来状态，成本随时间累积。本文提出潜在语义脚手架LSS，训练时通过小型投影头将动作令牌表示与阶段级物理推理理由文本嵌入对齐，推理时丢弃投影头。Dense LSS按操作阶段逐令牌对齐，取得最佳分布内成功率和未见任务迁移，而Pooled LSS过度专化。该机制以零推理时开销赋予VLA因果推理收益，并显著提升跨任务泛化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.04893v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 939, \"height\": 547, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.04893v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1506, \"height\": 793, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.04893v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1494, \"height\": 707, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.04893v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 884, \"height\": 177, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.04893v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 881, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.04893v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 824, \"height\": 431, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.04893v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 952, \"height\": 426, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.04893v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 730, \"height\": 207, \"label\": \"Table\"}]"
motivation: VLA模型仅模仿动作缺乏因果推理，现有推理增强方法在推理时需生成推理令牌或预测未来状态，成本随长时程累积。
method: 提出潜在语义脚手架LSS，在演示预训练时通过投影头将动作令牌表示与阶段级物理推理理由文本嵌入对齐，推理时丢弃投影头。
result: Dense LSS（逐阶段令牌对齐）获得最佳分布内成功率与未见任务迁移，表示可分性约为Pooled LSS两倍，证明阶段局部对齐有效。
conclusion: 在训练时注入、推理时丢弃的语义对齐可为零开销VLA带来因果推理收益并提升跨任务泛化。
---

## 摘要
视觉-语言-动作（VLA）模型通过模仿进行训练，能捕捉要采取什么动作，但无法捕捉原因；添加因果推理可以改善操作能力，但现有方法在推理时为此付出代价——每一步都生成推理标记或推演预测的未来状态，这种成本在长时间跨度上不断累积。我们探究这一益处能否改为在训练期间获得，并在部署前丢弃。我们提出潜在语义脚手架（LSS），一种在人类示范预训练期间应用的辅助损失，它通过一个小型投影头将 VLA 的动作标记表示与物理推理理由的文本嵌入对齐。该投影头在推理时被丢弃，使未修改的基础策略零额外成本。我们的核心发现涉及对齐粒度：将每个动作标记对齐到其自身操作阶段的理由（稠密 LSS），而非对齐到单一池化的回合级嵌入（池化 LSS），能产生在留出任务上迁移效果显著更好的表示。稠密 LSS 既取得了最佳分布内成功率，也取得了对对齐期间未见任务的最佳迁移效果，而池化对齐则过度特化于训练任务。表示探针显示，稠密 LSS 在骨干网络中诱导出大约两倍的逐阶段可分离性，这支持了阶段局部对齐是有效机制的观点。

## Abstract
Vision-language-action (VLA) models are trained by imitation and capture what action to take but not why; adding causal reasoning improves manipulation, but current methods pay for it at inference time - generating reasoning tokens or rolling out predicted future states at every step, a cost that compounds over long horizons. We ask whether this benefit can instead be captured during training and discarded before deployment. We introduce Latent Semantic Scaffolding (LSS), an auxiliary loss applied during human-demonstration pretraining that aligns a VLA's action-token representations to text embeddings of physical-reasoning rationales through a small projection head. The head is dropped at inference, leaving the unmodified base policy with zero added cost. Our central finding concerns alignment granularity: aligning each action token to the rationale of its own manipulation phase (Dense LSS) rather than to a single pooled episode-level embedding (Pooled LSS) yields representations that transfer markedly better to held-out tasks. Dense LSS attains both the best in-distribution success and the best transfer to tasks unseen during alignment, whereas pooled alignment over-specializes to the training task. A representational probe shows Dense LSS induces roughly twice the per-phase separability in the backbone, supporting that phase-local alignment is the operative mechanism.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景与动机**：视觉-语言-动作（VLA）模型通过模仿学习掌握“做什么动作”，但通常不理解动作背后的因果原因。已有工作通过引入显式推理（如生成推理文本、预测未来视觉状态）来提升操作能力，但这些方法在推理阶段需要逐步生成推理 token 或推演未来状态，计算开销大，并且会随着任务时间跨度增长而不断累积。
- **核心问题**：是否可以把“推理带来的好处”在训练阶段就注入模型，而在部署时完全丢弃推理模块，从而避免推理时的额外成本？
- **整体含义**：论文提出一种训练时辅助损失，将 VLA 的动作 token 表示与物理推理理由的文本嵌入对齐，推理时不再需要任何推理生成或预测模块，实现“零推理成本”下的因果推理收益，并重点研究对齐粒度（逐阶段 vs. 回合级）对泛化的影响。

## 2. 论文提出的方法论

- **核心思想**：潜在语义脚手架（Latent Semantic Scaffolding, LSS）通过在人类演示预训练阶段引入辅助损失，将动作 token 的隐藏表示对齐到对应操作阶段的物理推理文本嵌入；训练结束后丢弃投影头与推理编码器，部署时使用未修改的基础 VLA 策略。
- **基础模型**：构建在 H-RDT 之上，使用 DINO+SigLIP 视觉编码器、冻结的 T5-XXL 任务指令编码器、2B 参数扩散 Transformer 骨干网络和流匹配动作头。骨干网络输出 K=16 个动作 token，维度为 2176。
- **三阶段训练流程**：
  1. 人类数据预训练；
  2. 在人类预训练数据上加入 LSS 辅助损失继续预训练；
  3. 在少量机器人数据上微调。
- **Pooled LSS（池化对齐）**：
  - 将动作 token 平均池化得到 $\bar{z}$；
  - 将整条轨迹的推理文本由 T5 编码并平均池化得到 $e_{\text{text}}$；
  - 通过一个轻量投影头 $g_\phi$ 将 $\bar{z}$ 映射到 T5 空间，并用余弦距离计算损失：
    $$L_{\text{LSS}} = 1 - \frac{g_\phi(\bar{z}) \cdot e_{\text{text}}}{\|g_\phi(\bar{z})\| \|e_{\text{text}}\|}$$
  - 总损失：$L_{\text{total}} = L_{\text{flow}} + \lambda L_{\text{LSS}}$，其中 $\lambda = 0.1$。
- **Dense LSS（稠密 / 阶段局部对齐）**：
  - 将推理理由拆分为 $P$ 个有序操作阶段（如 approach、grip、rotate、withdraw），每个阶段有独立的因果理由和帧范围；
  - 每个动作 token 根据其覆盖的帧被分配到对应阶段；跨越阶段边界的 token 被掩码排除（约 5%）；
  - 每个 token 分别通过同一个投影头，与其所属阶段的 T5 嵌入对齐：
    $$L_{\text{dense}} = \frac{1}{|M|} \sum_{i \in M} \left(1 - \frac{g_\phi(z_i) \cdot e_{p(i)}}{\|g_\phi(z_i)\| \|e_{p(i)}\|}\right)$$
  - 其中 $p(i)$ 是 token $i$ 所属阶段，$M$ 是非跨阶段 token 集合。
- **训练时丢弃设计**：LSS 的投影头、推理 T5 编码器和对齐损失仅在训练阶段存在；推理时不保存投影头，不实例化推理编码器，部署图与基础 VLA 完全相同。
- **推理理由生成**：使用 Gemini-3-flash 离线生成每条轨迹的因果理由；Dense LSS 还需要阶段分割信息，通过 VLM 返回阶段标签和时间分数范围，再转换为动作-帧范围。

## 3. 实验设计

- **数据集与场景**：
  - 人类演示数据：使用自采集的 500 条 `adjust_bottle` 演示，采用 EgoDex 格式，通过 Apple Vision Pro 记录 48 维手/腕运动，ZED 相机记录前景 RGB 视频，形成左右手 × 两种瓶子（Coke/Sprite）的平衡 2×2 设计。
  - 机器人评估基准：RoboTwin 2.0，使用 aloha-agilex 14 自由度配置。
  - 评估任务按相似性梯度设计：
    - `adjust_bottle`（分布内任务，与人类预训练任务一致）；
    - `shake_bottle`（相关瓶操作任务，近迁移）；
    - `move_can_pot`（结构差异较大的任务，远迁移）。
- **微调协议**：所有 Stage-3 微调都使用相同预算：50 条 HDF5 演示片段、22,000 训练步；成功率基于 100 次 rollouts。
- **对比方法 / 消融**：
  - H-RDT 基线；
  - 加入人体运动数据但无推理对齐（AVP，no LSS）；
  - 将推理文本作为语言输入；
  - 将推理文本作为 token 预测损失；
  - Pooled LSS；
  - Dense LSS；
  - 使用琐碎指令文本替代推理文本的 LSS；
  - 冻结 Stage-2 骨干进行微调；
  - 四种 Stage-2 初始化（R1–R4）在三个任务上的迁移比较。

## 4. 资源与算力

- 论文**未明确说明**所使用的 GPU 型号、GPU 数量、训练总时长或具体算力消耗。
- 文中仅给出了训练规模相关信息：骨干网络为 2B 参数、Stage-3 微调固定为 50 episodes 和 22,000 步、推理成功率来自 100 次 rollouts，但缺少硬件与时间成本细节。

## 5. 实验数量与充分性

- **实验规模**：从论文呈现的内容看，主要包括：
  - 表 I 的消融阶梯：约 8 种配置在 `adjust_bottle` 上的成功率对比；
  - 表 II 的迁移实验：4 种 Stage-2 初始化 × 3 个任务 = 12 个成功率结果；
  - 表示可分离性探针：Dense LSS 与 Pooled LSS 在两种基线下的 silhouette score 对比；
  - 数据收集：500 条人类演示。
- **公平性**：
  - Stage-3 微调预算完全一致（50 episodes / 22k steps），排除了微调数据量的影响；
  - R2–R4 共享相同人类演示数据，只改变对齐目标，因此 R3/R4 相对 R2 的差异可以归因于 LSS；
  - 基线在相同硬件上
