---
title: "EmbodiedSkills: A Unified Framework for Orchestrating, Training, and Deploying VLA Agents"
title_zh: EmbodiedSkills：一种用于编排、训练和部署VLA智能体的统一框架
authors: "Wei Wang, Wenqiao Zhang, Yutong Lin, Yuqian Yuan, Tianwei Lin, Jinhao Mao, Zhenxuan Fan, Mingjian Gao, Yang Dai, Wentong Li, Zheqi Lv, Zheng Dong, Yingjie Niu, Jiaqi Zhu, Jun Xiao, Chao Li, Yueting Zhuang"
date: 2026-09-01
pdf: "https://arxiv.org/pdf/2609.01281v1"
tags: ["query:vla"]
score: 9.0
evidence: 面向长时程任务编排VLA智能体的统一框架
tldr: "在长时程具身任务中，VLA 模型仅做动作预测难以保证所提操作在当前状态有效且结果被验证。EmbodiedSkills 将每个技能决策视为执行提案，通过共享可执行技能接口在运行时检查前提条件并验证结果，形成统一代理循环。任务适应低层 VLA 策略在 RoboTwin 2.0 的 50 个任务上平均成功率达 86.20%，在 LIBERO 四套件上达 97.40%，但在 RMBench 四个记忆任务上仅 12.5%。该框架提供可训练和可检查的代理层，将低层策略转变为闭环具身系统。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.01281v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1696, \"height\": 848, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.01281v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1715, \"height\": 709, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.01281v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1686, \"height\": 666, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.01281v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1658, \"height\": 788, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.01281v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1728, \"height\": 655, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.01281v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1056, \"height\": 1662, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.01281v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 591, \"height\": 336, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.01281v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 799, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.01281v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1362, \"height\": 879, \"label\": \"Table\"}]"
motivation: 长时程任务要求协调感知、规划、执行、验证与恢复，而现有 VLA 动作预测无法保证操作有效性和结果验证。
method: 将每个技能决策当作执行提案，通过共享可执行技能接口在运行时检查前提条件并验证结果，连接高层选择、低层 VLA 执行和动作后验证。
result: "在 RoboTwin 2.0 50 个任务上平均成功率 86.20%，LIBERO 四套件上 97.40%；RMBench 四个记忆任务上 12.5%。"
conclusion: EmbodiedSkills 提供可训练和可检查的代理层，将任务适应低层 VLA 策略转变为闭环具身系统。
---

## 摘要
视觉-语言-动作（VLA）模型将视觉观察和语言指令直接映射到机器人动作，但长时程任务所需的不仅仅是动作预测。随着物理状态演化，智能体必须协调感知、规划、执行、进度验证与恢复。仅凭动作预测或模型生成的技能决策，本身并不能保证所提议的操作在当前状态下有效，也不能保证其结果会得到验证。我们提出 EmbodiedSkills，一个统一框架，将每个技能决策视为一个执行提议：运行时在执行前检查其前提条件，并在执行后验证结果。共享的可执行技能接口在单一智能体循环中连接高层技能选择、有界低层VLA执行和动作后验证。由于该接口保持固定，低层VLA策略可以被替换或调整，而无需改变智能体循环。该接口还将规划、执行、验证和恢复事件记录为结构化轨迹，为各个组件提供监督，并可在交互式反馈可用时支持可选的在线适应。我们在 RoboTwin 2.0 和 LIBERO 上使用 Qwen3-VL 和 OpenPI/pi0.5 实例化 EmbodiedSkills。任务适应的低层VLA策略在50个RoboTwin 2.0任务上实现86.20%的平均成功率，在四个LIBERO套件上实现97.40%的平均成功率。这些结果确立了 EmbodiedSkills 中所用的任务适应低层VLA策略的执行性能。在四个依赖记忆的 RMBench 任务上，相同的任务适应执行方法实现了12.5%的平均成功率。该框架提供了一个可训练且可检查的智能体层，用于将这些策略转化为闭环具身系统。

## Abstract
Vision-language-action (VLA) models map visual observations and language instructions directly to robot actions, but long-horizon tasks require more than action prediction. An agent must coordinate perception, planning, execution, progress verification, and recovery as the physical state evolves. An action prediction or a model-generated skill decision does not, by itself, guarantee that the proposed operation is valid in the current state or that its outcome will be verified. We propose EmbodiedSkills, a unified framework that treats each skill decision as an execution proposal: the runtime checks its prerequisites before execution and verifies the outcome afterward. A shared executable-skill interface connects high-level skill selection, bounded low-level VLA execution, and post-action verification within a single agent loop. Because this interface remains fixed, low-level VLA policies can be replaced or adapted without changing the agent loop. The interface also records planning, execution, verification, and recovery events as structured trajectories, which provide supervision for individual components and can support optional online adaptation when interactive feedback is available. We instantiate EmbodiedSkills with Qwen3-VL and OpenPI/pi0.5 on RoboTwin 2.0 and LIBERO. Task-adapted low-level VLA policies achieve an average success rate of 86.20% across 50 RoboTwin 2.0 tasks and 97.40% across the four LIBERO suites. These results establish the execution performance of the task-adapted low-level VLA policies used in EmbodiedSkills. On four memory-dependent RMBench tasks, the same task-adapted execution approach achieves 12.5% average success. The framework provides a trainable and inspectable agent layer for turning these policies into closed-loop embodied systems.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景**：视觉-语言-动作（VLA）模型虽然能将视觉观察和语言指令直接映射为机器人动作，但长时程（long-horizon）任务要求智能体在物理状态动态变化的过程中协调感知、规划、执行、进度验证与恢复等多种能力，仅靠动作预测并不足够。
- **核心问题**：模型生成的动作预测或技能决策本身无法保证：
  - 提议的操作在当前物理状态下是有效的；
  - 执行结果能够被验证；
  - 失败能被显式诊断（是对象定位、子目标选择、低层执行、进度验证还是恢复环节出错）。
- **整体含义**：论文提出 **EmbodiedSkills**，一个统一框架，把每个技能决策视为“执行提案”：运行时在执行前检查前提条件，执行后验证结果。其目标是将低层 VLA 策略提升为可靠的闭环具身系统，而不是替换现有 VLA 策略。

## 2. 论文提出的方法论

### 2.1 核心思想：策略提出 + 运行时守卫执行

- **策略-运行时分离**：高层智能体策略提出结构化操作（技能调用、前进阶段、结束运行），运行时独立执行语义检查，阻止无效决策被转换成物理动作。
- **共享可执行技能契约**：每个技能 `k` 定义为六元组：
  `k = (X_k, Y_k, pre_k, exec_k, post_k, fail_k)`
  其中 `pre_k` 是前提条件，`exec_k` 是实际操作，`post_k` 定义状态更新，`fail_k` 将失败映射为显式状态和证据。
- **技能接口固定**：低层 VLA 策略可被替换或微调，无需改变 AgentLoop。

### 2.2 六阶段闭环 AgentLoop

框架将长时程任务组织为六个语义阶段（非固定流水线，允许条件跳转）：

| 阶段 | 作用 |
|------|------|
| Observe | 获取当前视觉和任务相关证据 |
| Plan | 将任务分解为可验证的子目标序列 |
| Preflight | 检查当前子目标是否具备执行条件 |
| Execute | 调用低层 VLA 策略生成并执行一个有界动作块（action chunk） |
| Verify | 用最新动作后观测判断子目标是否取得进展 |
| Recover | 在不可继续的失败后修改子目标/计划，安全重入 |

### 2.3 关键机制与算法流程

- **智能体状态**：`s_t = (z_t, M_t, H_t)`，其中 `z_t` 是当前阶段，`M_t` 是任务工件集合，`H_t` 是有序循环轨迹。
- **策略决策**：
  - `d_t ∈ { RunSkill(k, q), AdvanceStage, FinishRun }`
  - 决策条件在部署一致的紧凑上下文 `C_t` 和状态允许的技能集合 `A_t` 上。
- **运行时守卫**：
  - 技能守卫：`G_skill(s_t, d_t)` 检查阶段匹配、技能允许性、前置条件等。
  - 前进守卫：`G_advance(s_t, d_t)` 检查当前阶段是否已生成后继所需证据。
  - 结束守卫：`G_finish(d_t)`。
- **有界动作执行**：低层 VLA 策略输出 `a_t = (τ_t, U_t, H_t, η_t)`，运行时检查动作类型、维度、数值有效性、执行长度以及与当前子目标和观测的一致性。
- **验证与恢复路由**：验证阶段输出语义路由 `{ Advance, Continue, Reobserve, Replan, Recover, Finish }`，子目标可以跨多个有界动作块继续执行。
- **算法总结**（Algorithm 1）：在每个时间步构建上下文，计算允许技能，策略提出决策，守卫检查，执行并通过结果更新状态和轨迹。

### 2.4 训练接口

- **组件级监督适应**：规划器、低层 VLA 策略、验证器、调度器可独立训练，共享固定运行时契约。
- **部署一致训练样本**：训练样本 `x_t = (g, z_t, I_t, es_t, K_t, h_t)` 只包含部署时可见的输入，损失只作用于生成 token。
- **可选闭环适应**：收集交互轨迹 `τ`，支持基于组相对优势 `A_i` 和掩码裁剪目标 `L_online` 的在线策略优化，但被定位为辅助手段，非主结果来源。

## 3. 实验设计

- **数据集/场景**：
  - **RoboTwin 2.0**：50 个双手操作任务，每任务 100 次评估；域随机化、多种物体配置和机器人本体。
  - **LIBERO**：四个套件——Spatial、Object、Goal、Long，测试空间、物体、目标和长时程泛化。
  - **RMBench**：四个记忆依赖任务（Battery Try、Blocks Ranking Try、Cover Blocks、Press Button），用于评估依赖交互历史的执行。
- **基准比较**：
  - RoboTwin 2.0 上对比策略基线：ACT、DP、RDT、DP3，以及通用 VLA：π0、X-VLA、π0.5（参考 LingBot-VA 报告值）。
  - LIBERO 上对比官方 OpenPI 参考值。
  - RMBench 上对比 DP、ACT、π0.5、X-VLA 的已发表结果。
- **消融实验**：在 RoboTwin 2.0 50 任务、5000 个评估集上进行了三个控制配置对比：
  - **Full**：完整计划-执行-验证循环。
  - **w/o Verify**：保留子任务序列和总预算，但不进行中间验证。
  - **w/o Subtask**：每个动作块只接收原始整体任务指令。
  - **One chunk/subtask**：每个子任务分配一个 32 步动作块，无中间重试。

## 4. 资源与算力

- 论文**未明确说明**训练所使用的 GPU 型号、数量、训练时长、显存或具体计算成本。
- 仅提到对 RoboTwin 2.0 的 50 个任务分别微调独立的 π0.5 策略，以及训练 Qwen3-VL 调度器，但未给出算力细节。
- 这是一个信息缺口，影响其可复现性和资源评估。

## 5. 实验数量与充分性

- **实验规模**：
  - 主实验覆盖 3 个基准：RoboTwin 2.0（50 任务×100 次 = 5000 次评估）、LIBERO（4 套件）、RMBench（4 任务）。
  - 消融实验额外 3 配置 × 50 任务 × 100 次 = 15,000 次评估。
- **充分性**：
  - 在任务数量和基准多样性上较充分，覆盖短时程、长时程、记忆依赖等场景。
  - 消融设计严格，控制了初始状态、规划器、低层策略和终端评估器，变量隔离较好。
- **客观性与公平性**：
  - 基线结果来自文献或官方发布，而非统一重新评估，可能存在一定偏差（如不同实现细节、评估协议差异）。
  - RoboTwin 2.0 上“Ours”是任务特定微调策略，基线 π0.5 是通用策略参考，对比不完全对等。
  - 缺少真实机器人实验，所有结果均为仿真基准。

## 6. 论文的主要结论与发现

- **执行性能**：
  - RoboTwin 2.0 上任务适应低层 VLA 策略平均成功率达 **86.20%**，比 LingBot-VA 报告的 π0.5（82.74%）提高 **3.46 个百分点**，在 39/50 任务上优于参考。
  - LIBERO 四套件平均成功率达 **97.40%**，优于官方 OpenPI（96.85%），在 LIBERO-Long 上提升最明显（+1.2 点）。
  - RMBench 四个记忆任务平均成功率仅 **12.5%**，说明当前任务适应执行方法在依赖记忆的复杂交互中仍有明显不足。
- **消融结论**：
  - 去掉中间验证后，平均成功率从 86.20% 暴跌至 **48.2%**（−38.0 点）。
  - 去掉子任务条件降至 **34.4%**（−51.8 点）。
  - 每子任务仅一个动作块降至 **19.5%**。
  - 证明显式子任务分解、动作后验证和自适应继续执行三者的结合非常关键。

## 7. 优点

- **清晰的策略-运行时分离**：将“模型提案”与“物理执行语义”解耦，错误可追踪，避免无效决策直接转化为动作。
- **统一可执行技能接口**：跨规划、执行、验证、恢复共享固定契约，支持模块化替换和独立训练。
- **结构化轨迹**：为诊断、监督学习和可选在线适应提供统一数据接口。
- **消融实验严谨**：在统一评估条件下隔离三个核心机制，结论有说服力。
- **跨基准验证**：在 RoboTwin 2.0、LIBERO、RMBench 上均进行了评估，不绑定单一环境。
- **训练接口设计合理**：组件级监督为主，在线 RL 仅为可选辅助，避免了复杂且昂贵的端到端联合训练。

## 8. 不足与局限

- **任务特定策略成本高**：RoboTwin 2.0 上为每个任务训练一个专家策略，训练、存储和部署成本高于单一通用策略。
- **记忆依赖任务性能差**：RMBench 四个任务平均仅 12.5%，说明在需要长期交互历史的场景中，当前框架仍远不能解决核心困难。
- **缺少真实机器人验证**：所有结果均为仿真，物理接触、延迟、传感器噪声等现实挑战未被测试。
- **额外延迟与计算开销**：多次 VLM 调用和动作后观测引入额外延迟，相比单次前传策略更慢。
- **验证能力受视觉限制**：当任务进度被遮挡、视觉模糊或依赖不可见属性时，验证器可能出错。
- **模块化协同权衡**：分离的规划器、调度器、验证器和 VLA 策略适配器增加接口匹配复杂性，长序列中可能累积误差。
- **基线对比公平性待加强**：“Ours”与通用基线的训练方式和数据量不完全对等，文献引用值不一定是统一复现结果。

（完）
